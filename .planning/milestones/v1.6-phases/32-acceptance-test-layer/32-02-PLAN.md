---
phase: 32-acceptance-test-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/agents/gsd-plan-checker.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/agents/gsd-auto-context.md
autonomous: true
requirements: [AT-04, AT-05]

must_haves:
  truths:
    - "gsd-plan-checker.md contains a Dimension 9: Acceptance Test Coverage section"
    - "Dimension 9 checks that each AT-{NN} from CONTEXT.md is referenced in at least one plan task"
    - "Missing AT coverage is a blocking issue (severity: blocker)"
    - "execute-plan.md contains an <acceptance_test_ownership> section declaring ATs read-only"
    - "gsd-auto-context.md explicitly states that <acceptance_tests> block is omitted in auto-context mode"
    - "The plan-checker Dimension 9 gracefully skips when no <acceptance_tests> block exists"
  artifacts:
    - path: "get-shit-done/agents/gsd-plan-checker.md"
      provides: "Plan checker with Dimension 9 for AT coverage"
    - path: "get-shit-done/workflows/execute-plan.md"
      provides: "Execute plan with AT ownership invariant instruction"
    - path: "get-shit-done/agents/gsd-auto-context.md"
      provides: "Auto-context agent with AT omission awareness"
  key_links:
    - from: "gsd-plan-checker.md Dimension 9"
      to: "CONTEXT.md <acceptance_tests> block"
      via: "Reads AT-{NN} identifiers from context, checks plan tasks for references"
    - from: "execute-plan.md <acceptance_test_ownership>"
      to: "CONTEXT.md <acceptance_tests> block"
      via: "Instruction tells executor the block is read-only"
---

<objective>
Add acceptance test coverage checking to plan-checker, ownership invariant to execute-plan, and auto-context awareness.

Purpose: Ensure plans cover all acceptance tests before execution begins (AT-04), prevent AI from modifying acceptance tests after discuss-phase approval (AT-05), and ensure auto-context mode does not fabricate acceptance tests.

Output: Updated gsd-plan-checker.md with Dimension 9; updated execute-plan.md with ownership invariant; updated gsd-auto-context.md with AT omission note.
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/32-acceptance-test-layer/32-CONTEXT.md
@.planning/phases/32-acceptance-test-layer/32-RESEARCH.md
@get-shit-done/agents/gsd-plan-checker.md
@get-shit-done/workflows/execute-plan.md
@get-shit-done/agents/gsd-auto-context.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dimension 9 to gsd-plan-checker.md</name>
  <files>get-shit-done/agents/gsd-plan-checker.md</files>
  <action>
Add a new Dimension 9: Acceptance Test Coverage section to gsd-plan-checker.md. Insert it AFTER the Dimension 8 closing content (after the "If FAIL: return to planner with specific fixes. Same revision loop as other dimensions (max 3 loops)." line) and BEFORE the `</verification_dimensions>` closing tag.

The new dimension:

```markdown
## Dimension 9: Acceptance Test Coverage

**Skip if:** CONTEXT.md has no `<acceptance_tests>` block. Output: "Dimension 9: SKIPPED (no acceptance tests defined)"

**Question:** Does every acceptance test from CONTEXT.md have covering task(s) in the plans?

**Process:**
1. Parse the `<acceptance_tests>` block from CONTEXT.md
2. Extract all AT-{NN} identifiers (e.g., AT-01, AT-02, ...)
3. For each AT-{NN}, search all plan task `<action>` and `<done>` elements for the identifier string
4. Flag any AT-{NN} with zero references across all plans

**Red flags:**
- AT-{NN} identifier appears in zero plan tasks (no coverage)
- AT-{NN} is referenced but only in a verification section, not in task action/done (no implementation)

**CRITICAL:** Missing coverage for any acceptance test is a **blocking** issue. Acceptance tests represent the human's contract -- uncovered tests mean the contract will not be fulfilled.

**Example issue:**
```yaml
issue:
  dimension: acceptance_test_coverage
  severity: blocker
  description: "AT-03 (verify commands map to pass/fail) has no covering task"
  acceptance_test: "AT-03"
  fix_hint: "Add AT-03 reference in a task action or done criteria that implements the verify command execution"
```

**Note:** Plans must NOT add, remove, or modify acceptance tests. Dimension 9 only checks coverage -- it verifies that plans reference existing ATs, not that plans create or change them.
```

Also update the success_criteria checklist at the bottom of the file to add:
- `- [ ] Acceptance test coverage checked (if <acceptance_tests> block exists in CONTEXT.md)`
  </action>
  <verify>
    <automated>grep -c "Dimension 9" get-shit-done/agents/gsd-plan-checker.md</automated>
  </verify>
  <done>gsd-plan-checker.md contains Dimension 9: Acceptance Test Coverage that parses AT-{NN} identifiers from CONTEXT.md, checks plan tasks for references, flags missing coverage as blocker severity, and gracefully skips when no acceptance tests exist</done>
</task>

<task type="auto">
  <name>Task 2: Add ownership invariant to execute-plan.md and AT omission to auto-context</name>
  <files>get-shit-done/workflows/execute-plan.md, get-shit-done/agents/gsd-auto-context.md</files>
  <action>
**Part A: execute-plan.md ownership invariant**

Add an `<acceptance_test_ownership>` section to execute-plan.md. Insert it AFTER the `</test_gate>` closing tag and BEFORE the `<tdd_plan_execution>` section:

```xml
<acceptance_test_ownership>

## Acceptance Test Ownership

The `<acceptance_tests>` block in CONTEXT.md is **read-only**. You MUST NOT add, remove, or modify any acceptance test entries (AT-{NN} identifiers, Given/When/Then lines, or Verify commands).

**What you CAN do:**
- Reference AT-{NN} identifiers in your work to show which acceptance test a task addresses
- Ensure your implementation satisfies the acceptance test's Then/Verify criteria

**What you MUST NOT do:**
- Edit the `<acceptance_tests>` block in CONTEXT.md
- Create new AT entries
- Change Verify commands to match your implementation (fix your code to match the test, not the reverse)

If an acceptance test's Verify command fails against your implementation, debug your code -- do not modify the acceptance test.

</acceptance_test_ownership>
```

**Part B: gsd-auto-context.md AT omission**

Read gsd-auto-context.md and find the section where it describes the CONTEXT.md template/output structure. Add a note about `<acceptance_tests>` omission. The note should be placed near the template output section (where the agent learns what sections to generate). Add:

```markdown
**`<acceptance_tests>` section:** Do NOT generate this section. Acceptance tests are human-defined during interactive discuss-phase only. In auto-context mode (where you operate), the `<acceptance_tests>` block is intentionally omitted. Never fabricate acceptance tests from requirements or success criteria -- this is explicitly deferred per REQUIREMENTS.md ("interactive-only for v1.6").
```

Find the most appropriate location in the agent file -- likely in the template or output section where CONTEXT.md structure is described. If there is a list of sections the agent generates (domain, decisions, specifics, code_context, deferred), add a note there clarifying that acceptance_tests is explicitly excluded.
  </action>
  <verify>
    <automated>grep -c "acceptance_test_ownership" get-shit-done/workflows/execute-plan.md && grep -c "acceptance_tests" get-shit-done/agents/gsd-auto-context.md</automated>
  </verify>
  <done>execute-plan.md contains <acceptance_test_ownership> section declaring ATs read-only with clear do/don't rules. gsd-auto-context.md contains explicit instruction to omit <acceptance_tests> section in auto-context mode.</done>
</task>

</tasks>

<verification>
1. `grep "Dimension 9" get-shit-done/agents/gsd-plan-checker.md` returns matches
2. `grep "acceptance_test_coverage" get-shit-done/agents/gsd-plan-checker.md` returns matches
3. `grep "acceptance_test_ownership" get-shit-done/workflows/execute-plan.md` returns matches
4. `grep "acceptance_tests" get-shit-done/agents/gsd-auto-context.md` returns matches
5. Dimension 9 includes blocker severity for missing coverage
6. Dimension 9 includes skip condition for missing acceptance_tests block
7. Ownership section is positioned after test_gate and before tdd_plan_execution
</verification>

<success_criteria>
- gsd-plan-checker.md has Dimension 9 for AT coverage checking
- Dimension 9 parses AT-{NN} from CONTEXT.md and checks plan tasks
- Missing AT coverage is severity: blocker
- Dimension 9 gracefully skips when no acceptance_tests block exists
- execute-plan.md has acceptance_test_ownership section with read-only instruction
- gsd-auto-context.md explicitly omits acceptance_tests in auto mode
</success_criteria>

<output>
After completion, create `.planning/phases/32-acceptance-test-layer/32-02-SUMMARY.md`
</output>
