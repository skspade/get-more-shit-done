---
phase: 32-acceptance-test-layer
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - get-shit-done/workflows/verify-phase.md
autonomous: true
requirements: [AT-03]

must_haves:
  truths:
    - "verify-phase.md contains a verify_acceptance_tests step that reads <acceptance_tests> from CONTEXT.md"
    - "The step executes each AT's Verify command via shell and captures exit code, stdout, and stderr"
    - "Exit code 0 maps to pass, non-zero maps to fail"
    - "Acceptance test results appear in VERIFICATION.md as a dedicated section"
    - "A failing acceptance test sets overall status to gaps_found (blocker)"
    - "The step gracefully skips when no <acceptance_tests> block exists in CONTEXT.md"
  artifacts:
    - path: "get-shit-done/workflows/verify-phase.md"
      provides: "Verify-phase workflow with acceptance test execution step"
  key_links:
    - from: "verify-phase.md verify_acceptance_tests step"
      to: "CONTEXT.md <acceptance_tests> block"
      via: "Reads AT entries and executes Verify commands"
    - from: "verify-phase.md verify_acceptance_tests step"
      to: "VERIFICATION.md report"
      via: "AT results written as dedicated section in report"
---

<objective>
Add acceptance test execution to the verify-phase workflow.

Purpose: During verify-phase, each acceptance test's Verify command is executed and the result maps to a verification truth (pass/fail with evidence). This closes the loop: humans define tests in discuss-phase, AI implements against them, and verify-phase confirms they pass.

Output: Updated verify-phase.md with `verify_acceptance_tests` step.
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/32-acceptance-test-layer/32-CONTEXT.md
@.planning/phases/32-acceptance-test-layer/32-RESEARCH.md
@get-shit-done/workflows/verify-phase.md
@get-shit-done/templates/verification-report.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verify_acceptance_tests step to verify-phase.md</name>
  <files>get-shit-done/workflows/verify-phase.md</files>
  <action>
Add a new `<step name="verify_acceptance_tests">` to verify-phase.md. Insert it AFTER the `verify_requirements` step and BEFORE the `scan_antipatterns` step. This positions acceptance test verification alongside other verification checks but as a distinct, dedicated step.

The new step:

```xml
<step name="verify_acceptance_tests">
Check if CONTEXT.md contains an `<acceptance_tests>` block:

```bash
CONTEXT_FILE=$(ls "$PHASE_DIR"/*-CONTEXT.md 2>/dev/null | head -1)
if [ -n "$CONTEXT_FILE" ]; then
  grep -q "<acceptance_tests>" "$CONTEXT_FILE" 2>/dev/null
fi
```

**If no CONTEXT.md or no `<acceptance_tests>` block:** Skip this step. Log: "Acceptance tests: SKIPPED (none defined)". This is normal -- phases without interactive discuss-phase or with test.acceptance_tests disabled will not have acceptance tests.

**If `<acceptance_tests>` block exists:**

1. **Parse acceptance tests:** Read the `<acceptance_tests>` block from CONTEXT.md. Extract each AT entry:
   - Identifier (AT-{NN})
   - Title (from ### heading)
   - Given, When, Then lines
   - Verify command (the shell command inside backticks on the Verify: line)

2. **Execute each Verify command:**

For each AT entry:
```bash
# Execute the Verify command, capture exit code and output
VERIFY_OUTPUT=$(eval "{verify_command}" 2>&1)
VERIFY_EXIT=$?
```

**Map result:**
| Exit Code | Status | Meaning |
|-----------|--------|---------|
| 0 | PASS | Acceptance test satisfied |
| Non-zero | FAIL | Acceptance test not satisfied |

Capture both stdout and stderr as evidence. If the command times out (>120s), treat as FAIL with evidence "Command timed out after 120 seconds".

3. **Record results:**

Build an acceptance test results table:
```
| AT ID | Title | Status | Evidence |
|-------|-------|--------|----------|
| AT-01 | {title} | PASS | {first line of output or "exit 0"} |
| AT-02 | {title} | FAIL | {error output or exit code} |
```

4. **Determine impact on overall status:**

- If ANY acceptance test FAILs: this is a **blocker**. Set overall verification status to `gaps_found`.
- Acceptance test failures are equivalent to truth failures -- the human's contract was not met.
- Include failing ATs in the fix plan generation (same as other gaps).

5. **Write to VERIFICATION.md report:**

Add a dedicated `## Acceptance Test Results` section to the verification report, positioned after `## Requirements Coverage` and before `## Anti-Patterns`. The section contains:

```markdown
## Acceptance Test Results

**Source:** {CONTEXT.md path}
**Tests:** {total} | **Passed:** {pass_count} | **Failed:** {fail_count}

| AT ID | Title | Status | Evidence |
|-------|-------|--------|----------|
| AT-01 | {title} | PASS | {evidence} |
| AT-02 | {title} | FAIL | {evidence} |

{If any failures:}
### Failing Acceptance Tests

**AT-{NN}: {title}**
- Expected: {Then line}
- Verify command: `{command}`
- Exit code: {code}
- Output:
  ```
  {stdout + stderr, truncated to 20 lines}
  ```
```

If all acceptance tests pass, the section shows the summary table only (no failure details needed).
</step>
```

Also update the `<step name="determine_status">` to mention acceptance tests:

Add to the **gaps_found** condition list: "any acceptance test FAIL". This ensures the determine_status logic accounts for AT failures alongside truth failures, artifact issues, and anti-pattern blockers.

Update the `<success_criteria>` checklist at the bottom to add:
- `- [ ] Acceptance tests executed (if <acceptance_tests> block exists in CONTEXT.md)`
  </action>
  <verify>
    <automated>grep -c "verify_acceptance_tests" get-shit-done/workflows/verify-phase.md</automated>
  </verify>
  <done>verify-phase.md contains a verify_acceptance_tests step that: parses AT entries from CONTEXT.md, executes each Verify command capturing exit code + output, maps results to pass/fail, writes results as dedicated section in VERIFICATION.md, sets gaps_found status on any failure, and gracefully skips when no acceptance tests exist</done>
</task>

</tasks>

<verification>
1. `grep "verify_acceptance_tests" get-shit-done/workflows/verify-phase.md` returns matches
2. The step appears between verify_requirements and scan_antipatterns
3. The step checks for <acceptance_tests> block and skips gracefully if absent
4. The step executes Verify commands and captures exit codes
5. The step maps exit 0 to PASS and non-zero to FAIL
6. The step writes results to VERIFICATION.md as a dedicated section
7. Failing ATs set status to gaps_found
8. The determine_status step mentions acceptance test failures
</verification>

<success_criteria>
- verify-phase.md has verify_acceptance_tests step in correct position
- Step parses acceptance tests from CONTEXT.md
- Step executes Verify commands via shell
- Exit code 0 = PASS, non-zero = FAIL
- Both stdout and stderr captured as evidence
- Results written as dedicated VERIFICATION.md section
- Failing AT is a blocker (status = gaps_found)
- Graceful skip when no acceptance tests exist
</success_criteria>

<output>
After completion, create `.planning/phases/32-acceptance-test-layer/32-03-SUMMARY.md`
</output>
