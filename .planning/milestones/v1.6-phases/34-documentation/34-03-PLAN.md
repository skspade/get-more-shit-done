---
phase: 34-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [README.md, docs/CLI.md]
autonomous: true
requirements: [DOC-03]

must_haves:
  truths:
    - README.md contains a "### Test Configuration" subsection in the Configuration section
    - README.md Utilities table includes /gsd:audit-tests
    - README.md standalone CLI section includes gsd test-count
    - docs/CLI.md includes gsd test-count command documentation
  artifacts:
    - README.md (modified)
    - docs/CLI.md (modified)
  key_links:
    - Test configuration example JSON with test.* keys
    - Link to USER-GUIDE.md for full test documentation
    - audit-tests command in Utilities table
    - test-count in CLI commands list and CLI.md
---

<objective>
Update README.md with test configuration section and update docs/CLI.md with test-count command documentation.

Purpose: Users can discover test configuration from the README and find test-count in the CLI reference.
Output: Modified `README.md` with test configuration subsection, updated command tables, and modified `docs/CLI.md` with test-count command documentation.
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@docs/CLI.md
@.planning/phases/34-documentation/34-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test configuration and commands to README.md</name>
  <files>README.md</files>
  <action>
Make three updates to README.md:

**1. Add `/gsd:audit-tests` to the Utilities command table** (the table under `### Utilities`):

Insert this row after the `/gsd:brainstorm [topic]` row:

```markdown
| `/gsd:audit-tests` | Run on-demand test suite health check |
```

**2. Add `gsd test-count` to the standalone CLI section:**

In the standalone CLI code block (under `### Standalone CLI`), add `gsd test-count` after `gsd health`:

```bash
gsd progress    # Show milestone progress
gsd todos       # List pending todos
gsd health      # Validate project integrity
gsd test-count  # Count test cases in project
gsd settings    # View/update configuration
gsd help        # Show available commands
```

**3. Add a "### Test Configuration" subsection** in the `## Configuration` section. Insert it AFTER the "### Workflow Agents" subsection (after the `/gsd:plan-phase --skip-verify` paragraph) and BEFORE the "### Execution" subsection:

```markdown
### Test Configuration

GSD includes a dual-layer test system: human-defined acceptance tests and AI-managed unit/regression tests with a hard gate. All features work with zero configuration.

| Setting | Default | What it controls |
|---------|---------|------------------|
| `test.hard_gate` | `true` | Run test suite after each task commit |
| `test.acceptance_tests` | `true` | Gather acceptance tests during discuss-phase |
| `test.budget.per_phase` | `50` | Per-phase test count limit |
| `test.budget.project` | `800` | Project-wide test count limit |
| `test.steward` | `true` | Enable test steward during audit-milestone |
| `test.command` | auto-detected | Override test runner command |
| `test.framework` | auto-detected | Override framework detection |

```json
{
  "test": {
    "hard_gate": true,
    "budget": { "per_phase": 50, "project": 800 }
  }
}
```

See the [User Guide](docs/USER-GUIDE.md#test-architecture) for the full test architecture documentation.
```

Preserve all existing content. Match the existing formatting style (compact tables, brief descriptions, links to USER-GUIDE for details).
  </action>
  <verify>
    <automated>grep -c "audit-tests" README.md && grep -c "test-count" README.md && grep -c "test.hard_gate" README.md && grep -c "Test Configuration" README.md</automated>
  </verify>
  <done>README.md contains /gsd:audit-tests in Utilities table, gsd test-count in standalone CLI section, and a Test Configuration subsection with config table and JSON example</done>
</task>

<task type="auto">
  <name>Task 2: Add gsd test-count command to CLI.md</name>
  <files>docs/CLI.md</files>
  <action>
Add a `### gsd test-count` section to docs/CLI.md. Insert it AFTER the `### gsd health` section and BEFORE the `### gsd settings` section. Follow the exact documentation pattern used by the existing commands (usage, description, flags, examples, JSON output fields).

```markdown
### gsd test-count

Count test cases across the project.

\`\`\`
gsd test-count [--phase <N>] [--json] [--plain]
\`\`\`

Counts individual test cases (`it`/`test` blocks) in the project's test files. Supports filtering by phase to see per-phase test counts.

**Flags:**

| Flag | Description |
|------|-------------|
| `--phase <N>` | Count tests only for the specified phase number |
| `--json` | Output as structured JSON |
| `--plain` | Output as plain text with ANSI color codes stripped |

**Example (rich mode):**

\`\`\`
gsd test-count
\`\`\`

\`\`\`
606 tests
\`\`\`

**Example (per-phase):**

\`\`\`
gsd test-count --phase 30
\`\`\`

\`\`\`
12 tests (phase 30)
\`\`\`

**JSON output fields:**

| Field | Description |
|-------|-------------|
| `count` | Total number of test cases |
| `phase` | Phase number filter (if `--phase` used), or `null` |

---
```

Also update the `gsd help` section's overview mode command list to include test-count:

In the code block showing `gsd help` output, add this line after the `health` entry:

```
  test-count  Count test cases in project
```

Preserve all existing content. Match the existing formatting style.
  </action>
  <verify>
    <automated>grep -c "gsd test-count" docs/CLI.md && grep -c "test-count" docs/CLI.md && grep -c "Count test cases" docs/CLI.md</automated>
  </verify>
  <done>docs/CLI.md contains a gsd test-count section with usage, flags, examples, and JSON output fields matching the format of other CLI command docs. Help overview includes test-count.</done>
</task>

</tasks>

<verification>
- [ ] README.md has /gsd:audit-tests in Utilities table
- [ ] README.md has gsd test-count in standalone CLI code block
- [ ] README.md has Test Configuration subsection with 7 config keys
- [ ] README.md has JSON example with test.* keys
- [ ] README.md links to USER-GUIDE.md#test-architecture for details
- [ ] docs/CLI.md has gsd test-count section matching existing command doc format
- [ ] docs/CLI.md help overview includes test-count
- [ ] Existing content unchanged in both files
</verification>

<success_criteria>
- README.md includes a test configuration section showing how to enable and configure the dual-layer test system
- README.md command tables include audit-tests and test-count
- docs/CLI.md documents the test-count command with usage, flags, and examples
</success_criteria>

<output>
After completion, create `.planning/phases/34-documentation/34-03-SUMMARY.md`
</output>
