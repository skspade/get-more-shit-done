---
phase: 34-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [get-shit-done/workflows/help.md]
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - help.md contains a "### Test Suite Management" subsection with test-count and audit-tests command references
    - help.md contains test.* configuration keys with defaults and descriptions
  artifacts:
    - get-shit-done/workflows/help.md (modified)
  key_links:
    - test-count command documented with usage pattern
    - audit-tests command documented with description and usage
    - test.hard_gate, test.acceptance_tests, test.budget.per_phase, test.budget.project, test.steward, test.command, test.framework keys documented
---

<objective>
Update help.md with test-related commands and configuration references.

Purpose: Users running `/gsd:help` can discover test suite management commands and configuration keys.
Output: Modified `get-shit-done/workflows/help.md` with test commands and config documentation.
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@get-shit-done/workflows/help.md
@commands/gsd/audit-tests.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test commands to help.md</name>
  <files>get-shit-done/workflows/help.md</files>
  <action>
Add two new sections to help.md:

1. **Add "/gsd:audit-tests" to the "### Utility Commands" section**, inserting it before `/gsd:cleanup` (alphabetical order within utilities). Use this content:

```markdown
**`/gsd:audit-tests`**
Run an on-demand test suite health check.

- Spawns the gsd-test-steward agent to analyze redundancy, staleness, and budget status
- Produces a test health report without requiring a full milestone audit
- Requires `test.steward: true` in config (enabled by default)

Usage: `/gsd:audit-tests`
```

2. **Add a "### Test Configuration" subsection** inside the existing "## Planning Configuration" section, after the `planning.search_gitignored` documentation block. Use this content:

```markdown
### Test Configuration

Configure the dual-layer test system in `.planning/config.json` under the `test` key:

| Setting | Options | Default | What it Controls |
|---------|---------|---------|------------------|
| `test.hard_gate` | `true`, `false` | `true` | Run test suite after each task commit during execution |
| `test.acceptance_tests` | `true`, `false` | `true` | Gather acceptance tests during discuss-phase |
| `test.budget.per_phase` | integer | `50` | Per-phase test count budget (warnings at 80%) |
| `test.budget.project` | integer | `800` | Project-wide test count budget (warnings at 80%) |
| `test.steward` | `true`, `false` | `true` | Enable test steward during audit-milestone |
| `test.command` | string | auto-detected | Override test runner command |
| `test.framework` | string | auto-detected | Override framework detection (jest, vitest, mocha, node:test) |

All test settings use zero-config defaults. Omitting the entire `test` section is equivalent to enabling all features with default values.

Example config:
\`\`\`json
{
  "test": {
    "hard_gate": true,
    "budget": {
      "per_phase": 50,
      "project": 800
    }
  }
}
\`\`\`
```

Preserve all existing content. Match the existing formatting style (bold command names, bullet points for features, Usage lines).
  </action>
  <verify>
    <automated>grep -c "audit-tests" get-shit-done/workflows/help.md && grep -c "test.hard_gate" get-shit-done/workflows/help.md && grep -c "test.budget" get-shit-done/workflows/help.md</automated>
  </verify>
  <done>help.md contains /gsd:audit-tests command reference in Utility Commands, and a Test Configuration subsection with all 7 test.* config keys documented with defaults and descriptions</done>
</task>

</tasks>

<verification>
- [ ] `/gsd:audit-tests` appears in Utility Commands section
- [ ] Test Configuration subsection exists with all 7 config keys
- [ ] Each config key has Options, Default, and What it Controls columns filled
- [ ] Existing help.md content is unchanged
- [ ] Formatting matches existing style (bold names, tables, code blocks)
</verification>

<success_criteria>
- help.md includes test-count command reference, audit-tests command reference, and test.* configuration keys
- All content matches existing documentation style
</success_criteria>

<output>
After completion, create `.planning/phases/34-documentation/34-01-SUMMARY.md`
</output>
