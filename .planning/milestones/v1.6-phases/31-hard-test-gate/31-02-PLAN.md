---
phase: 31-hard-test-gate
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - get-shit-done/workflows/execute-plan.md
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04, GATE-05]

must_haves:
  truths:
    - execute-plan.md contains a <test_gate> section that instructs the executor to run the test suite after each task commit
    - The gate instructions tell the executor to capture a baseline before the first task of each plan
    - The gate instructions tell the executor to skip the gate on TDD RED commits
    - The gate instructions tell the executor to follow deviation Rule 1 when new failures are detected
    - The gate instructions reference gsd-tools.cjs test-run as the execution mechanism
    - The gate silently skips when hard_gate is disabled or no test command is available
  artifacts:
    - get-shit-done/workflows/execute-plan.md (updated with <test_gate> section)
  key_links:
    - <test_gate> references the test-run command from Plan 01
    - <test_gate> references deviation Rule 1 from existing <deviation_rules>
    - <test_gate> is positioned after <task_commit> in the workflow
---

<objective>
Add the `<test_gate>` workflow section to execute-plan.md that instructs executors to run the test suite after each task commit.

Purpose: This is the workflow-level integration that makes the hard gate operational. Without this section, executors have no instruction to run tests after commits. The gate calls `gsd-tools.cjs test-run` (from Plan 01) and uses the structured result to decide whether to proceed or trigger deviation Rule 1.

Output: Updated execute-plan.md with `<test_gate>` section between `<task_commit>` and the next task.
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/31-hard-test-gate/31-CONTEXT.md
@.planning/phases/31-hard-test-gate/31-RESEARCH.md
@.planning/phases/31-hard-test-gate/31-01-PLAN.md
@get-shit-done/workflows/execute-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add baseline capture instruction to execute-plan.md</name>
  <files>get-shit-done/workflows/execute-plan.md</files>
  <action>
Add a `<test_gate_baseline>` section to execute-plan.md BEFORE the `<step name="execute">` section. This section instructs the executor to capture a test baseline before executing any tasks in the plan:

```xml
<test_gate_baseline>
## Test Gate Baseline

Before executing the first task, capture the test baseline for regression comparison:

```bash
BASELINE_JSON=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" test-run --baseline 2>/dev/null)
```

Parse the JSON result:
- If `status` is `"skip"`: no test command or hard_gate disabled. Set `TEST_GATE_ACTIVE=false`. All subsequent test gate checks are silently skipped.
- If `status` is `"pass"` or `"fail"`: Set `TEST_GATE_ACTIVE=true`. Store the `baseline` object from the response for use in post-commit checks.
- If `status` is `"error"`: Set `TEST_GATE_ACTIVE=false` with warning. Gate disabled for this plan.

The baseline captures pre-existing test failures so the gate only blocks on NEW failures introduced by plan tasks.

</test_gate_baseline>
```

Position this section after `<previous_phase_check>` and before `<step name="execute">`.
  </action>
  <verify>
    <automated>grep -c "test_gate_baseline" get-shit-done/workflows/execute-plan.md</automated>
  </verify>
  <done>execute-plan.md contains a test_gate_baseline section that instructs the executor to capture baseline before first task</done>
</task>

<task type="auto">
  <name>Task 2: Add post-commit test gate section to execute-plan.md</name>
  <files>get-shit-done/workflows/execute-plan.md</files>
  <action>
Add a `<test_gate>` section to execute-plan.md AFTER the `<task_commit>` section (which ends at approximately line 262). This section instructs the executor to run the test gate after every task commit:

```xml
<test_gate>
## Post-Commit Test Gate

After each task commit (step 5 of task_commit), if `TEST_GATE_ACTIVE` is true, run the test gate:

**1. Check for TDD RED commit:**
```bash
COMMIT_MSG=$(git log -1 --format="%s")
```

**2. Run test gate:**
```bash
GATE_RESULT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" test-run --commit-msg "$COMMIT_MSG" --baseline-data '$BASELINE_JSON' 2>/dev/null)
```

**3. Evaluate result:**

Parse the JSON `GATE_RESULT`:

| `status` | Action |
|-----------|--------|
| `"pass"` | Gate passed. Log `Test gate: PASSED ({passed}/{total})`. Proceed to next task. |
| `"skip"` | Gate skipped (TDD RED or no tests). Log reason from `summary`. Proceed to next task. |
| `"fail"` | NEW test failures detected. See failure protocol below. |
| `"error"` | Gate error (timeout, crash). Log warning. Proceed to next task (gate is advisory on errors). |

**4. Failure protocol (status = "fail"):**

When `new_failures` is non-empty:

a. Log: `Test gate: FAILED -- {new_failures.length} new failure(s) detected`
b. Display the `summary` field (contains pass/fail counts and failure names)
c. Follow deviation Rule 1 (Bug): debug the failures, fix the code, run `git add` + `git commit` for the fix
d. After fix commit, re-run the test gate (step 2) to verify the fix resolved the new failures
e. If fix does not resolve after 3 attempts: escalate to human via checkpoint

**Important:** The gate only blocks on NEW failures (tests in `new_failures` array). Pre-existing failures from the baseline are expected and do not block.

**5. Context budget:** The gate result `summary` is a short string (1-3 lines). Never request or display raw test output. The `raw_length` field tells you how much output was suppressed.

</test_gate>
```

The section must be placed immediately after the closing `</task_commit>` tag and before `<step name="checkpoint_protocol">`.
  </action>
  <verify>
    <automated>grep -c "test_gate" get-shit-done/workflows/execute-plan.md</automated>
  </verify>
  <done>execute-plan.md contains a test_gate section after task_commit that instructs executor to run post-commit test gate with baseline comparison, TDD detection, and Rule 1 failure handling</done>
</task>

</tasks>

<verification>
1. `grep "test_gate_baseline\|test_gate" get-shit-done/workflows/execute-plan.md` returns matches for both sections
2. The `<test_gate_baseline>` section appears before `<step name="execute">`
3. The `<test_gate>` section appears after `</task_commit>` and before `<step name="checkpoint_protocol">`
4. The test gate references `gsd-tools.cjs test-run` with `--commit-msg` and `--baseline-data` flags
5. The test gate references deviation Rule 1 for failure handling
6. The test gate mentions the 3-attempt retry limit before escalation
</verification>

<success_criteria>
- execute-plan.md has `<test_gate_baseline>` section for baseline capture before first task
- execute-plan.md has `<test_gate>` section for post-commit test verification
- Gate instructions reference `gsd-tools.cjs test-run` as execution mechanism
- Gate instructions handle all four statuses: pass, fail, skip, error
- Failure handling follows deviation Rule 1 with 3-attempt retry limit
- TDD RED detection via commit message is documented
- Gate silently skips when disabled or no test command
- No raw test output is ever displayed to executor
</success_criteria>

<output>
After completion, create `.planning/phases/31-hard-test-gate/31-02-SUMMARY.md`
</output>
