---
phase: 58-close-verification-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/54-core-streaming-function/54-VERIFICATION.md, .planning/phases/56-debug-retry-integration/56-VERIFICATION.md, .planning/REQUIREMENTS.md]
autonomous: true
requirements: [STREAM-01, STREAM-02, STREAM-03, STREAM-04, STREAM-05, STREAM-06, STALL-01, STALL-02, STALL-03, STALL-04, CLI-01, CLI-03, CLI-05]
gap_closure: true

must_haves:
  truths:
    - "Phase 54 has a VERIFICATION.md that covers all 12 requirements (STREAM-01 through STREAM-06, STALL-01 through STALL-04, CLI-01, CLI-05) with codebase evidence from actual current line numbers"
    - "Phase 56 has a VERIFICATION.md that covers CLI-03 with codebase evidence from actual current line numbers"
    - "All 13 orphaned requirements in REQUIREMENTS.md have their checkboxes changed from [ ] to [x]"
    - "The traceability table in REQUIREMENTS.md shows Verified status for all 13 requirements"
  artifacts:
    - path: ".planning/phases/54-core-streaming-function/54-VERIFICATION.md"
      provides: "Formal verification for 12 Phase 54 requirements"
      contains: "status: passed"
    - path: ".planning/phases/56-debug-retry-integration/56-VERIFICATION.md"
      provides: "Formal verification for CLI-03"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes and traceability table"
      contains: "[x] **STREAM-01**"
  key_links:
    - from: "54-VERIFICATION.md"
      to: "get-shit-done/scripts/autopilot.mjs"
      via: "codebase evidence line numbers"
      pattern: "autopilot\\.mjs"
    - from: "56-VERIFICATION.md"
      to: "get-shit-done/scripts/autopilot.mjs"
      via: "codebase evidence for debug retry sites"
      pattern: "runClaudeStreaming.*debugPrompt"
---

<objective>
Create VERIFICATION.md for phases 54 and 56 by inspecting the actual codebase for evidence, and update REQUIREMENTS.md to mark all 13 orphaned requirements as verified.

Purpose: Close the verification gap identified by the v2.4 milestone audit -- all 13 requirements were implemented and tested but never formally verified because phases 54 and 56 lack VERIFICATION.md documents.
Output: Two VERIFICATION.md files and updated REQUIREMENTS.md with all requirements checked off.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.4-MILESTONE-AUDIT.md
@.planning/phases/54-core-streaming-function/54-01-PLAN.md
@.planning/phases/54-core-streaming-function/54-02-PLAN.md
@.planning/phases/54-core-streaming-function/54-01-SUMMARY.md
@.planning/phases/54-core-streaming-function/54-02-SUMMARY.md
@.planning/phases/56-debug-retry-integration/56-01-PLAN.md
@.planning/phases/56-debug-retry-integration/56-01-SUMMARY.md
@.planning/phases/55-step-function-integration/55-VERIFICATION.md
@.planning/phases/57-config-schema-and-verification/57-VERIFICATION.md
@get-shit-done/scripts/autopilot.mjs
@tests/autopilot.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 54-VERIFICATION.md with codebase evidence for 12 requirements</name>
  <files>.planning/phases/54-core-streaming-function/54-VERIFICATION.md</files>
  <action>
Create `.planning/phases/54-core-streaming-function/54-VERIFICATION.md` following the established format from 55-VERIFICATION.md and 57-VERIFICATION.md.

**CRITICAL: Do NOT copy line numbers from plan documents or this research section. You MUST inspect the actual current source files to find accurate line numbers.** Phases 55 and 56 modified autopilot.mjs after Phase 54, so line numbers have shifted.

Steps:
1. Read `get-shit-done/scripts/autopilot.mjs` to find current line numbers for each piece of evidence
2. Read `tests/autopilot.test.cjs` to find test evidence
3. Create the VERIFICATION.md with the following structure:

```markdown
---
status: passed
phase: 54-core-streaming-function
verified: 2026-03-12
---

# Phase 54: Core Streaming Function - Verification

## Phase Goal
Users observe real-time streaming output from Claude CLI invocations instead of waiting for buffered JSON

## Must-Haves Verification

### Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | runClaudeStreaming() spawns claude CLI with --output-format stream-json and reads NDJSON lines in real-time | PASSED | {cite actual line numbers from autopilot.mjs} |
| 2 | displayStreamEvent() writes assistant text to stdout and tool call indicators to stderr | PASSED | {cite actual line numbers} |
| 3 | Stall timer fires a warning to stderr after 5 minutes of no output and re-arms for repeated warnings | PASSED | {cite actual line numbers} |
| 4 | When quiet flag is set, runClaudeStreaming() uses --output-format json and returns buffered output | PASSED | {cite actual line numbers} |
| 5 | The < /dev/null stdin redirect is present on both streaming and quiet code paths | PASSED | {cite actual line numbers} |

### Artifacts

| Artifact | Status | Evidence |
|----------|--------|----------|
| get-shit-done/scripts/autopilot.mjs | PASSED | Contains `function runClaudeStreaming` and `function displayStreamEvent` |
| tests/autopilot.test.cjs | PASSED | Contains 10 streaming function static analysis tests |

### Key Links

| From | To | Via | Status |
|------|----|-----|--------|
| runClaudeStreaming | displayStreamEvent | called for each parsed NDJSON event | PASSED |
| runClaudeStreaming | getConfig | reads stall timeout | PASSED |

## Requirement Coverage

| Req ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| STREAM-01 | Empirical stream-json format discovery | PASSED | {54-CONTEXT.md documents discovery; stream-json flag in autopilot.mjs} |
| STREAM-02 | runClaudeStreaming reads NDJSON via async iterator | PASSED | {cite createInterface + for await line numbers} |
| STREAM-03 | displayStreamEvent writes text to stdout, tools to stderr | PASSED | {cite process.stdout.write and process.stderr.write line numbers} |
| STREAM-04 | NDJSON lines accumulated for result.stdout compatibility | PASSED | {cite lines array push and join line numbers} |
| STREAM-05 | Output file receives lines in real-time | PASSED | {cite fs.appendFileSync line number} |
| STREAM-06 | Uses --output-format stream-json | PASSED | {cite the stream-json flag line number} |
| STALL-01 | Configurable stall timer resets on every event | PASSED | {cite armStallTimer() call inside for await} |
| STALL-02 | Warning on stderr + log at timeout | PASSED | {cite process.stderr.write warning and logMsg line numbers} |
| STALL-03 | Warning re-arms at each interval | PASSED | {cite setTimeout recursive re-arm line number} |
| STALL-04 | Timer cleanup on all exit paths | PASSED | {cite finally block with clearTimeout} |
| CLI-01 | --quiet flag for buffered JSON fallback | PASSED | {cite knownFlags 'quiet', QUIET const, quiet code path} |
| CLI-05 | stdin redirect preserved | PASSED | {cite < /dev/null in both code paths} |

## Success Criteria

| Criterion | Status |
|-----------|--------|
| Running autopilot with streaming-capable CLI displays assistant text to stdout in real-time | PASSED |
| Tool call events appear as compact indicators on stderr | PASSED |
| When no stream events arrive for 5 minutes, a warning appears on stderr and re-arms | PASSED |
| Running autopilot with --quiet produces buffered JSON output with no streaming | PASSED |
| < /dev/null stdin redirect preserved in consolidated function | PASSED |

## Test Results

All {N} tests in autopilot.test.cjs pass:
{list test groups with counts}

## Score

**5/5 must-haves verified. All 12 requirements covered. All tests pass.**

---
*Verified: 2026-03-12*
```

Replace all `{...}` placeholders with actual evidence gathered from reading the source files. Every line number MUST match the current file content.
  </action>
  <verify>
    <automated>grep -c 'PASSED' .planning/phases/54-core-streaming-function/54-VERIFICATION.md</automated>
  </verify>
  <done>
    - 54-VERIFICATION.md exists with YAML frontmatter (status: passed)
    - All 12 requirements have evidence from actual current codebase line numbers
    - Must-haves truths, artifacts, and key links are all marked PASSED
    - Success criteria section matches ROADMAP success criteria
    - Test results section references current test counts
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 56-VERIFICATION.md and update REQUIREMENTS.md for all 13 requirements</name>
  <files>.planning/phases/56-debug-retry-integration/56-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>
**Part A: Create 56-VERIFICATION.md**

Create `.planning/phases/56-debug-retry-integration/56-VERIFICATION.md` following the same format.

**CRITICAL: Inspect actual current autopilot.mjs to find the 3 debug retry `runClaudeStreaming(debugPrompt)` call sites.** Do NOT use plan line numbers.

```markdown
---
status: passed
phase: 56-debug-retry-integration
verified: 2026-03-12
---

# Phase 56: Debug Retry Integration - Verification

## Phase Goal
Debug retry cycles show live streaming output so users can watch the debugger work in real-time

## Must-Haves Verification

### Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 3 debug retry claude -p invocations use runClaudeStreaming() instead of direct $ template literals | PASSED | {cite 3 line numbers with runClaudeStreaming(debugPrompt)} |
| 2 | No process.stdout.write(debugResult.stdout) calls remain for debug retry invocations | PASSED | {grep confirms 0 debugResult references} |
| 3 | Exit code checking in runStepWithRetry() continues to work via destructured exitCode | PASSED | {cite destructured exitCode line number} |
| 4 | Static analysis test passes with updated invocation count | PASSED | {cite test line with count of 2} |

### Artifacts

| Artifact | Status | Evidence |
|----------|--------|----------|
| get-shit-done/scripts/autopilot.mjs | PASSED | 3 debug retry sites call runClaudeStreaming(debugPrompt) |
| tests/autopilot.test.cjs | PASSED | Invocation count test expects 2 direct shell invocations |

### Key Links

| From | To | Via | Status |
|------|----|-----|--------|
| runClaudeStreaming | 3 debug retry sites | called at all 3 sites | PASSED |
| Static analysis test | actual invocation count | shellInvocationLines.length === 2 | PASSED |

## Requirement Coverage

| Req ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| CLI-03 | All 3 debug retry claude -p invocations route through runClaudeStreaming | PASSED | {cite 3 line numbers} |

## Success Criteria

| Criterion | Status |
|-----------|--------|
| All 3 debug retry claude -p invocations route through runClaudeStreaming() | PASSED |
| During debug retry, assistant text and tool indicators stream to terminal in real-time | PASSED |

## Test Results

All tests in autopilot.test.cjs pass with invocation count of 2.

## Score

**4/4 must-haves verified. CLI-03 covered. All tests pass.**

---
*Verified: 2026-03-12*
```

Replace all `{...}` placeholders with actual evidence from reading the source.

**Part B: Update REQUIREMENTS.md**

Read `.planning/REQUIREMENTS.md` and make the following changes:

1. **Update checkboxes** for all 13 orphaned requirements -- change `- [ ]` to `- [x]` for:
   - STREAM-01, STREAM-02, STREAM-03, STREAM-04, STREAM-05, STREAM-06
   - STALL-01, STALL-02, STALL-03, STALL-04
   - CLI-01, CLI-03, CLI-05

2. **Update traceability table** -- for each of those 13 requirements, change the Status column from `Pending` to `Verified`. Also update the Phase column to reflect the actual verification phase (Phase 58 for those verified here, or keep the existing phase assignment).

Note: CLI-02 and CLI-04 may already be satisfied per the audit. Check their current status and update if they are also still showing as Pending (the audit shows them as "satisfied" via phases 55 and 57 VERIFICATION.md files, but their checkboxes may still be unchecked).
  </action>
  <verify>
    <automated>grep -c '\[x\]' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - 56-VERIFICATION.md exists with YAML frontmatter (status: passed)
    - CLI-03 has evidence from actual current codebase line numbers
    - REQUIREMENTS.md has all 13 orphaned requirements checked off with [x]
    - Traceability table shows Verified status for all 13 requirements
    - All 15 requirements in REQUIREMENTS.md are now [x] (including CLI-02 and CLI-04 which were already verified)
  </done>
</task>

</tasks>

<verification>
1. `ls .planning/phases/54-core-streaming-function/54-VERIFICATION.md` -- file exists
2. `ls .planning/phases/56-debug-retry-integration/56-VERIFICATION.md` -- file exists
3. `grep -c 'status: passed' .planning/phases/54-core-streaming-function/54-VERIFICATION.md` returns 1
4. `grep -c 'status: passed' .planning/phases/56-debug-retry-integration/56-VERIFICATION.md` returns 1
5. `grep -c '\[x\]' .planning/REQUIREMENTS.md` returns 15 (all requirements checked)
6. `grep -c 'Pending' .planning/REQUIREMENTS.md` returns 0 (no pending requirements in traceability table)
7. `grep -c 'Verified' .planning/REQUIREMENTS.md` returns 15 (all requirements show Verified in traceability)
</verification>

<success_criteria>
- Phase 54 VERIFICATION.md exists with all 12 requirements verified against actual codebase evidence
- Phase 56 VERIFICATION.md exists with CLI-03 verified against actual codebase evidence
- All 13 orphaned requirements are checked off in REQUIREMENTS.md (both checkbox and traceability table)
- No fabricated or stale line numbers -- all evidence reflects current file content
</success_criteria>

<output>
After completion, create `.planning/phases/58-close-verification-gaps/58-01-SUMMARY.md`
</output>
