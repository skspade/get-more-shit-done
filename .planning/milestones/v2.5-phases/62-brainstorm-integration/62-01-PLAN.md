---
phase: 62-brainstorm-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [get-shit-done/workflows/brainstorm.md]
autonomous: true
requirements: [INT-01, INT-02]

must_haves:
  truths:
    - brainstorm.md step 10 milestone route invokes SlashCommand("/gsd:new-milestone --auto") instead of inline steps
    - MILESTONE-CONTEXT.md is written and committed before the SlashCommand handoff
    - Steps 10b (init new-milestone) and 10c (inline execution) are removed
    - Step 10a (build MILESTONE-CONTEXT.md) is retained unchanged
    - New-project route (lines 309-318) is unchanged
  artifacts:
    - get-shit-done/workflows/brainstorm.md (modified step 10)
  key_links:
    - brainstorm.md step 10 milestone route -> SlashCommand("/gsd:new-milestone --auto")
    - MILESTONE-CONTEXT.md write -> git commit -> SlashCommand handoff
---

<objective>
Replace brainstorm.md step 10 inline milestone creation (steps 10b-10c) with a SlashCommand delegation to `/gsd:new-milestone --auto`.

Purpose: Eliminate ~20 lines of duplicated milestone creation logic. The new-milestone workflow now handles everything autonomously (Phases 59-61), so brainstorm just needs to write MILESTONE-CONTEXT.md, commit it, and hand off.
Output: Modified brainstorm.md with simplified step 10 milestone route.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@get-shit-done/workflows/brainstorm.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline milestone creation with SlashCommand delegation</name>
  <files>get-shit-done/workflows/brainstorm.md</files>
  <action>
In `get-shit-done/workflows/brainstorm.md`, modify step 10 "Route into Creation Flow" — specifically the "Milestone Route (PROJECT.md exists)" section.

**RETAIN step 10a unchanged** (lines 263-283) — the MILESTONE-CONTEXT.md build block stays exactly as-is.

**REPLACE steps 10b and 10c** (lines 285-307) with:

```markdown
**10b. Commit and hand off to new-milestone:**

Commit MILESTONE-CONTEXT.md so it persists for the new-milestone workflow:

\`\`\`bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs: write MILESTONE-CONTEXT.md from brainstorm" --files ".planning/MILESTONE-CONTEXT.md"
\`\`\`

Display:
\`\`\`
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► ROUTING TO NEW-MILESTONE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Milestone context committed. Handing off to new-milestone...
\`\`\`

Exit skill and invoke SlashCommand("/gsd:new-milestone --auto")
```

**VERIFY the new-project route** (lines 309-318) is NOT modified — it should remain exactly as-is after the milestone route changes.

The net result: step 10a writes MILESTONE-CONTEXT.md, step 10b commits it and delegates via SlashCommand. Steps 10b (init) and 10c (inline execution) from the old version are gone.
  </action>
  <verify>
Read brainstorm.md and confirm:
1. Step 10a (Build MILESTONE-CONTEXT.md) content is unchanged
2. Old step 10b (gsd-tools.cjs init new-milestone) is gone
3. Old step 10c (Execute new-milestone workflow steps 1-11 inline) is gone
4. New step 10b contains git commit + SlashCommand("/gsd:new-milestone --auto")
5. New-project route section is unchanged
6. No orphaned references to MINIT variable or inline milestone steps
  </verify>
  <done>
brainstorm.md step 10 milestone route uses SlashCommand delegation instead of inline execution. MILESTONE-CONTEXT.md is committed before handoff. Old inline code (~20 lines) is removed.
  </done>
</task>

</tasks>

<verification>
1. Read brainstorm.md step 10 — milestone route should be: write MILESTONE-CONTEXT.md -> commit -> SlashCommand
2. Grep for "init new-milestone" in brainstorm.md — should return zero matches
3. Grep for "Execute new-milestone workflow steps" in brainstorm.md — should return zero matches
4. Grep for "SlashCommand.*new-milestone" in brainstorm.md — should return one match
5. Grep for "MILESTONE-CONTEXT.md" in brainstorm.md — should still appear (in step 10a and 10b)
6. Confirm new-project route is intact (search for "New-Project Route")
</verification>

<success_criteria>
- brainstorm.md step 10 milestone route invokes `/gsd:new-milestone --auto` via SlashCommand
- MILESTONE-CONTEXT.md is written (step 10a) and committed (step 10b) before handoff
- Previous inline milestone creation code (steps 10b-10c) is removed
- New-project route is unchanged
- No broken references or orphaned variables
</success_criteria>

<output>
After completion, create `.planning/phases/62-brainstorm-integration/62-01-SUMMARY.md`
</output>
