---
phase: 59-flag-parsing-and-context-resolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/new-milestone.md
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03, CTX-01, CTX-02, CTX-03, CTX-04, CTX-05]

must_haves:
  truths:
    - "new-milestone.md parses --auto flag from $ARGUMENTS"
    - "new-milestone.md reads workflow.auto_advance config as fallback"
    - "new-milestone.md persists workflow.auto_advance when --auto flag used"
    - "Context resolution follows priority: MILESTONE-CONTEXT.md > @file > inline text > error"
    - "Context validation occurs before any PROJECT.md or STATE.md mutations"
    - "Auto mode with no context errors with usage message"
  artifacts:
    - path: "get-shit-done/workflows/new-milestone.md"
      provides: "Auto mode detection and context resolution blocks"
      contains: "--auto"
    - path: "get-shit-done/workflows/new-milestone.md"
      provides: "Context priority resolution logic"
      contains: "MILESTONE-CONTEXT.md"
    - path: "get-shit-done/workflows/new-milestone.md"
      provides: "Early error on missing context"
      contains: "requires milestone goals"
  key_links:
    - from: "get-shit-done/workflows/new-milestone.md"
      to: "get-shit-done/workflows/discuss-phase.md"
      via: "Same --auto flag + config-get pattern"
      pattern: "workflow.auto_advance"
    - from: "get-shit-done/workflows/new-milestone.md"
      to: "get-shit-done/workflows/new-project.md"
      via: "Same @file and auto mode error pattern"
      pattern: "@file"
---

<objective>
Add `--auto` flag parsing and context resolution to `new-milestone.md` workflow.

Purpose: Enable `/gsd:new-milestone --auto` to detect context from MILESTONE-CONTEXT.md, @file references, or inline text — and error with a usage message when no context is available. This is the workflow-level counterpart to Plan 01's init plumbing. No decision points are skipped yet (Phase 60 scope).

Output: Updated new-milestone.md with auto_mode and context_resolution sections
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-flag-parsing-and-context-resolution/59-CONTEXT.md
@.planning/phases/59-flag-parsing-and-context-resolution/59-RESEARCH.md

<interfaces>
<!-- Key existing patterns the executor needs -->

From get-shit-done/workflows/discuss-phase.md (--auto flag pattern, line 133):
```markdown
**If `--auto` flag present in $ARGUMENTS:**
```
And the auto-advance step (line 761-775):
```markdown
1. Parse `--auto` flag from $ARGUMENTS
2. Read `workflow.auto_advance` from config:
   AUTO_CFG=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-get workflow.auto_advance 2>/dev/null || echo "false")

If `--auto` flag present AND `AUTO_CFG` is not true: Persist auto-advance to config:
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-set workflow.auto_advance true
```

From get-shit-done/workflows/new-project.md (auto_mode block, line 9-40):
```markdown
<auto_mode>
## Auto Mode Detection

Check if `--auto` flag is present in $ARGUMENTS.

**Document requirement:**
Auto mode requires an idea document — either:
- File reference: `/gsd:new-project --auto @prd.md`
- Pasted/written text in the prompt

If no document content provided, error:
Error: --auto requires an idea document.
</auto_mode>
```

From get-shit-done/workflows/new-milestone.md current structure:
Step 1: Load Context (read PROJECT.md, MILESTONES.md, STATE.md, check MILESTONE-CONTEXT.md)
Step 2: Gather Milestone Goals (interactive or from MILESTONE-CONTEXT.md)
Step 3: Determine Milestone Version
Step 4: Update PROJECT.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto_mode and context_resolution blocks to new-milestone.md</name>
  <files>get-shit-done/workflows/new-milestone.md</files>
  <action>
Add two new blocks to `new-milestone.md`. Insert them between the `</required_reading>` tag and the `<process>` tag (before line 13).

**Block 1: Auto Mode Detection** — Insert after `</required_reading>` and before `<process>`:

```markdown
<auto_mode>
## Auto Mode Detection

Check if `--auto` flag is present in $ARGUMENTS.

**If `--auto` flag present:**

1. Read `workflow.auto_advance` from config:
```bash
AUTO_CFG=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-get workflow.auto_advance 2>/dev/null || echo "false")
```

2. If `AUTO_CFG` is not true, persist auto-advance to config:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-set workflow.auto_advance true
```

3. Strip `--auto` from arguments. Remaining args are the context source (inline text or @file reference).

**Auto mode is active when:** `--auto` flag is present OR `workflow.auto_advance` config is true.

</auto_mode>
```

**Block 2: Context Resolution** — Insert after the `</auto_mode>` block and before `<process>`:

```markdown
<context_resolution>
## Context Resolution (auto mode)

**Skip if:** Auto mode is NOT active. Continue to `<process>` step 1.

**If auto mode is active:** Resolve context BEFORE any mutations (before step 4).

Resolution priority order:

1. **MILESTONE-CONTEXT.md** — If `.planning/MILESTONE-CONTEXT.md` exists, use it as context.
2. **@file reference** — If remaining args (after stripping `--auto`) start with `@`, read the referenced file as milestone goals.
3. **Inline text** — If remaining args contain text (after stripping `--auto`), use as milestone goals.
4. **Error** — If no context source found:

```
Error: --auto requires milestone goals.

Usage:
  /gsd:new-milestone --auto "description of what to build"
  /gsd:new-milestone --auto @path/to/context.md
  Or provide .planning/MILESTONE-CONTEXT.md

The context should describe what you want to build in this milestone.
```

Exit workflow. No file mutations occur.

**Effect:** Resolved context is used in step 2 (Gather Milestone Goals) instead of interactive questioning. Steps 3-11 proceed normally — decision points are NOT skipped in this phase (see Phase 60).

</context_resolution>
```

Then update step 2 (Gather Milestone Goals) to reference the resolved context. Find the existing step 2 text and add a conditional at the top:

Find:
```markdown
## 2. Gather Milestone Goals

**If MILESTONE-CONTEXT.md exists:**
```

Replace with:
```markdown
## 2. Gather Milestone Goals

**If auto mode with resolved context:**
- Use resolved context from `<context_resolution>` as milestone goals
- Skip interactive questioning
- Continue to step 3

**If MILESTONE-CONTEXT.md exists:**
```
  </action>
  <verify>
    <automated>grep -c "auto_mode" get-shit-done/workflows/new-milestone.md && grep -c "context_resolution" get-shit-done/workflows/new-milestone.md && grep -c "requires milestone goals" get-shit-done/workflows/new-milestone.md</automated>
  </verify>
  <done>new-milestone.md contains auto_mode block with --auto flag parsing and config persistence, context_resolution block with priority order and error message, and step 2 references resolved context</done>
</task>

</tasks>

<verification>
1. `grep -c "auto_mode" get-shit-done/workflows/new-milestone.md` returns at least 2 (block + reference)
2. `grep -c "context_resolution" get-shit-done/workflows/new-milestone.md` returns at least 2 (block + reference)
3. `grep "MILESTONE-CONTEXT.md" get-shit-done/workflows/new-milestone.md` shows both existing reference and new priority reference
4. `grep "requires milestone goals" get-shit-done/workflows/new-milestone.md` shows the error message
5. `grep "workflow.auto_advance" get-shit-done/workflows/new-milestone.md` shows config read and persist commands
6. The error block appears BEFORE step 4 (Update PROJECT.md) in the document, satisfying CTX-05
</verification>

<success_criteria>
- `--auto` flag parsing follows the established pattern from discuss-phase.md
- `workflow.auto_advance` config is read and persisted following the hybrid pattern
- Context resolution follows the priority order: MILESTONE-CONTEXT.md > @file > inline > error
- Error message includes all valid usage forms
- Error exits before any PROJECT.md/STATE.md mutations (CTX-05)
- Step 2 conditionally uses resolved context in auto mode
- All existing workflow steps remain unchanged for interactive mode
</success_criteria>

<output>
After completion, create `.planning/phases/59-flag-parsing-and-context-resolution/59-02-SUMMARY.md`
</output>
