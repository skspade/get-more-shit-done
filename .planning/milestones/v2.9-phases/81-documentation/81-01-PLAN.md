---
phase: 81-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".claude/get-shit-done/workflows/help.md", "docs/USER-GUIDE.md", "README.md"]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - help.md contains /gsd:test-review command reference with --report-only flag and usage examples
    - USER-GUIDE.md contains a Test Review section with pipeline diagram, flags table, and examples
    - README.md command table includes a test-review row
  artifacts:
    - .claude/get-shit-done/workflows/help.md (updated)
    - docs/USER-GUIDE.md (updated)
    - README.md (updated)
  key_links: []
---

<objective>
Add `/gsd:test-review` documentation to all three project documentation files.

Purpose: Users can discover and learn how to use the test-review command from project documentation.
Output: Updated help.md, USER-GUIDE.md, and README.md with test-review entries.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-documentation/81-CONTEXT.md
@.planning/phases/81-documentation/81-RESEARCH.md
@.claude/commands/gsd/test-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test-review entry to help.md</name>
  <files>.claude/get-shit-done/workflows/help.md</files>
  <action>
Add `/gsd:test-review` to help.md in TWO locations:

**1. Utility Commands section (after the `/gsd:audit-tests` entry around line 363):**

Add a new entry following the exact format pattern of existing entries (bold command with flags, bullet list, Usage lines, Result line):

```
### Test Review

**`/gsd:test-review [--report-only]`**

Analyze git diff for test coverage gaps, stale tests, and consolidation opportunities. Routes findings to quick task or milestone based on user choice.

- Gathers diff vs main and spawns gsd-test-reviewer agent for analysis
- Detects missing test files, coverage gaps, stale tests, and consolidation opportunities
- Large diffs (>2000 lines) automatically switch to summarized mode
- Routes findings via user choice: quick task, milestone, or done (no auto-scoring)
- Zero-recommendation case skips routing entirely

Usage: `/gsd:test-review` (full analysis with routing)
Usage: `/gsd:test-review --report-only` (analysis only, skip routing)

Result: Report at `.planning/reviews/YYYY-MM-DD-test-review.md`
```

**2. Quick reference section (after the "PR Review:" block around line 576):**

Add a new quick reference block:

```
**Test Review:**

```
/gsd:test-review                             # Analyze diff for test gaps, route findings
/gsd:test-review --report-only               # Report only, no routing
```
```
  </action>
  <verify>grep -c "gsd:test-review" .claude/get-shit-done/workflows/help.md returns at least 4 (entry + 2 usage + quick ref)</verify>
  <done>help.md contains /gsd:test-review command reference with --report-only flag, usage examples, and quick reference entry</done>
</task>

<task type="auto">
  <name>Task 2: Add test-review section to USER-GUIDE.md</name>
  <files>docs/USER-GUIDE.md</files>
  <action>
Add `/gsd:test-review` to USER-GUIDE.md in TWO locations:

**1. Command table (after the `/gsd:pr-review` row at line 366):**

Add a new row:

```
| `/gsd:test-review [--report-only]` | Analyze diff for test coverage gaps and route findings | After code changes, to check test health |
```

**2. New section (after the PR Review section which ends at line 682, before the `---` separator and Troubleshooting at line 686):**

Add a Test Review section following the exact same structure as the PR Review section (pipeline diagram, flags table, examples, closing note):

```markdown
### Test Review

Analyze your branch diff for test coverage gaps, stale tests, and consolidation opportunities. Unlike PR review (which auto-scores and routes), test review lets you choose how to handle findings.

#### Pipeline

```
  /gsd:test-review
         |
         +-- Gather diff vs main (summarized if >2000 lines)
         |
         +-- Spawn gsd-test-reviewer agent
         |     Map changed files to tests, detect gaps
         |
         +-- Write report to .planning/reviews/
         |
         +-- Route (user choice)
               |
               +-- Quick task --> fix findings now
               +-- Milestone --> comprehensive test improvement
               +-- Done --> keep report, no action
```

#### Flags

| Flag | Effect |
|------|--------|
| *(none)* | Full analysis with user-choice routing |
| `--report-only` | Write report and exit, skip routing |

#### Examples

```
# Run test review with routing options
/gsd:test-review

# Generate report only, no routing
/gsd:test-review --report-only
```

After completion, GSD displays a banner with route taken (if any), report path, and artifacts created. The permanent report at `.planning/reviews/YYYY-MM-DD-test-review.md` serves as an audit trail.
```
  </action>
  <verify>grep -c "test-review" docs/USER-GUIDE.md returns at least 6 (table row + section heading + pipeline + examples)</verify>
  <done>USER-GUIDE.md contains test review command table entry and a full Test Review section with pipeline diagram, flags table, and examples</done>
</task>

<task type="auto">
  <name>Task 3: Add test-review row to README.md command table</name>
  <files>README.md</files>
  <action>
Add a new row to the Commands table in README.md, immediately after the `/gsd:pr-review` row (line 89):

```
| `/gsd:test-review` | Analyze test coverage gaps for current branch diff |
```

Follow the existing terse style: action-oriented description, no flags listed in the table.
  </action>
  <verify>grep "gsd:test-review" README.md returns the new table row</verify>
  <done>README.md command table includes test-review entry after pr-review</done>
</task>

</tasks>

<verification>
- [ ] help.md has /gsd:test-review entry in Utility Commands section
- [ ] help.md has /gsd:test-review quick reference block
- [ ] USER-GUIDE.md has test-review row in command table
- [ ] USER-GUIDE.md has Test Review section with pipeline, flags, examples
- [ ] README.md has test-review row in command table
- [ ] Only --report-only flag is documented (no --quick, --milestone, --ingest, --full)
- [ ] Routing described as user-choice, not auto-scoring
</verification>

<success_criteria>
- help.md contains `/gsd:test-review` command reference with flags and usage
- USER-GUIDE.md contains a test review usage guide with examples
- README.md command table includes test-review entry
</success_criteria>

<output>
After completion, create `.planning/phases/81-documentation/81-01-SUMMARY.md`
</output>
