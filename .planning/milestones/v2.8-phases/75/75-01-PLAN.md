---
phase: 75
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [get-shit-done/workflows/audit-milestone.md, get-shit-done/workflows/plan-milestone-gaps.md]
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03, ROUTE-01, ROUTE-02, ROUTE-03]

must_haves:
  truths:
    - "audit-milestone step 6 writes gaps.test_consolidation array to MILESTONE-AUDIT.md frontmatter when steward proposals exist"
    - "Each test_consolidation entry has strategy, source, action, and estimated_reduction fields"
    - "gaps.test_consolidation is nested under gaps alongside requirements, integration, flows"
    - "Consolidation proposals but no other gaps produces tech_debt status"
    - "Consolidation proposals with other gaps produces gaps_found status"
    - "No consolidation proposals produces identical behavior to pre-v2.8"
    - "plan-milestone-gaps parses gaps.test_consolidation from frontmatter"
  artifacts:
    - path: "get-shit-done/workflows/audit-milestone.md"
      provides: "Schema write side and status routing"
    - path: "get-shit-done/workflows/plan-milestone-gaps.md"
      provides: "Schema read side with guard clause"
  key_links:
    - from: "audit-milestone.md step 6"
      to: "MILESTONE-AUDIT.md frontmatter"
      via: "gaps.test_consolidation array in YAML template"
      pattern: "test_consolidation"
    - from: "plan-milestone-gaps.md step 1"
      to: "MILESTONE-AUDIT.md frontmatter"
      via: "gaps.test_consolidation field enumeration"
      pattern: "test_consolidation"
    - from: "audit-milestone.md step 5e/7"
      to: "status field"
      via: "consolidation-aware routing logic"
      pattern: "tech_debt"
---

<objective>
Add `gaps.test_consolidation` schema to MILESTONE-AUDIT.md frontmatter and implement consolidation-aware status routing, modifying both audit-milestone.md and plan-milestone-gaps.md atomically.

Purpose: Bridge steward consolidation proposals into the gap closure pipeline so plan-milestone-gaps can consume them, and route consolidation-only audits to tech_debt instead of passed.
Output: Updated audit-milestone.md and plan-milestone-gaps.md workflow files.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75/75-CONTEXT.md
@.planning/phases/75/75-RESEARCH.md

@get-shit-done/workflows/audit-milestone.md
@get-shit-done/workflows/plan-milestone-gaps.md
@.planning/milestones/v2.7-MILESTONE-AUDIT.md
@agents/gsd-test-steward.md

<interfaces>
<!-- Steward proposal output format (from gsd-test-steward.md step 6): -->
#### Proposal {N}: {strategy} -- {title}
- **Strategy:** {parameterize | promote | prune | merge}
- **Source:** `{file(s) and test names}`
- **Action:** {specific action to take}
- **Estimated reduction:** {N} test(s)

<!-- Current audit frontmatter gaps structure (from v2.7 audit): -->
gaps:
  requirements: []
  integration:
    - id: "INT-01"
      severity: "documentation"
      description: "..."
      affected_requirements: [...]
      evidence: "..."
  flows: []

<!-- Current audit frontmatter test_health (separate from gaps): -->
test_health:
  budget_status: "{OK | Warning | Over Budget}"
  redundant_tests: N
  stale_tests: N
  consolidation_proposals: N

<!-- Current status values in audit-milestone.md step 6: -->
status: passed | gaps_found | tech_debt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gaps.test_consolidation schema and status routing to audit-milestone.md</name>
  <files>get-shit-done/workflows/audit-milestone.md</files>
  <action>
Make two changes to audit-milestone.md:

**Change 1 — Step 6 frontmatter template: Add `gaps.test_consolidation` to the YAML template.**

In the step 6 YAML frontmatter template, add `test_consolidation` under the `gaps` object, after `flows`:

```yaml
gaps:
  requirements:
    - id: "{REQ-ID}"
      status: "unsatisfied | partial | orphaned"
      phase: "{assigned phase}"
      claimed_by_plans: ["{plan files that reference this requirement}"]
      completed_by_plans: ["{plan files whose SUMMARY marks it complete}"]
      verification_status: "passed | gaps_found | missing | orphaned"
      evidence: "{specific evidence or lack thereof}"
  integration: [...]
  flows: [...]
  test_consolidation:   # From steward proposals (when steward enabled and proposals exist)
    - strategy: "{parameterize | promote | prune | merge}"
      source: "{verbatim file path and location from steward report}"
      action: "{verbatim action description from steward report}"
      estimated_reduction: "{integer count of tests removed}"
```

Add instructions in the step 6 prose (after the existing steward/test_health section) explaining how to populate `gaps.test_consolidation`:

- When `STEWARD_ENABLED` is true and the steward report contains `#### Proposal N:` blocks, parse each proposal block to extract `**Strategy:**`, `**Source:**`, `**Action:**`, and `**Estimated reduction:**` labeled fields
- Build a `gaps.test_consolidation` array entry for each valid proposal with all four fields
- Skip malformed proposals (missing required fields) with a warning — do not propagate incomplete entries
- When steward is disabled or produces no proposals, omit `gaps.test_consolidation` from the frontmatter entirely (do not write an empty array)
- The existing `test_health.consolidation_proposals` integer count remains unchanged — it is informational metadata, not the structured data

**Change 2 — Add consolidation-aware status routing after step 5e.**

Add a new routing check. The exact placement: after step 5e (FAIL Gate and Orphan Detection) and before step 6 (Aggregate). Add text like:

```markdown
### 5f. Consolidation-Aware Status Routing

After determining requirements coverage:

- If all requirements are satisfied AND no integration/flow gaps exist AND `consolidation_proposals > 0` (from steward report): set status to `tech_debt`
- If consolidation proposals exist AND other gaps also exist: status remains `gaps_found` (other gaps take precedence)
- If no consolidation proposals (steward disabled or no findings): no change to status determination — behaves identically to pre-v2.8
```

Do NOT modify the existing step 5e logic. Do NOT change the `gaps_found` condition. Only add the new `tech_debt` routing for the consolidation-only case.
  </action>
  <verify>
    <automated>grep -c "test_consolidation" get-shit-done/workflows/audit-milestone.md</automated>
  </verify>
  <done>audit-milestone.md step 6 contains gaps.test_consolidation schema with strategy/source/action/estimated_reduction fields, and step 5f adds consolidation-aware tech_debt routing. The three existing gap types are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add gaps.test_consolidation parsing to plan-milestone-gaps.md</name>
  <files>get-shit-done/workflows/plan-milestone-gaps.md</files>
  <action>
Make two changes to plan-milestone-gaps.md:

**Change 1 — Step 1 (Load Audit Results): Add `gaps.test_consolidation` to the parsed fields.**

In step 1 where the workflow says "Parse YAML frontmatter to extract structured gaps" and lists `gaps.requirements`, `gaps.integration`, `gaps.flows`, add `gaps.test_consolidation` to that enumeration:

```markdown
Parse YAML frontmatter to extract structured gaps:
- `gaps.requirements` — unsatisfied requirements
- `gaps.integration` — missing cross-phase connections
- `gaps.flows` — broken E2E flows
- `gaps.test_consolidation` — steward consolidation proposals (when present)
```

Add a guard clause instruction: "If `gaps.test_consolidation` is absent or empty, skip test consolidation with no error. Treat as: `const consolidationGaps = gaps.test_consolidation || [];`"

**Change 2 — Step 3 (Group Gaps into Phases): Add test consolidation grouping rule.**

In step 3 where grouping rules are defined, add the test consolidation grouping rule after the existing rules:

```markdown
**Test consolidation grouping:**
- All test consolidation proposals are grouped into a single phase called "Test Suite Consolidation"
- The consolidation phase is always the last phase in the gap closure sequence
- Each proposal appears as a task in the consolidation phase
- If no test consolidation proposals exist, no consolidation phase is created
```

Do NOT modify the existing grouping rules for requirements/integration/flow gaps. The test consolidation grouping is additive.
  </action>
  <verify>
    <automated>grep -c "test_consolidation" get-shit-done/workflows/plan-milestone-gaps.md</automated>
  </verify>
  <done>plan-milestone-gaps.md step 1 enumerates gaps.test_consolidation alongside existing gap types with a guard clause for absent/empty, and step 3 includes the consolidation grouping rule. Existing gap type handling is untouched.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep "test_consolidation" get-shit-done/workflows/audit-milestone.md` returns matches in both the frontmatter template (step 6) and the status routing section (step 5f)
2. `grep "test_consolidation" get-shit-done/workflows/plan-milestone-gaps.md` returns matches in step 1 (parsing) and step 3 (grouping)
3. The existing `gaps.requirements`, `gaps.integration`, `gaps.flows` references in both files are unchanged
4. The existing status routing logic (`passed`, `gaps_found`, `tech_debt`) in audit-milestone.md is preserved — only the consolidation-specific `tech_debt` condition is added
5. No code files (frontmatter.cjs, autopilot.mjs) are modified
</verification>

<success_criteria>
- audit-milestone.md step 6 contains `gaps.test_consolidation` array schema with strategy, source, action, estimated_reduction fields
- audit-milestone.md has consolidation-aware status routing (step 5f) that routes consolidation-only to tech_debt
- plan-milestone-gaps.md step 1 parses `gaps.test_consolidation` with guard clause
- plan-milestone-gaps.md step 3 groups consolidation proposals into a single last phase
- Both files modified atomically — no write/read mismatch
- Pre-v2.8 behavior preserved when steward is disabled or no proposals exist
</success_criteria>

<output>
After completion, create `.planning/phases/75/75-01-SUMMARY.md`
</output>
