---
phase: 77
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [tests/edge-cases-consolidation.test.cjs]
autonomous: true
requirements: [EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "Consolidation-only gaps (no requirement/integration/flow gaps) parse correctly and produce a valid structure for phase creation"
    - "Empty and absent test_consolidation fields do not cause parsing errors"
    - "Budget gating values (OK, Warning, Over Budget) parse correctly from frontmatter"
    - "Steward proposal fields (source, action, estimated_reduction) pass through frontmatter parsing verbatim"
    - "Existing gap types parse identically whether or not test_consolidation is present"
    - "Autopilot tech_debt routing handles consolidation-only audits via existing code paths"
  artifacts:
    - path: "tests/edge-cases-consolidation.test.cjs"
      provides: "Edge case validation tests for consolidation bridge"
  key_links:
    - from: "tests/edge-cases-consolidation.test.cjs"
      to: "get-shit-done/bin/lib/frontmatter.cjs"
      via: "extractFrontmatter import"
      pattern: "require.*frontmatter"
---

<objective>
Validate all edge cases in the consolidation bridge produce correct behavior and existing gap types remain unaffected.

Purpose: Confirm Phases 75-76 work handles every edge case: consolidation-only gaps, mixed gaps, empty/absent consolidation, budget gating values, verbatim field passthrough, and autopilot compatibility.
Output: Test file covering all EDGE-01/02/03 requirements plus regression protection.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
@/Users/seanspade/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/77/77-CONTEXT.md
@.planning/phases/77/77-RESEARCH.md

@get-shit-done/bin/lib/frontmatter.cjs
@get-shit-done/workflows/plan-milestone-gaps.md
@get-shit-done/scripts/autopilot.mjs
@tests/frontmatter.test.cjs
@tests/helpers.cjs
@.planning/v2.3-MILESTONE-AUDIT.md

<interfaces>
From get-shit-done/bin/lib/frontmatter.cjs:
```javascript
function extractFrontmatter(content) // Parses YAML frontmatter, returns object
function reconstructFrontmatter(obj) // Serializes object back to YAML string
function spliceFrontmatter(content, newObj) // Replaces frontmatter in content
```

From tests/helpers.cjs:
```javascript
function runGsdTools(args, cwd) // Run gsd-tools command, returns {success, output, error}
function createTempProject() // Creates temp dir with .planning/phases/
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write edge case tests for consolidation frontmatter parsing</name>
  <files>tests/edge-cases-consolidation.test.cjs</files>
  <behavior>
    - EDGE-01a: extractFrontmatter with consolidation-only gaps (requirements=[], integration=[], flows=[], test_consolidation=[{entries}]) parses all fields correctly; test_consolidation is a non-empty array
    - EDGE-01b: extractFrontmatter with consolidation-only gaps and budget_status="Over Budget" parses budget_status correctly
    - EDGE-01c: When requirements/integration/flows are all empty arrays, the parsed result still has gaps as an object with those empty arrays — no crash or undefined
    - EDGE-02a: extractFrontmatter with status="tech_debt" parses the status field correctly (autopilot reads this to route consolidation-only audits)
    - EDGE-02b: Frontmatter with tech_debt status and test_health section parses both top-level status and nested test_health fields
    - EDGE-03a: Steward proposal source paths like "tests/foo/bar.test.js" survive extractFrontmatter round-trip without modification
    - EDGE-03b: Steward proposal action strings like "Delete stale tests: should handle edge case" survive parsing without truncation or re-interpretation
    - EDGE-03c: estimated_reduction numeric values parse as strings (frontmatter parser behavior) and are accessible
    - REGRESSION-01: Frontmatter with requirements/integration/flows gaps but NO test_consolidation field parses identically to pre-v2.8 structure (existing gap types unaffected)
    - REGRESSION-02: Frontmatter with empty test_consolidation array ([]) parses without error
    - REGRESSION-03: gaps_found status with mixed gap types and no consolidation still parses correctly
    - BUDGET-01: budget_status="OK" parses correctly from nested test_health object
    - BUDGET-02: budget_status="Warning" parses correctly
    - BUDGET-03: Absent test_health section results in no test_health key in parsed output (caller applies || default)
  </behavior>
  <action>
    Create `tests/edge-cases-consolidation.test.cjs` using node:test and node:assert.

    Import `extractFrontmatter` from `../get-shit-done/bin/lib/frontmatter.cjs`.

    Structure tests in describe blocks:
    1. "EDGE-01: Consolidation-only gaps" — Build frontmatter strings with gaps.requirements=[], gaps.integration=[], gaps.flows=[], gaps.test_consolidation populated with entries. Assert parsed structure is correct.
    2. "EDGE-02: Autopilot tech_debt routing compatibility" — Build frontmatter with status: tech_debt and test_health section. Assert status and test_health fields parse correctly.
    3. "EDGE-03: Verbatim steward data passthrough" — Build frontmatter with specific file paths and action strings in test_consolidation entries. Assert exact string equality after parsing.
    4. "Regression: Existing gap types unaffected" — Build frontmatter matching pre-v2.8 structure (no test_consolidation). Assert identical parse results.
    5. "Budget gating values" — Test OK, Warning, Over Budget parsing from test_health.budget_status.

    Use the v2.3-MILESTONE-AUDIT.md frontmatter structure as reference for realistic fixtures.

    Frontmatter fixture for consolidation-only (EDGE-01):
    ```
    ---
    milestone: "2.8"
    audited: "2026-03-20"
    status: tech_debt
    scores:
      requirements: 20/20
      phases: 3/3
      integration: 20/20
      flows: 3/3
    gaps:
      requirements: []
      integration: []
      flows: []
      test_consolidation:
        - strategy: prune
          source: "tests/commands.test.cjs"
          action: "Delete stale tests: should handle legacy format"
          estimated_reduction: 3
    test_health:
      budget_status: "Over Budget"
      redundant_tests: 2
      stale_tests: 1
      consolidation_proposals: 1
    ---
    ```

    For EDGE-03, use distinctive file paths and action text to confirm verbatim passthrough:
    - source: "tests/foo/bar-baz.test.cjs"
    - action: "Convert to test.each with inputs: [1, 2, 3], expected: [a, b, c]"

    Keep test count minimal — aim for ~14 tests covering all edge cases without redundancy.
  </action>
  <verify>
    <automated>node --test tests/edge-cases-consolidation.test.cjs</automated>
  </verify>
  <done>All edge case tests pass, covering EDGE-01 (consolidation-only parsing), EDGE-02 (tech_debt status compatibility), EDGE-03 (verbatim field passthrough), regression protection (existing gaps unaffected), and budget gating values</done>
</task>

<task type="auto">
  <name>Task 2: Validate autopilot.mjs tech_debt routing handles consolidation (read-only)</name>
  <files>tests/edge-cases-consolidation.test.cjs</files>
  <action>
    Add a describe block "EDGE-02: Autopilot consolidation compatibility (read-only validation)" to the same test file.

    These tests confirm the autopilot.mjs code paths exist and handle consolidation-only audits correctly. Per REQUIREMENTS.md Out of Scope, autopilot.mjs must NOT be modified.

    Read autopilot.mjs and verify:
    1. The `runMilestoneAudit()` function has a `case 'tech_debt':` branch (the path consolidation-only audits take)
    2. The `auto_accept_tech_debt` config check exists within that branch
    3. The `runGapClosureLoop()` function processes phases generically (no consolidation-specific code)

    Implementation: Use fs.readFileSync to read autopilot.mjs source, then use string/regex assertions:
    - Assert source contains `case 'tech_debt':`
    - Assert source contains `auto_accept_tech_debt`
    - Assert source does NOT contain `consolidat` (confirming no special-casing for consolidation in autopilot)
    - Assert `runGapClosureLoop` function exists

    This is a read-only structural validation — no imports or execution of autopilot.mjs.
  </action>
  <verify>
    <automated>node --test tests/edge-cases-consolidation.test.cjs</automated>
  </verify>
  <done>Autopilot.mjs structural validation confirms tech_debt routing exists, auto_accept_tech_debt gating exists, and no consolidation-specific code was added to autopilot</done>
</task>

</tasks>

<verification>
- All tests in `tests/edge-cases-consolidation.test.cjs` pass
- Full test suite still passes: `node --test tests/`
- No modifications to autopilot.mjs, plan-milestone-gaps.md, or audit-milestone.md
- Test count stays within budget (adding ~14-18 tests, phase budget is 50)
</verification>

<success_criteria>
- [ ] Consolidation-only gaps parse correctly (EDGE-01)
- [ ] tech_debt status routes through autopilot without special-casing (EDGE-02)
- [ ] Steward fields survive frontmatter parsing verbatim (EDGE-03)
- [ ] Existing gap types produce identical behavior with/without test_consolidation
- [ ] Budget gating values parse from nested test_health object
- [ ] All tests pass
- [ ] Full test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/77/77-01-SUMMARY.md`
</output>
