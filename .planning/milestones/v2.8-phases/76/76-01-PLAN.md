---
phase: 76
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [get-shit-done/workflows/plan-milestone-gaps.md]
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03, PHASE-01, PHASE-02, PHASE-03, TASK-01, TASK-02, TASK-03, TASK-04, TASK-05]

must_haves:
  truths:
    - "plan-milestone-gaps reads gaps.test_consolidation and creates tasks from it"
    - "When gaps.test_consolidation is absent or empty, plan-milestone-gaps skips with no error"
    - "Consolidation phase is created only when budget_status is Warning or Over Budget"
    - "Prune maps to delete, parameterize to refactor, promote to delete-and-verify, merge to reorganize"
    - "Each task includes verbatim steward file paths and estimated_reduction counts"
  artifacts:
    - path: "get-shit-done/workflows/plan-milestone-gaps.md"
      provides: "Budget gating, strategy-to-task mapping, consolidation phase presentation"
      contains: "budget_status"
  key_links:
    - from: "plan-milestone-gaps.md step 1"
      to: "step 3 budget gating"
      via: "test_health.budget_status extracted from frontmatter"
      pattern: "budget_status"
    - from: "plan-milestone-gaps.md step 3"
      to: "gap_to_phase_mapping"
      via: "consolidation proposals mapped to tasks via strategy templates"
      pattern: "test_consolidation"
---

<objective>
Add budget gating, strategy-to-task mapping, and consolidation phase presentation to plan-milestone-gaps.md

Purpose: Complete the bridge from steward proposals to executable gap closure tasks by extending the three locations Phase 75 prepared (step 1 parsing, step 3 grouping, gap_to_phase_mapping)
Output: Updated plan-milestone-gaps.md with budget-gated consolidation phase creation and four strategy-to-task templates
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76/76-CONTEXT.md
@.planning/phases/76/76-RESEARCH.md
@.planning/phases/75/75-01-SUMMARY.md
@get-shit-done/workflows/plan-milestone-gaps.md
@.planning/designs/2026-03-20-test-steward-consolidation-bridge-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add budget_status extraction and budget gating logic</name>
  <files>get-shit-done/workflows/plan-milestone-gaps.md</files>
  <action>
Two changes to plan-milestone-gaps.md:

**Step 1 (Load Audit Results):** Add `test_health.budget_status` to the frontmatter extraction list, right after the `gaps.test_consolidation` line added in Phase 75. Use the pattern:

```
Also extract `test_health.budget_status` from the frontmatter (values: `OK`, `Warning`, `Over Budget`). If absent, default to `OK`.
```

**Step 3 (Group Gaps into Phases):** In the "Test consolidation grouping" section that Phase 75 added, insert budget gating as the FIRST check before the existing grouping rules. Add this before the line "All test consolidation proposals are grouped into a single phase":

```
**Budget gating for test consolidation:**
- Read `test_health.budget_status` (extracted in step 1, defaults to `OK` if absent)
- If `budget_status` is `OK`: skip consolidation phase creation entirely, even if proposals exist — defer to tech debt
- If `budget_status` is `Warning` or `Over Budget`: proceed with consolidation phase creation below
- Log when skipping: "Skipping test consolidation — budget status OK, deferring to tech debt"
```

Do NOT modify the guard clause or parsing from Phase 75. Only ADD the budget_status extraction and gating.
  </action>
  <verify>
Read plan-milestone-gaps.md and confirm:
1. Step 1 mentions `test_health.budget_status` extraction with OK default
2. Step 3 has budget gating check before the consolidation grouping rules
3. The existing Phase 75 guard clause and grouping rule are preserved unchanged
  </verify>
  <done>Budget gating logic is in place: OK budget skips consolidation, Warning/Over Budget proceeds</done>
</task>

<task type="auto">
  <name>Task 2: Add strategy-to-task mapping templates and step 5 presentation</name>
  <files>get-shit-done/workflows/plan-milestone-gaps.md</files>
  <action>
Two changes to plan-milestone-gaps.md:

**`<gap_to_phase_mapping>` section:** After the existing flow gap mapping, add a new test consolidation mapping section:

```markdown
**Test consolidation gap -> Tasks:**

Each strategy maps to a specific task type with the steward's verbatim file paths and estimated_reduction count.

```yaml
# Prune -> Delete task
- name: "Remove stale tests in {source}"
  type: "delete"
  files: ["{source}"]
  action: "Delete test cases: {action}. Estimated reduction: {estimated_reduction} tests. Run test suite to confirm no regressions."

# Parameterize -> Refactor task
- name: "Parameterize tests in {source}"
  type: "refactor"
  files: ["{source}"]
  action: "{action}. Estimated reduction: {estimated_reduction} tests. Run test suite to confirm no regressions."

# Promote -> Delete-and-verify task
- name: "Remove unit tests subsumed in {source}"
  type: "delete-and-verify"
  files: ["{source}"]
  action: "{action}. Estimated reduction: {estimated_reduction} tests. Verify subsuming integration test still covers assertions. Run test suite."

# Merge -> Reorganize task
- name: "Consolidate tests from {source}"
  type: "reorganize"
  files: ["{source}"]
  action: "{action}. Estimated reduction: {estimated_reduction} tests. Run test suite to confirm no regressions."
```

All field values (`{source}`, `{action}`, `{estimated_reduction}`) come verbatim from the `gaps.test_consolidation` entries. Do not re-interpret or generalize.
```

**Step 5 (Present Gap Closure Plan):** After the existing gap presentation format, add the consolidation phase format that appears when consolidation proposals exist:

```markdown
{If consolidation proposals exist and budget gating passed:}

**Phase {N}: Test Suite Consolidation** (last in sequence)
Estimated total reduction: {sum of all estimated_reduction} tests
Tasks:
- [{strategy}] {task name} — {source} (est. -{estimated_reduction} tests)
- [{strategy}] {task name} — {source} (est. -{estimated_reduction} tests)
```

Do NOT modify the existing requirement/integration/flow gap templates.
  </action>
  <verify>
Read plan-milestone-gaps.md and confirm:
1. gap_to_phase_mapping section contains all four strategy templates (prune/delete, parameterize/refactor, promote/delete-and-verify, merge/reorganize)
2. Each template includes {source}, {action}, {estimated_reduction}, and "Run test suite"
3. Step 5 has consolidation phase presentation format with task listing
4. Existing gap templates are unchanged
  </verify>
  <done>All four strategy-to-task templates exist with verbatim fields, step 5 shows consolidation tasks</done>
</task>

</tasks>

<verification>
After both tasks, verify the complete plan-milestone-gaps.md:
1. Step 1 extracts both `gaps.test_consolidation` (Phase 75) AND `test_health.budget_status` (Phase 76)
2. Step 3 has budget gating before consolidation grouping rules
3. `<gap_to_phase_mapping>` has all four strategy templates following the existing template pattern
4. Step 5 includes consolidation phase presentation with per-task detail
5. All Phase 75 additions are preserved intact
</verification>

<success_criteria>
- plan-milestone-gaps.md reads gaps.test_consolidation and creates strategy-specific tasks
- Budget gating skips consolidation when budget_status is OK
- Budget gating creates consolidation phase when Warning or Over Budget
- Prune->delete, parameterize->refactor, promote->delete-and-verify, merge->reorganize templates exist
- Each template uses verbatim steward fields (source, action, estimated_reduction)
- Consolidation phase appears in step 5 presentation with task-level detail
</success_criteria>

<output>
After completion, create `.planning/phases/76/76-01-SUMMARY.md`
</output>
