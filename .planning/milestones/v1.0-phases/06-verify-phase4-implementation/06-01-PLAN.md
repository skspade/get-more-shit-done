---
phase: 06-verify-phase4-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-failure-handling/04-VERIFICATION.md
autonomous: true
requirements: [FAIL-01, FAIL-02, FAIL-03, FAIL-04]
must_haves:
  truths:
    - "Phase 4 has a VERIFICATION.md confirming FAIL-01 through FAIL-04 are satisfied with code-level evidence"
    - "Each FAIL-xx requirement is traced to specific functions and behavior in autopilot.sh, not just SUMMARY claims"
    - "VERIFICATION.md is written to the Phase 4 directory (.planning/phases/04-failure-handling/04-VERIFICATION.md), not the Phase 6 directory"
    - "Verification evidence distinguishes Phase 4 work (8 new functions) from Phase 5 fixes (extract_verification_status, extract_gaps_summary)"
  artifacts:
    - path: ".planning/phases/04-failure-handling/04-VERIFICATION.md"
      provides: "Phase-level verification report for failure handling implementation"
      contains: "FAIL-01"
  key_links:
    - from: "04-VERIFICATION.md requirements coverage"
      to: "autopilot.sh Phase 4 functions"
      via: "code inspection of construct_debug_prompt, run_step_with_retry, run_verify_with_debug_retry, write_failure_state, clear_failure_state, write_failure_report"
      pattern: "FAIL-0[1-4].*SATISFIED"
---

<objective>
Run phase-level verification on Phase 4's failure handling implementation to produce a VERIFICATION.md that closes the 4 unverified requirement gaps (FAIL-01 through FAIL-04).

Purpose: The v1.0 milestone audit found that Phase 4 was executed (04-01-SUMMARY.md and 04-02-SUMMARY.md exist) but no phase-level verification was performed. SUMMARY frontmatter claims are not sufficient evidence. This plan runs the standard verify-phase workflow to produce code-level evidence that the failure handling requirements are satisfied.
Output: `.planning/phases/04-failure-handling/04-VERIFICATION.md`
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-verify-phase4-implementation/06-RESEARCH.md
@.planning/phases/04-failure-handling/04-01-PLAN.md
@.planning/phases/04-failure-handling/04-02-PLAN.md
@.planning/phases/04-failure-handling/04-01-SUMMARY.md
@.planning/phases/04-failure-handling/04-02-SUMMARY.md

<interfaces>
<!-- Phase 4 functions to verify in autopilot.sh -->

From ~/.claude/get-shit-done/scripts/autopilot.sh (Phase 4 additions):
```bash
# Plan 01 functions:
construct_debug_prompt()          # Builds debugger spawn prompt with failure context
run_step_captured()               # Output capture variant of run_step using tee
run_step_with_retry()             # Wraps run_step_captured with debug-retry loop
run_verify_with_debug_retry()     # Handles verify step with crash retry and gaps retry
cleanup_temp()                    # Removes temp files tracked in TEMP_FILES array

# Plan 02 functions:
write_failure_state()             # Writes failure blocker to STATE.md via gsd-tools state add-blocker
clear_failure_state()             # Removes phase-specific failure blocker via resolve-blocker
write_failure_report()            # Generates FAILURE.md in phase directory

# Global variables:
TEMP_FILES=()                     # Array tracking temp files for cleanup
MAX_DEBUG_RETRIES                 # Loaded from config, default 3
```

From ~/.claude/get-shit-done/bin/lib/config.cjs:
```javascript
// Hardcoded defaults include:
autopilot: {
  circuit_breaker_threshold: 3,
  max_debug_retries: 3,
}
```

Verification tooling:
```bash
# Extract must_haves from plan frontmatter
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter get PLAN.md --field must_haves

# Automated artifact verification
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify artifacts PLAN.md

# Automated key link verification
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify key-links PLAN.md
```

Verification report template: @/home/seans/.claude/get-shit-done/templates/verification-report.md
Verification patterns reference: @/home/seans/.claude/get-shit-done/references/verification-patterns.md
Example verification report: @.planning/phases/05-fix-autopilot-wiring-bugs/05-VERIFICATION.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract must_haves and verify Phase 4 artifacts and key links</name>
  <files>.planning/phases/04-failure-handling/04-VERIFICATION.md</files>
  <action>
Run the standard verification workflow against Phase 4's implementation. This task covers the full verification process: extracting must_haves, checking artifacts, checking key links, inspecting code, and writing the report.

**Step 1: Extract must_haves from both Phase 4 plans:**

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter get \
  ".planning/phases/04-failure-handling/04-01-PLAN.md" --field must_haves

node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter get \
  ".planning/phases/04-failure-handling/04-02-PLAN.md" --field must_haves
```

These return the truths, artifacts, and key_links that define what Phase 4 must have delivered. Use these as the verification checklist.

**Step 2: Run automated artifact verification:**

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify artifacts \
  ".planning/phases/04-failure-handling/04-01-PLAN.md"

node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify artifacts \
  ".planning/phases/04-failure-handling/04-02-PLAN.md"
```

NOTE: Phase 4 artifacts use tilde paths (`~/.claude/get-shit-done/scripts/autopilot.sh`). If `gsd-tools verify artifacts` fails on path resolution, fall back to manual verification by reading the files at their resolved paths (`$HOME/.claude/get-shit-done/scripts/autopilot.sh` and `$HOME/.claude/get-shit-done/bin/lib/config.cjs`).

**Step 3: Run automated key link verification:**

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify key-links \
  ".planning/phases/04-failure-handling/04-01-PLAN.md"

node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify key-links \
  ".planning/phases/04-failure-handling/04-02-PLAN.md"
```

**Step 4: Manual code inspection of Phase 4 functions in autopilot.sh:**

Read `~/.claude/get-shit-done/scripts/autopilot.sh` and verify:

a) **All 8 Phase 4 functions exist and are substantive (not stubs):**
   - `construct_debug_prompt()` -- builds debugger prompt with failure context params
   - `run_step_captured()` -- captures output via tee to temp file
   - `run_step_with_retry()` -- wraps execution with debug-retry loop
   - `run_verify_with_debug_retry()` -- handles verify with crash retry and gap retry
   - `cleanup_temp()` -- removes tracked temp files
   - `write_failure_state()` -- writes failure blocker to STATE.md
   - `clear_failure_state()` -- removes failure blocker on success
   - `write_failure_report()` -- generates FAILURE.md in phase directory

b) **FAIL-01 evidence:** `run_step_with_retry` calls `construct_debug_prompt` and spawns `claude -p` with the debug prompt on step failure. Both execute and verify steps use retry-enabled functions in the main loop.

c) **FAIL-02 evidence:** `MAX_DEBUG_RETRIES` loaded from config with default 3. Loop counter increments and exits at threshold. `config.cjs` has `max_debug_retries: 3`.

d) **FAIL-03 evidence:** `run_step_with_retry` and `run_verify_with_debug_retry` both call `write_failure_report` on exhaustion. `print_halt_report` shows debug session info. FAILURE.md report includes required sections (type, attempts, error, sessions, resume).

e) **FAIL-04 evidence:** `write_failure_state` calls `gsd_tools state add-blocker` with structured failure fields. `clear_failure_state` removes blockers on success. Failure blocker format includes type, step, retries, exit_code, debug_sessions.

f) **Main loop integration:** The `execute)` case uses `run_step_with_retry` and the `verify)` case uses `run_verify_with_debug_retry`.

g) **Distinguish Phase 4 work from Phase 5 fixes:** Phase 5 only modified `extract_verification_status` and `extract_gaps_summary` (UAT.md fallback). Do NOT credit Phase 4 for the UAT.md fallback pattern -- that was Phase 5's fix.

**Step 5: Check config.cjs:**

Read `~/.claude/get-shit-done/bin/lib/config.cjs` and verify `max_debug_retries: 3` exists in the hardcoded autopilot defaults.

**Step 6: Scan for anti-patterns:**

Check the Phase 4 functions for:
- Stub functions (empty or single-line bodies with TODO/placeholder)
- Missing error handling in failure state writes
- Hardcoded retry counts instead of config-loaded values
- Functions defined but never called from main loop

**Step 7: Write VERIFICATION.md to Phase 4 directory:**

Write the report to `.planning/phases/04-failure-handling/04-VERIFICATION.md` (NOT Phase 6 directory).

Use the verification-report.md template structure. The report MUST include:
- Frontmatter with phase, verified date, status, score, re_verification flag
- Observable Truths table (from plan must_haves, verified against code)
- Required Artifacts table (with line counts and pattern checks)
- Key Link Verification table (wiring checks between functions)
- Requirements Coverage section mapping FAIL-01 through FAIL-04 to specific evidence
- Anti-Patterns Found section
- Gaps Summary

Model the format on the Phase 5 verification report (`.planning/phases/05-fix-autopilot-wiring-bugs/05-VERIFICATION.md`).

**CRITICAL:** Every truth must be confirmed against actual code, not SUMMARY claims. The entire reason Phase 6 exists is that SUMMARY claims were accepted without verification during Phase 4.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-failure-handling/04-VERIFICATION.md && grep -c "FAIL-0[1-4]" .planning/phases/04-failure-handling/04-VERIFICATION.md</automated>
    Verify: `04-VERIFICATION.md` exists in Phase 4 directory (not Phase 6 directory)
    Verify: All four FAIL-xx requirements appear with SATISFIED status
    Verify: Report has frontmatter with status field
    Verify: Truths table has status column with VERIFIED entries
    Verify: Requirements Coverage section maps each FAIL-xx to specific code evidence
  </verify>
  <done>
    - 04-VERIFICATION.md exists at .planning/phases/04-failure-handling/04-VERIFICATION.md
    - FAIL-01 verified: run_step_with_retry spawns gsd-debugger via claude -p on failure
    - FAIL-02 verified: MAX_DEBUG_RETRIES loaded from config with default 3, loop exits at threshold
    - FAIL-03 verified: write_failure_report generates FAILURE.md, print_halt_report shows debug sessions
    - FAIL-04 verified: write_failure_state writes structured blocker to STATE.md, clear_failure_state removes it
    - All truths confirmed against actual code in autopilot.sh, not SUMMARY claims
    - Report follows verification-report.md template format
  </done>
</task>

</tasks>

<verification>
- [ ] `.planning/phases/04-failure-handling/04-VERIFICATION.md` exists
- [ ] Report has valid frontmatter (phase, verified, status, score)
- [ ] All must_haves truths from 04-01-PLAN.md and 04-02-PLAN.md are checked
- [ ] FAIL-01 has code-level evidence (not just SUMMARY claims)
- [ ] FAIL-02 has code-level evidence (not just SUMMARY claims)
- [ ] FAIL-03 has code-level evidence (not just SUMMARY claims)
- [ ] FAIL-04 has code-level evidence (not just SUMMARY claims)
- [ ] Requirements Coverage section maps all 4 FAIL-xx to evidence
- [ ] Phase 4 functions verified against code, not Phase 5's changes
- [ ] No VERIFICATION.md written to Phase 6 directory (report belongs in Phase 4)
</verification>

<success_criteria>
Phase 4 has a VERIFICATION.md at `.planning/phases/04-failure-handling/04-VERIFICATION.md` that confirms FAIL-01 through FAIL-04 are satisfied. All verification evidence is traced to actual code behavior in autopilot.sh and config.cjs, not SUMMARY claims. The 4 unverified requirement gaps identified in the v1.0 milestone audit are closed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-verify-phase4-implementation/06-01-SUMMARY.md`
</output>
