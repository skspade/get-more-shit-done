---
phase: 26-design-presentation-and-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [get-shit-done/workflows/brainstorm.md, commands/gsd/brainstorm.md]
autonomous: true
requirements: [BRAIN-04, BRAIN-05, DESIGN-01, DESIGN-02]

must_haves:
  truths:
    - Workflow presents the selected approach as design sections, scaled to topic complexity (3-4 sections for simple, 5-7 for complex)
    - Each design section is presented individually with an AskUserQuestion approval prompt offering "Approve" and "Request revisions" options
    - When user requests revisions, workflow asks what to change, revises the section, and re-presents it for approval
    - Revision loop continues per-section until user approves — no artificial limit on revision rounds
    - After all sections are approved, the full design is assembled into a single markdown file
    - Design file is written to .planning/designs/YYYY-MM-DD-<topic-slug>-design.md
    - The .planning/designs/ directory is created via mkdir -p if it does not exist
    - Design file is committed to git with message format docs(brainstorm) using individual file staging (not git add . or git add -A)
    - Command file objective is updated to describe the full flow including design output
  artifacts:
    - get-shit-done/workflows/brainstorm.md
    - commands/gsd/brainstorm.md
  key_links:
    - Workflow step 5 (Session Complete) is replaced with design presentation steps 5-8
    - AskUserQuestion is reused for per-section approval (same pattern as steps 1, 3, 4)
    - Git commit follows execute-plan.md protocol — individual file staging, conventional commit format
---

<objective>
Extend the brainstorm workflow with design presentation after approach selection, per-section approval/revision loop, design doc file writing to `.planning/designs/`, and git commit.

Purpose: After a user selects an approach in the brainstorming session, the workflow now presents a full design in sections, allows revisions, writes the approved design to disk, and commits it.
Output: Modified workflow file and updated command file.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-design-presentation-and-output/26-CONTEXT.md

## Existing Files to Modify

@get-shit-done/workflows/brainstorm.md
@commands/gsd/brainstorm.md

## Key Patterns

- AskUserQuestion is already used in steps 1, 3, and 4 of the workflow for topic prompt, clarifying questions, and approach selection
- Git commit protocol from execute-plan.md: stage files individually with `git add`, use conventional commit format `{type}({scope}): {description}`
- File naming convention: `.planning/designs/YYYY-MM-DD-<topic-slug>-design.md` — topic slug is lowercase, hyphens, no special characters
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend brainstorm workflow with design presentation and output</name>
  <files>get-shit-done/workflows/brainstorm.md</files>
  <action>
Modify `get-shit-done/workflows/brainstorm.md` to replace step 5 (Session Complete) with four new steps: design section presentation, revision loop, file writing, and git commit.

**Replace step 5 entirely** with the following steps:

## 5. Present Design Sections

After the user selects an approach, break the selected approach into design sections. Scale section count to topic complexity:
- Simple topics (single feature, small scope): 3-4 sections
- Complex topics (architecture, multi-component): 5-7 sections

Section names and content should adapt to the topic domain rather than using a fixed template. Example section patterns:
- For a feature: Overview, Implementation, Data Model, API/Interface
- For architecture: Overview, Components, Data Flow, Integration Points, Migration
- For a process: Overview, Steps, Edge Cases, Success Criteria

Present each section one at a time:

```
## Design: {Section Name}

{Section content — detailed design for this aspect of the selected approach}
```

After presenting each section, use AskUserQuestion:
- header: "Design Review"
- question: "How does this section look?"
- options: ["Approve", "Request revisions"]

**If user approves:** Store the approved section content and proceed to the next section.

**If user requests revisions:** Go to step 6.

After all sections are approved, proceed to step 7.

## 6. Handle Revisions

When a user requests revisions for a section:

Use AskUserQuestion:
- header: "Revisions"
- question: "What would you like to change in this section?"
- (open-ended, no options)

Incorporate the user's feedback, revise the section content, and re-present it with the same approval prompt from step 5. This loop continues until the user approves the section. There is no limit on revision rounds.

## 7. Write Design File

After all sections are approved, assemble the full design into a markdown file.

Create the designs directory if needed:
```bash
mkdir -p .planning/designs
```

Generate the topic slug from $TOPIC: lowercase, replace spaces and special characters with hyphens, remove consecutive hyphens.

Generate the date string: YYYY-MM-DD format using current date.

Write to `.planning/designs/{date}-{topic-slug}-design.md`:

```markdown
# {$TOPIC} — Design

**Date:** {YYYY-MM-DD}
**Approach:** {Selected approach name}

{For each approved section:}
## {Section Name}

{Approved section content}
```

Display the file path:
```
Design written to: .planning/designs/{date}-{topic-slug}-design.md
```

## 8. Commit Design File

Stage and commit the design file:

```bash
git add ".planning/designs/{date}-{topic-slug}-design.md"
git commit -m "docs(brainstorm): design for {topic}"
```

Display completion:
```
Design committed to git.

Brainstorming session complete.
```

**Important implementation notes:**
- Do NOT use `git add .` or `git add -A` — stage only the design file
- The workflow ends after the commit — GSD routing is a future phase concern
- Keep the existing steps 1-4 completely unchanged
- The purpose section should be updated to mention design presentation and output
  </action>
  <verify>
```bash
# Verify workflow has all required sections
test -f get-shit-done/workflows/brainstorm.md && \
grep -q "Design Review" get-shit-done/workflows/brainstorm.md && \
grep -q "Request revisions" get-shit-done/workflows/brainstorm.md && \
grep -q "planning/designs/" get-shit-done/workflows/brainstorm.md && \
grep -q "mkdir -p" get-shit-done/workflows/brainstorm.md && \
grep -q 'git add' get-shit-done/workflows/brainstorm.md && \
grep -q 'git commit' get-shit-done/workflows/brainstorm.md && \
grep -q "AskUserQuestion" get-shit-done/workflows/brainstorm.md && \
echo "PASS" || echo "FAIL"
```
  </verify>
  <done>Workflow file has 8 steps: parse topic, explore context, clarifying questions, propose approaches, present design sections with per-section approval, handle revisions, write design file, commit to git</done>
</task>

<task type="auto">
  <name>Task 2: Update brainstorm command file objective</name>
  <files>commands/gsd/brainstorm.md</files>
  <action>
Update `commands/gsd/brainstorm.md` to reflect the full flow including design presentation and output.

Update the description in the frontmatter:
```yaml
description: Start a collaborative brainstorming session that explores context, asks questions, proposes approaches, and writes an approved design doc
```

Update the `<objective>` section to describe the complete end-to-end flow:

```markdown
<objective>
Run a collaborative brainstorming session: explore project context, ask clarifying questions one at a time, propose 2-3 distinct approaches with trade-offs and a recommendation, then present the selected approach as a design document in sections for approval. Approved design is written to `.planning/designs/` and committed to git.

**How it works:**
1. Parse optional topic from arguments (prompt if not provided)
2. Explore project context — read project files, recent commits, and relevant source code
3. Ask 3-5 clarifying questions one at a time, preferring multiple choice format
4. Propose 2-3 approaches with pros, cons, and a recommendation
5. User selects an approach
6. Present design in sections scaled to complexity, with approval after each section
7. User can request revisions to any section before approving
8. Write approved design to `.planning/designs/YYYY-MM-DD-<topic>-design.md` and commit to git

**Output:** Design document committed to `.planning/designs/`
</objective>
```

Do NOT change the frontmatter `allowed-tools` list, `name`, or `argument-hint`.
Do NOT change the `<execution_context>`, `<context>`, or `<process>` sections.
  </action>
  <verify>
```bash
# Verify command file updates
grep -q "writes an approved design doc" commands/gsd/brainstorm.md && \
grep -q "planning/designs/" commands/gsd/brainstorm.md && \
grep -q "committed to git" commands/gsd/brainstorm.md && \
grep -q "name: gsd:brainstorm" commands/gsd/brainstorm.md && \
echo "PASS" || echo "FAIL"
```
  </verify>
  <done>Command file objective describes the full brainstorming flow through design output, including section approval, file writing, and git commit</done>
</task>

<task type="auto">
  <name>Task 3: Verify end-to-end requirement coverage</name>
  <files>get-shit-done/workflows/brainstorm.md, commands/gsd/brainstorm.md</files>
  <action>
Verify all four requirements are covered:

1. **BRAIN-04** (design sections scaled to complexity with approval after each):
```bash
# Workflow has per-section approval with AskUserQuestion
grep -c "AskUserQuestion" get-shit-done/workflows/brainstorm.md
# Should be >= 4 (topic prompt, clarifying questions, approach selection, section approval)
```

2. **BRAIN-05** (user can request revisions before approving):
```bash
# Workflow has revision handling
grep -q "Request revisions" get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"
grep -q "What would you like to change" get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"
```

3. **DESIGN-01** (design written to .planning/designs/YYYY-MM-DD-<topic>-design.md):
```bash
grep -q 'planning/designs/' get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"
grep -q 'design.md' get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"
```

4. **DESIGN-02** (design committed to git):
```bash
grep -q 'git commit' get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"
grep -q 'git add' get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"
# Verify no git add . or git add -A
! grep -q 'git add \.' get-shit-done/workflows/brainstorm.md && ! grep -q 'git add -A' get-shit-done/workflows/brainstorm.md && echo "PASS: no broad staging" || echo "FAIL: broad staging found"
```
  </action>
  <verify>
```bash
echo "=== BRAIN-04: Per-section approval ==="
grep -q "Approve" get-shit-done/workflows/brainstorm.md && grep -q "section" get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"

echo "=== BRAIN-05: Revision loop ==="
grep -q "revisions" get-shit-done/workflows/brainstorm.md && grep -q "what to change\|What would you like to change\|what.*change" get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"

echo "=== DESIGN-01: Design file output ==="
grep -q "planning/designs/" get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"

echo "=== DESIGN-02: Git commit ==="
grep -q "git commit" get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"

echo "=== No Phase 27+ leaks ==="
! grep -q "ROUTE-01\|new-milestone\|new-project" get-shit-done/workflows/brainstorm.md && echo "PASS" || echo "FAIL"
```
  </verify>
  <done>All four requirements (BRAIN-04, BRAIN-05, DESIGN-01, DESIGN-02) are covered in the modified workflow, no Phase 27+ implementation leaks</done>
</task>

</tasks>

<verification>
1. `get-shit-done/workflows/brainstorm.md` has 8 process steps (4 existing + 4 new)
2. Step 5 presents design sections scaled to complexity with per-section AskUserQuestion approval
3. Step 6 handles revisions with open-ended question and re-presentation loop
4. Step 7 writes design file to `.planning/designs/YYYY-MM-DD-<topic-slug>-design.md`
5. Step 8 commits design file with individual staging and conventional commit format
6. `commands/gsd/brainstorm.md` objective describes the full flow through design output
7. No Phase 27 concerns (routing, new-milestone, new-project) implemented
8. Existing steps 1-4 remain unchanged
</verification>

<success_criteria>
- [ ] Design sections presented individually with per-section approval prompt (BRAIN-04)
- [ ] Section count scales to topic complexity: 3-4 simple, 5-7 complex (BRAIN-04)
- [ ] User can request revisions to any section and revise until approved (BRAIN-05)
- [ ] Design file written to `.planning/designs/YYYY-MM-DD-<topic-slug>-design.md` (DESIGN-01)
- [ ] `.planning/designs/` directory created if not exists (DESIGN-01)
- [ ] Design file committed to git with individual staging (DESIGN-02)
- [ ] Command file objective updated to describe full flow (supporting discoverability)
- [ ] Existing brainstorm steps 1-4 unchanged (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/26-design-presentation-and-output/26-01-SUMMARY.md`
</output>
