---
phase: 65-structure-and-state-checks
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified: [get-shit-done/bin/lib/validation.cjs, tests/validation.test.cjs]
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04]

must_haves:
  truths:
    - "STATE-01 detects milestone name mismatch between STATE.md and ROADMAP.md as error"
    - "STATE-02 detects completed_phases count mismatch as warning"
    - "STATE-03 detects total_phases count mismatch as warning"
    - "STATE-04 detects status field inconsistency as warning"
    - "All STATE checks skip gracefully when STATE.md or ROADMAP.md is missing"
    - "STATE checks have category 'state'"
  artifacts:
    - get-shit-done/bin/lib/validation.cjs
    - tests/validation.test.cjs
  key_links:
    - "STATE checks use extractFrontmatter() from frontmatter.cjs"
    - "STATE checks use getMilestoneInfo() from core.cjs"
---

<objective>
Implement all state consistency checks (STATE-01 through STATE-04) in validation.cjs using TDD. These checks compare STATE.md frontmatter fields against ROADMAP.md data to detect drift and inconsistencies.

Purpose: Detect STATE.md/ROADMAP.md mismatches that cause autopilot confusion
Output: Updated validation.cjs with 4 state checks + comprehensive tests
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
@/Users/seanspade/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/65-structure-and-state-checks/65-CONTEXT.md
@.planning/phases/65-structure-and-state-checks/65-RESEARCH.md
@.planning/phases/65-structure-and-state-checks/65-01-SUMMARY.md
@get-shit-done/bin/lib/validation.cjs
@get-shit-done/bin/lib/frontmatter.cjs
@get-shit-done/bin/lib/core.cjs
@tests/validation.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for state consistency checks</name>
  <files>tests/validation.test.cjs</files>
  <behavior>
    - STATE-01: When STATE.md milestone_name matches ROADMAP.md milestone name, check passes
    - STATE-01: When STATE.md milestone_name differs from ROADMAP.md milestone name, check fails with severity 'error'
    - STATE-01: When STATE.md or ROADMAP.md missing, check passes (skipped)
    - STATE-02: When STATE.md completed_phases matches ROADMAP.md [x] count, check passes
    - STATE-02: When counts differ, check fails with severity 'warning' and message shows both counts
    - STATE-02: When STATE.md or ROADMAP.md missing, check passes (skipped)
    - STATE-03: When STATE.md total_phases matches ROADMAP.md total phase count, check passes
    - STATE-03: When counts differ, check fails with severity 'warning'
    - STATE-03: When STATE.md or ROADMAP.md missing, check passes (skipped)
    - STATE-04: When status is 'completed' and all phases are [x], check passes
    - STATE-04: When status is 'completed' but unchecked phases exist, check fails with severity 'warning'
    - STATE-04: When status is 'active' and unchecked phases exist, check passes
    - STATE-04: When STATE.md or ROADMAP.md missing, check passes (skipped)
    - All STATE checks have category 'state'
    - runChecks with categories: ['state'] returns only STATE checks
    - validateProjectHealth runs both structure and state checks
  </behavior>
  <action>
Add new describe blocks to tests/validation.test.cjs for state consistency checks.

Create test fixtures:
- A minimal STATE.md with frontmatter containing milestone_name, progress.completed_phases, progress.total_phases, status
- A minimal ROADMAP.md with milestone heading and phase checkboxes

For STATE-01 tests:
- Create tmpDir with .planning/STATE.md (milestone_name: 'Test Milestone') and .planning/ROADMAP.md with heading containing 'Test Milestone'
- Test match (passes) and mismatch (fails with error)
- Test missing files (skipped, passes)

For STATE-02 tests:
- STATE.md with progress.completed_phases: 2, ROADMAP.md with 2x [x] Phase entries -> passes
- STATE.md with progress.completed_phases: 3, ROADMAP.md with 2x [x] -> fails warning

For STATE-03 tests:
- STATE.md with progress.total_phases: 5, ROADMAP.md with 5 total phase entries -> passes
- STATE.md with progress.total_phases: 3, ROADMAP.md with 5 total -> fails warning

For STATE-04 tests:
- STATUS 'completed' + all [x] -> passes
- STATUS 'completed' + some [ ] -> fails warning
- STATUS 'active' + some [ ] -> passes

ROADMAP fixture format (strip <details> blocks in tests):
```markdown
## Milestone v1.0: Test Milestone

## Phases

- [x] **Phase 1: Setup**
- [x] **Phase 2: Build**
- [ ] **Phase 3: Test**
```

STATE.md fixture format:
```markdown
---
milestone_name: Test Milestone
status: active
progress:
  completed_phases: 2
  total_phases: 3
---
```

All tests should FAIL initially since STATE checks don't exist yet.
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/get-more-shit-done && node --test tests/validation.test.cjs 2>&1 | tail -10</automated>
  </verify>
  <done>All new state consistency tests exist and fail (RED phase).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement state consistency checks in validation.cjs</name>
  <files>get-shit-done/bin/lib/validation.cjs</files>
  <action>
Add state consistency checks to validation.cjs:

1. Add imports at top:
```javascript
const { extractFrontmatter } = require('./frontmatter.cjs');
const { getMilestoneInfo } = require('./core.cjs');
```

2. Add helper function to count ROADMAP phases:
```javascript
function countRoadmapPhases(cwd) {
  const roadmapPath = path.join(cwd, '.planning', 'ROADMAP.md');
  const content = fs.readFileSync(roadmapPath, 'utf-8');
  // Strip <details> blocks (shipped milestones)
  const cleaned = content.replace(/<details>[\s\S]*?<\/details>/gi, '');
  const checked = (cleaned.match(/- \[x\]\s+\*?\*?Phase\s+\d+/gi) || []).length;
  const unchecked = (cleaned.match(/- \[ \]\s+\*?\*?Phase\s+\d+/gi) || []).length;
  return { checked, total: checked + unchecked };
}
```

3. Add STATE-01 check to checks array:
   - category: 'state', severity: 'error'
   - Skip if STATE.md or ROADMAP.md missing (return passed: true)
   - Read STATE.md, extractFrontmatter to get milestone_name
   - Call getMilestoneInfo(cwd) to get ROADMAP milestone name
   - Compare: if STATE.md milestone_name contains ROADMAP name or ROADMAP name contains STATE.md milestone_name, pass
   - Otherwise fail with message showing both values

4. Add STATE-02 check:
   - category: 'state', severity: 'warning'
   - Skip if STATE.md or ROADMAP.md missing
   - Get STATE.md progress.completed_phases via extractFrontmatter
   - Get ROADMAP [x] count via countRoadmapPhases
   - Compare as numbers; mismatch = fail with both counts in message

5. Add STATE-03 check:
   - category: 'state', severity: 'warning'
   - Skip if STATE.md or ROADMAP.md missing
   - Get STATE.md progress.total_phases
   - Get ROADMAP total count via countRoadmapPhases
   - Compare as numbers; mismatch = fail

6. Add STATE-04 check:
   - category: 'state', severity: 'warning'
   - Skip if STATE.md or ROADMAP.md missing
   - Get STATE.md status field
   - Get ROADMAP counts
   - If status is 'completed' but unchecked > 0, fail
   - If status is 'active' or anything else, pass (no inconsistency to detect beyond completed)

Key constraints:
- Wrap each check body in try/catch, return { passed: true, message: 'Skipped — error reading state files' } on error
- Use optional chaining for nested frontmatter access (fm.progress?.completed_phases)
- Parse string values to numbers for comparison (frontmatter returns strings)
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/get-more-shit-done && node --test tests/validation.test.cjs</automated>
  </verify>
  <done>All state consistency tests pass GREEN. runChecks with categories: ['state'] returns STATE-01 through STATE-04. validateProjectHealth includes both structure and state checks.</done>
</task>

</tasks>

<verification>
- `node --test tests/validation.test.cjs` — all tests pass
- `node -e "const v = require('./get-shit-done/bin/lib/validation.cjs'); const r = v.runChecks('.', { categories: ['state'] }); console.log(r.map(c => c.id).join(', '))"` — shows STATE-01, STATE-02, STATE-03, STATE-04
- `node -e "const v = require('./get-shit-done/bin/lib/validation.cjs'); const r = v.runChecks('.'); const cats = [...new Set(r.map(c => c.category))]; console.log(cats.sort().join(', '))"` — shows "state, structure"
- `node -e "const v = require('./get-shit-done/bin/lib/validation.cjs'); const r = v.validateProjectHealth('.'); console.log(r.checks.length)"` — shows total check count (structure + state)
</verification>
