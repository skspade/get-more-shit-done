---
phase: 68-testing-and-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [tests/cli.test.cjs, tests/verify-health.test.cjs]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-04]

must_haves:
  truths:
    - Total test count is at or below 750 after both plans complete
    - Entire handleHealth describe block removed from cli.test.cjs (17 tests)
    - 2 redundant tests removed from verify-health.test.cjs
    - Net removal of 19 tests (766 - 19 + 3 from plan 01 = 750)
  artifacts:
    - tests/cli.test.cjs (modified — handleHealth block removed)
    - tests/verify-health.test.cjs (modified — 2 redundant tests removed)
  key_links:
    - validation.test.cjs already covers all check categories (TEST-01 satisfied)
    - validation.test.cjs already covers auto-repair (TEST-02 satisfied)
---

<objective>
Remove redundant health tests to bring the total test count to 750 or below. Remove the entire handleHealth describe block from cli.test.cjs (17 tests fully covered by validation.test.cjs) and 2 redundant file-existence tests from verify-health.test.cjs.

Purpose: Satisfy TEST-04 requirement — total test count at or below 750 (pre-milestone budget). Also documents that TEST-01 and TEST-02 are already satisfied by existing validation.test.cjs tests.
Output: 19 fewer tests, total project count exactly 750 (with plan 01's +3)
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-testing-and-consolidation/68-CONTEXT.md
@.planning/phases/68-testing-and-consolidation/68-RESEARCH.md
@tests/cli.test.cjs
@tests/verify-health.test.cjs
@tests/validation.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove entire handleHealth describe block from cli.test.cjs</name>
  <files>tests/cli.test.cjs</files>
  <action>
In `tests/cli.test.cjs`, remove the entire `handleHealth` describe block. This includes:
- The `// --- handleHealth ---` comment (around line 533)
- The `describe('handleHealth', () => { ... });` block (lines ~535-712)
- All 17 tests within it, plus the beforeEach/afterEach setup

These 17 tests are fully redundant:
- File existence tests (HLTH-01): covered by validation.test.cjs STRUCT-01a through STRUCT-01f
- Config tests (HLTH-02): covered by validation.test.cjs STRUCT-02
- State consistency tests (HLTH-03): covered by validation.test.cjs STATE checks
- Error reporting tests (HLTH-04): covered by validation.test.cjs result shape
- Output mode tests: the rich/JSON/plain output format is a thin adapter; the underlying validation is tested elsewhere

Also remove the `const os = require('os');` import if it was only used by the handleHealth block (check if os is used elsewhere in the file first — if used elsewhere, keep it).

Make sure no dangling references or broken code remain after the removal.
  </action>
  <verify>
    <automated>node --test tests/cli.test.cjs 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
  </verify>
  <done>
    - handleHealth describe block completely removed from cli.test.cjs
    - All remaining cli.test.cjs tests pass
    - 17 tests removed from the file
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove 2 redundant tests from verify-health.test.cjs</name>
  <files>tests/verify-health.test.cjs</files>
  <action>
In `tests/verify-health.test.cjs`, remove 2 tests from the `validate health command` describe block that are most redundant with validation.test.cjs:

1. Remove `"returns 'broken' when .planning directory is missing"` (line ~61) — This exact scenario (STRUCT-01a) is already tested in validation.test.cjs. The verify-health version just adds a gsd-tools dispatch layer on top.

2. Remove `"returns 'healthy' when all checks pass"` (line ~163) — This general "everything passes" scenario is already tested in validation.test.cjs via the `validateProjectHealth return type` describe block which creates a valid project and verifies the healthy return.

Keep the remaining 12 tests which test specific check codes through the gsd-tools dispatch layer (STRUCT-01b through STRUCT-04, degraded status, repairable_count) — these provide meaningful integration coverage of the dispatch path.

Make sure no dangling references remain after removal.
  </action>
  <verify>
    <automated>node --test tests/verify-health.test.cjs 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
  </verify>
  <done>
    - verify-health.test.cjs has 12 tests (down from 14)
    - All remaining tests pass
    - 2 redundant tests removed
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify total test count is at or below 750</name>
  <files>tests/cli.test.cjs, tests/verify-health.test.cjs</files>
  <action>
Run the full test suite and check the total count:
```bash
node --test 2>&1 | grep "^# tests"
```

**Expected count (if plan 01 has also run):** 766 - 17 - 2 + 3 = 750
**Expected count (if plan 01 has NOT yet run):** 766 - 17 - 2 = 747

**If the count is above 750:** Identify additional redundant tests using:
```bash
node --test 2>&1 | grep "^# tests"
```
And remove more redundant tests from verify-health.test.cjs (next candidates: `errors when PROJECT.md is missing`, `errors when ROADMAP.md is missing` which test STRUCT-01b and STRUCT-01c already covered by validation.test.cjs).

**If the count is below 747 (plan 01 not yet run):** This is fine — plan 01 will add 3 and bring it to 750.

The goal is total count <= 750 after both plans complete. Document the final count.
  </action>
  <verify>
    <automated>node --test 2>&1 | grep "^# tests"</automated>
  </verify>
  <done>
    - `node --test` reports total tests <= 750
    - No test regressions (only the pre-existing roadmap.test.cjs failure remains)
  </done>
</task>

</tasks>

<verification>
- `node --test` total test count is at or below 750
- `node --test tests/cli.test.cjs` passes — handleHealth block fully removed
- `node --test tests/verify-health.test.cjs` passes — 12 remaining tests pass
- `grep -c "test(" tests/cli.test.cjs` shows reduced count (87 - 17 = 70)
- All other test files unaffected
</verification>
