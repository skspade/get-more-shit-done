---
phase: 68-testing-and-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/autopilot.test.cjs]
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - autopilot.test.cjs contains a 'pre-flight validation' describe block
    - Three test scenarios covered — healthy project passes, unhealthy project fails, repairable project repairs and passes
    - Tests call validateProjectHealth() directly with temp directory fixtures
    - All tests pass without requiring the claude CLI binary
  artifacts:
    - tests/autopilot.test.cjs (modified with new describe block)
  key_links:
    - validateProjectHealth() from get-shit-done/bin/lib/validation.cjs is the function under test
    - Pre-flight code path in autopilot.mjs lines 75-95 is the behavior being validated
---

<objective>
Add autopilot pre-flight integration tests to verify that `validateProjectHealth()` behaves correctly for the three scenarios the autopilot pre-flight code path depends on: healthy project, unhealthy project, and repairable project.

Purpose: Satisfy TEST-03 requirement — autopilot pre-flight integration tests confirm correct behavior for healthy, unhealthy, and repairable project states.
Output: 3 new tests in autopilot.test.cjs
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-testing-and-consolidation/68-CONTEXT.md
@.planning/phases/68-testing-and-consolidation/68-RESEARCH.md
@tests/autopilot.test.cjs
@tests/validation.test.cjs
@get-shit-done/bin/lib/validation.cjs
@get-shit-done/scripts/autopilot.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-flight validation describe block to autopilot.test.cjs</name>
  <files>tests/autopilot.test.cjs</files>
  <action>
Add a new describe block `autopilot pre-flight validation` to `tests/autopilot.test.cjs`. This block tests `validateProjectHealth()` from `get-shit-done/bin/lib/validation.cjs` directly, matching the same call the autopilot pre-flight makes at line 76.

Import `validateProjectHealth` at the top of the file:
```javascript
const { validateProjectHealth } = require('../get-shit-done/bin/lib/validation.cjs');
```

Use the existing `fs`, `path`, `os` imports already in the file. Create temp directories with `fs.mkdtempSync(path.join(os.tmpdir(), 'gsd-preflight-'))` in beforeEach and `fs.rmSync(tmpDir, { recursive: true, force: true })` in afterEach. Note: `os` is not currently imported in autopilot.test.cjs — add `const os = require('os');` if missing.

Add 3 tests:

**Test 1: healthy project passes pre-flight validation**
- Create a temp directory with a valid `.planning/` structure:
  - `.planning/PROJECT.md` with required sections (What This Is, Core Value, Requirements)
  - `.planning/ROADMAP.md` with `### v1.0 Test (In Progress)\n\n- [ ] Phase 1: Setup\n`
  - `.planning/STATE.md` with frontmatter: `milestone: v1.0`, `milestone_name: Test`, `status: active`, `progress: { total_phases: 1, completed_phases: 0 }`
  - `.planning/config.json` with `{ "model_profile": "balanced" }`
  - `.planning/phases/01-setup/` directory with `.gitkeep`
- Call `validateProjectHealth(tmpDir, { autoRepair: true })`
- Assert: `result.healthy === true`, `result.errors.length === 0`

**Test 2: unhealthy project fails pre-flight validation**
- Create same temp directory but omit `PROJECT.md`
- Call `validateProjectHealth(tmpDir, { autoRepair: true })`
- Assert: `result.healthy === false`, `result.errors.length > 0`
- This mirrors the autopilot code path where `!healthResult.healthy` triggers `process.exit(1)`

**Test 3: repairable project repairs and passes pre-flight validation**
- Create same valid temp directory but set STATE.md frontmatter `completed_phases: 99` and `total_phases: 99` (stale counts)
- Call `validateProjectHealth(tmpDir, { autoRepair: true })`
- Assert: `result.healthy === true` (repairs fixed the issues), `result.repairs.length > 0`
- This mirrors the autopilot code path where repairs are logged but execution continues

Place the new describe block after the existing `autopilot.mjs streaming functions` describe block (before the end of the file).
  </action>
  <verify>
    <automated>node --test tests/autopilot.test.cjs 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
  </verify>
  <done>
    - autopilot.test.cjs has a 'pre-flight validation' describe block with 3 passing tests
    - Tests cover healthy, unhealthy, and repairable scenarios
    - Tests do not require the claude CLI binary (no skip conditions)
  </done>
</task>

</tasks>

<verification>
- `node --test tests/autopilot.test.cjs` passes all tests including the 3 new ones
- New tests appear in output as `autopilot pre-flight validation > ...`
- No test requires the claude CLI (no skip conditions on new tests)
</verification>
