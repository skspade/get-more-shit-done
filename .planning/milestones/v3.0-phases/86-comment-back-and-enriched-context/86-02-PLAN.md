---
phase: 86-comment-back-and-enriched-context
plan: 02
type: execute
wave: 2
depends_on: [86-01]
files_modified: [get-shit-done/workflows/linear.md]
autonomous: true
requirements: [WKFL-02, WKFL-03, WKFL-04]

must_haves:
  truths:
    - Step 7 (write linear-context.md) frontmatter includes interview_summary multi-line field and route_source field
    - Step 8a quick route builds $DESCRIPTION from $INTERVIEW_CONTEXT goal/scope/criteria instead of raw title+description truncation
    - Step 8a milestone route MILESTONE-CONTEXT.md template includes $SELECTED_APPROACH (verify already present from Phase 85, no duplication)
    - Total $DESCRIPTION truncation limit remains 2000 chars
    - Linear comments still appended as supplementary context in quick route description
  artifacts:
    - get-shit-done/workflows/linear.md
  key_links:
    - Step 3 interview sets $INTERVIEW_CONTEXT -> Step 7 writes interview_summary to frontmatter
    - Step 3 interview sets $INTERVIEW_CONTEXT -> Step 8a quick route uses for $DESCRIPTION
    - Step 5 hybrid output sets $SELECTED_APPROACH -> Step 8a milestone route writes to MILESTONE-CONTEXT.md
---

<objective>
Enrich downstream consumers with interview data: linear-context.md frontmatter, quick route description synthesis, and milestone MILESTONE-CONTEXT.md.

Purpose: Replace raw ticket truncation with interview-enriched context (WKFL-02, WKFL-03, WKFL-04), giving planners and milestone builders higher quality input.
Output: Updated linear.md Steps 7 and 8 with enriched context patterns.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/86-comment-back-and-enriched-context/86-CONTEXT.md
@.planning/phases/86-comment-back-and-enriched-context/86-RESEARCH.md
@.planning/phases/86-comment-back-and-enriched-context/86-01-PLAN.md
@get-shit-done/workflows/linear.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich Step 7 linear-context.md frontmatter</name>
  <files>get-shit-done/workflows/linear.md</files>
  <action>
Modify Step 7 (Write linear-context.md — formerly Step 6 before Plan 01 renumbering) to add two new frontmatter fields to the linear-context.md template.

Find the YAML frontmatter block in the linear-context.md template within Step 7. The current template looks like:

```yaml
---
issue_ids: [{comma-separated issue identifiers}]
route: {quick|milestone}
interview: true
fetched: {ISO date}
---
```

Replace with:

```yaml
---
issue_ids: [{comma-separated issue identifiers}]
route: {quick|milestone}
route_source: {$FORCE_QUICK || $FORCE_MILESTONE ? "override" : "interview"}
interview_summary: |
  Goal: {extracted from $INTERVIEW_CONTEXT **Goal:** line}
  Scope: {extracted from $INTERVIEW_CONTEXT **Scope:** line}
  Criteria: {extracted from $INTERVIEW_CONTEXT **Success Criteria:** line}
  Route: {$ROUTE}
fetched: {ISO date}
---
```

Keep the `interview: true` field removed — `route_source` replaces it as the provenance indicator. The `route_source` value is "override" when either `$FORCE_QUICK` or `$FORCE_MILESTONE` flags were used, and "interview" otherwise (including ticket inference with confirmation).

The `interview_summary` field uses YAML pipe literal block scalar (`|`) for multi-line content. The values are extracted from the labeled sections in `$INTERVIEW_CONTEXT`.
  </action>
  <verify>
    <automated>grep -c "interview_summary:" get-shit-done/workflows/linear.md | grep -q "1" && grep -c "route_source:" get-shit-done/workflows/linear.md | grep -q "1"</automated>
  </verify>
  <done>Step 7 linear-context.md template has `route_source` and `interview_summary` fields. The old `interview: true` field is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich Step 8a quick route description synthesis</name>
  <files>get-shit-done/workflows/linear.md</files>
  <action>
Modify Step 8a (quick route description synthesis — formerly Step 7a) to use interview-enriched context instead of raw title+description truncation.

Find the current Step 8a description synthesis section. The current pattern is:

```
Build `$DESCRIPTION` from issue data:
- Start with first issue title
- Append "\n\n" + first issue description (truncated to 1500 chars)
- Append "\n\nLinear comments:\n" + first 3 comment bodies (each truncated to 200 chars)
- Truncate total `$DESCRIPTION` to 2000 chars
```

Replace with:

```
Build `$DESCRIPTION` from interview context and issue data:
- Start with first issue identifier + " — " + first issue title
- Append "\n\nGoal: " + {goal extracted from $INTERVIEW_CONTEXT **Goal:** line}
- Append "\nScope: " + {scope extracted from $INTERVIEW_CONTEXT **Scope:** line}
- Append "\nCriteria: " + {criteria extracted from $INTERVIEW_CONTEXT **Success Criteria:** line}
- Append "\n\nLinear comments:\n" + first 3 comment bodies (each truncated to 200 chars)
- Truncate total `$DESCRIPTION` to 2000 chars
```

The key change: primary content comes from $INTERVIEW_CONTEXT (validated by user during confirmation summary) instead of raw ticket description. Linear comments remain as supplementary context. The 2000-char total limit is unchanged.
  </action>
  <verify>
    <automated>grep -c "interview context and issue data" get-shit-done/workflows/linear.md | grep -q "1" && grep -c "INTERVIEW_CONTEXT" get-shit-done/workflows/linear.md | grep -qv "0"</automated>
  </verify>
  <done>Step 8a quick route builds $DESCRIPTION from $INTERVIEW_CONTEXT goal/scope/criteria with issue identifier header, appends Linear comments as supplementary context, and maintains 2000-char truncation limit.</done>
</task>

<task type="auto">
  <name>Task 3: Verify milestone MILESTONE-CONTEXT.md has Selected Approach</name>
  <files>get-shit-done/workflows/linear.md</files>
  <action>
Check Step 8a milestone route (formerly Step 7a milestone) to verify that the MILESTONE-CONTEXT.md template already includes `${$SELECTED_APPROACH}` from Phase 85's changes.

Read the milestone route's "Build MILESTONE-CONTEXT.md" section in Step 8 (after Plan 01 renumbering). Look for the `${$SELECTED_APPROACH}` variable in the template.

**If `${$SELECTED_APPROACH}` is already in the template** (expected from Phase 85): WKFL-04 is already satisfied. Verify it appears after the `## Additional Context` section. No changes needed — just confirm.

**If `${$SELECTED_APPROACH}` is NOT in the template** (unexpected): Add it at the end of the MILESTONE-CONTEXT.md template, after the `## Additional Context` section:

```markdown
## Additional Context

${For each issue with comments:}
...
${end for each}

${$SELECTED_APPROACH}
```

The `$SELECTED_APPROACH` variable was set in Step 5 (hybrid output) and contains the full "## Selected Approach" section with approach name, description, pros, and cons.
  </action>
  <verify>
    <automated>grep -c "SELECTED_APPROACH" get-shit-done/workflows/linear.md | grep -qv "0"</automated>
  </verify>
  <done>Milestone route MILESTONE-CONTEXT.md template includes $SELECTED_APPROACH after the Additional Context section. No duplication.</done>
</task>

</tasks>

<verification>
- [ ] Step 7 frontmatter has route_source field with interview|override logic
- [ ] Step 7 frontmatter has interview_summary multi-line field with Goal/Scope/Criteria/Route
- [ ] Step 7 no longer has `interview: true` field (replaced by route_source)
- [ ] Step 8a quick route uses $INTERVIEW_CONTEXT for $DESCRIPTION instead of raw description[:1500]
- [ ] Step 8a quick route still appends Linear comments as supplementary context
- [ ] Step 8a quick route still truncates total $DESCRIPTION to 2000 chars
- [ ] Step 8a milestone route MILESTONE-CONTEXT.md includes $SELECTED_APPROACH
- [ ] No duplication of $SELECTED_APPROACH in the template
</verification>
