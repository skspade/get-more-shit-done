---
phase: 87-command-spec-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [commands/gsd/linear.md]
autonomous: true
requirements: [WKFL-05, WKFL-06]

must_haves:
  truths:
    - "Command spec objective describes interview-driven routing, not complexity scoring"
    - "Command spec process section references interview instead of complexity scoring"
    - "No remaining references to 'scoring' or 'heuristic' in command spec"
  artifacts:
    - path: "commands/gsd/linear.md"
      provides: "Updated command spec with interview-driven language"
      contains: "interview"
  key_links:
    - from: "commands/gsd/linear.md"
      to: "get-shit-done/workflows/linear.md"
      via: "objective description accurately reflects workflow behavior"
      pattern: "interview"
---

<objective>
Update the command spec to accurately describe interview-driven routing instead of the removed complexity scoring heuristic.

Purpose: The workflow file was updated in Phases 84-86 to use interview-driven routing, but the command spec still references "complexity scoring". This creates a mismatch between what the command spec says and what the workflow actually does.
Output: Updated commands/gsd/linear.md with interview-driven language in objective and process sections.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@commands/gsd/linear.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update command spec objective section</name>
  <files>commands/gsd/linear.md</files>
  <action>
Replace the objective section (lines 19-27) with updated interview-driven language:

Replace line 20:
OLD: "Route Linear issues to GSD workflows. Fetches issue from Linear, auto-routes to quick or milestone based on complexity scoring, delegates to appropriate workflow."
NEW: "Route Linear issues to GSD workflows. Fetches issue from Linear, conducts an adaptive interview to gather goal, scope, and complexity signal, then routes to quick or milestone workflow."

Replace the bullet list (lines 22-26):
OLD:
- Reads the Linear issue via MCP tools
- Scores complexity to determine routing (quick task vs new milestone)
- Accepts override flags: --quick (force quick), --milestone (force milestone)
- Delegates to the appropriate GSD workflow with issue context
- Posts status comments back to Linear on completion

NEW:
- Reads the Linear issue via MCP tools
- Conducts 3-5 adaptive interview questions covering goal, scope, criteria, and complexity
- Routes based on complexity signal from interview (quick task vs new milestone)
- Accepts override flags: --quick (force quick), --milestone (force milestone)
- Presents confirmation summary (quick) or approach proposals (milestone)
- Posts interview summary to Linear before execution, completion comment after
  </action>
  <verify>
    <automated>grep -c "scoring\|heuristic" commands/gsd/linear.md | grep -q "^0$" && grep -q "interview" commands/gsd/linear.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Objective section describes interview-driven routing with no references to scoring or heuristic</done>
</task>

<task type="auto">
  <name>Task 2: Update command spec process section</name>
  <files>commands/gsd/linear.md</files>
  <action>
Replace line 41 in the process section:

OLD: "Preserve all workflow gates (issue fetch, complexity scoring, routing, execution, Linear status updates)."
NEW: "Preserve all workflow gates (issue fetch, interview, routing, hybrid output, execution, Linear status updates)."
  </action>
  <verify>
    <automated>grep -q "issue fetch, interview, routing, hybrid output" commands/gsd/linear.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Process section references interview and hybrid output instead of complexity scoring</done>
</task>

</tasks>

<verification>
1. `grep -c "scoring\|heuristic" commands/gsd/linear.md` returns 0
2. `grep -c "interview" commands/gsd/linear.md` returns at least 2 (objective + process)
3. Command spec YAML frontmatter unchanged (name, description, allowed-tools intact)
4. File still valid markdown with correct XML structure
</verification>

<success_criteria>
- commands/gsd/linear.md objective mentions interview phase, not scoring
- commands/gsd/linear.md process section says "interview" not "complexity scoring"
- Zero occurrences of "scoring" or "heuristic" in the file
- YAML frontmatter and execution_context/context sections unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/87-command-spec-and-documentation/87-01-SUMMARY.md`
</output>
