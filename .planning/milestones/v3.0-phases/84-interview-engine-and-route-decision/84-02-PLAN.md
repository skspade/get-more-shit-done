---
phase: 84-interview-engine-and-route-decision
plan: 02
type: execute
wave: 2
depends_on:
  - 84-01
files_modified:
  - .claude/get-shit-done/workflows/linear.md
  - .claude/commands/gsd/linear.md
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
  - WKFL-01

must_haves:
  truths:
    - "Complexity signal answer from interview determines route: Quick->quick, Medium->quick+$FULL_MODE, Milestone->milestone"
    - "The old $MILESTONE_SCORE heuristic (6-factor scoring table and threshold) is fully removed with no remnants"
    - "Override flags (--quick, --milestone) skip only the complexity question while other interview questions still run"
    - "When complexity is skipped because ticket explicitly states scope, Claude infers route and asks for confirmation"
    - "Workflow steps are renumbered from 7 to 9 steps with correct mapping"
    - "linear-context.md frontmatter no longer has a score field"
  artifacts:
    - path: ".claude/get-shit-done/workflows/linear.md"
      provides: "Routing decision in new Step 4, renumbered steps 5-9"
      contains: "Step 4: Route decision"
    - path: ".claude/commands/gsd/linear.md"
      provides: "Updated command spec referencing interview-driven routing"
      contains: "interview"
  key_links:
    - from: "linear.md Step 3 ($INTERVIEW_CONTEXT, complexity answer)"
      to: "linear.md Step 4 (routing decision)"
      via: "complexity answer parsed to set $ROUTE and $FULL_MODE"
      pattern: "Quick task.*quick.*Medium.*FULL_MODE.*Milestone.*milestone"
    - from: "linear.md Step 4 ($ROUTE)"
      to: "linear.md Step 5 (write linear-context.md)"
      via: "$ROUTE variable consumed by linear-context.md write"
      pattern: "route.*ROUTE"
---

<objective>
Add the routing decision as a new Step 4, delete the old scoring heuristic, renumber all workflow steps from 7 to 9, remove the `score` field from linear-context.md frontmatter, and update the command spec to reference interview-driven routing.

Purpose: Complete the transition from algorithmic scoring to interview-driven routing
Output: Modified linear.md with routing + renumbering, updated command spec
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-interview-engine-and-route-decision/84-CONTEXT.md
@.planning/phases/84-interview-engine-and-route-decision/84-RESEARCH.md
@.planning/phases/84-interview-engine-and-route-decision/84-01-SUMMARY.md

@.claude/get-shit-done/workflows/linear.md
@.claude/commands/gsd/linear.md

<interfaces>
<!-- Variables from Plan 01 (Step 3 interview) consumed by this plan: -->
$INTERVIEW_CONTEXT — structured string with labeled sections
$SKIP_COMPLEXITY — boolean
$INFERRED_COMPLEXITY — string (set when ticket explicitly states scope)
$FORCE_QUICK — boolean (from Step 1)
$FORCE_MILESTONE — boolean (from Step 1)
$FULL_MODE — boolean (from Step 1, may be updated by routing)

<!-- Step mapping (old -> new): -->
Old Step 1 (parse args)    -> New Step 1 (unchanged)
Old Step 2 (fetch issues)  -> New Step 2 (unchanged)
Old Step 3 (scoring)       -> DELETED, replaced by:
                              New Step 3 (interview) — added by Plan 01
                              New Step 4 (route decision) — added by this plan
Old Step 4 (write context) -> New Step 5
Old Step 5 (execute route) -> New Step 6
Old Step 6 (comment-back)  -> New Step 7
Old Step 7 (cleanup)       -> New Step 8
                              New Step 9 (done — final status)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace scoring heuristic with routing decision (new Step 4)</name>
  <files>.claude/get-shit-done/workflows/linear.md</files>
  <action>
Delete the entire current **Step 3: Route via heuristic** section (the section with the `$MILESTONE_SCORE` 6-factor scoring table, the scoring conditions, and the route decision based on score >= 3). This is approximately lines 94-126 in the current file.

In its place (after the new Step 3: Interview added by Plan 01), add a new **Step 4: Route decision** section:

```markdown
**Step 4: Route decision**

Three-tier routing fallback: flag override -> interview answer -> ticket inference with confirmation.

**Tier 1: Flag override** (already set in Step 1)

If `$FORCE_QUICK`:
- Set `$ROUTE = "quick"`
- Display: "Route: QUICK (flag override)"
- Proceed to Step 5.

If `$FORCE_MILESTONE`:
- Set `$ROUTE = "milestone"`
- Display: "Route: MILESTONE (flag override)"
- Proceed to Step 5.

**Tier 2: Interview answer** (from Step 3 Q4)

If complexity question was answered (not skipped):
- Parse the complexity answer from `$INTERVIEW_CONTEXT`:
  - "Quick task (hours)" → set `$ROUTE = "quick"`
  - "Medium (1-2 sessions)" → set `$ROUTE = "quick"`, set `$FULL_MODE = true`
  - "Milestone (multi-phase)" → set `$ROUTE = "milestone"`
- Display: "Route: {$ROUTE}{$FULL_MODE ? ' (full mode)' : ''} (from interview)"
- Proceed to Step 5.

**Tier 3: Ticket inference with confirmation** (when Q4 was skipped by ticket content)

If `$SKIP_COMPLEXITY` is true AND no flag override (Tier 1 didn't fire):
- Use `$INFERRED_COMPLEXITY` from Step 3 pre-scan to determine proposed route:
  - "quick fix" / "small change" → propose `$ROUTE = "quick"`
  - "epic" / "milestone" → propose `$ROUTE = "milestone"`
- Ask for confirmation:
```
AskUserQuestion(
  header: "Route Confirmation",
  question: "Based on the ticket, this looks like a {proposed route}. Is that right?",
  options: ["Yes, {proposed route}", "No, it's a quick task (hours)", "No, it's medium (1-2 sessions)", "No, it's a milestone (multi-phase)"],
  followUp: null
)
```
- Map confirmation answer to `$ROUTE` and `$FULL_MODE` (same mapping as Tier 2)
- Append to `$INTERVIEW_CONTEXT`: update the Complexity line with the confirmed answer
- Display: "Route: {$ROUTE} (confirmed from ticket inference)"

Display route banner:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► ROUTE: {$ROUTE}{$FULL_MODE ? ' (FULL MODE)' : ''}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---
```

Ensure there are no remaining references to `$MILESTONE_SCORE` anywhere in the file. Search for "MILESTONE_SCORE", "score:", and the 6-factor scoring conditions. Delete all of them.
  </action>
  <verify>
    <automated>grep -c "MILESTONE_SCORE" .claude/get-shit-done/workflows/linear.md | grep -q "^0$" && grep -c "Step 4: Route decision" .claude/get-shit-done/workflows/linear.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
Old scoring heuristic fully deleted. New Step 4 has three-tier routing (flag override -> interview answer -> ticket inference with confirmation). No remnants of $MILESTONE_SCORE in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Renumber steps, update linear-context.md frontmatter, update command spec</name>
  <files>.claude/get-shit-done/workflows/linear.md, .claude/commands/gsd/linear.md</files>
  <action>
**Part A: Renumber steps in linear.md**

Renumber all steps after the new Step 4:
- Current "Step 4: Write linear-context.md" → **Step 5: Write linear-context.md**
- Current "Step 5: Execute route" → **Step 6: Execute route**
- Current "Step 6: Comment-back to Linear issues" → **Step 7: Comment-back to Linear issues**
- Current "Step 7: Cleanup" → **Step 8: Cleanup**

Search the entire file for any step number references in:
- Section headers (e.g., "**Step N:**")
- Cross-references within step text (e.g., "proceed to Step 4" should become "proceed to Step 5")
- The `<purpose>` section at the top (update to say "9 steps" if it mentions step count)
- Success criteria at the bottom
- Comments or notes referencing step numbers

Also search for references like "(WKFL-05)" and "(WKFL-06)" in step headers — these are requirement traceability tags. Leave them as-is (they refer to REQUIREMENTS.md IDs, not step numbers).

**Part B: Update linear-context.md frontmatter in Step 5**

In the new Step 5 (write linear-context.md), update the frontmatter template:
- Remove the `score: {$MILESTONE_SCORE or "override"}` line
- Add `interview: true` field
- The `route` field stays as-is

The updated frontmatter should be:
```yaml
---
issue_ids: [{comma-separated issue identifiers}]
route: {quick|milestone}
interview: true
fetched: {ISO date}
---
```

**Part C: Update command spec**

In `.claude/commands/gsd/linear.md`:
- Update the `<objective>` text: change "auto-routes to quick or milestone based on complexity scoring" to "asks adaptive interview questions and routes based on complexity signal"
- Change "Scores complexity to determine routing" to "Asks adaptive interview questions to determine routing"
- In `<process>`, change "complexity scoring" to "interview-driven routing"

**Part D: Update success criteria in linear.md**

Update the `<success_criteria>` section at the bottom of linear.md:
- Replace scoring-related criteria with interview-related criteria
- Specifically: "Routing heuristic scores on issue count, sub-issues..." → "Interview asks 3-5 adaptive questions after ticket fetch, skipping already-answered questions"
- "Score >= 3 routes to milestone, < 3 routes to quick" → "Complexity signal from interview determines route: Quick/Medium/Milestone"
- Update step count references
  </action>
  <verify>
    <automated>grep -c "score:" .claude/get-shit-done/workflows/linear.md | grep -qE "^[01]$" && grep -c "Step 5:" .claude/get-shit-done/workflows/linear.md | grep -q "[1-9]" && grep -c "Step 8:" .claude/get-shit-done/workflows/linear.md | grep -q "[1-9]" && grep -c "interview" .claude/commands/gsd/linear.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
Steps renumbered 1-8 (old 1-2 unchanged, old 3 deleted, new 3-4 inserted, old 4-7 become 5-8). linear-context.md frontmatter has no score field. Command spec references interview-driven routing. Success criteria updated.
  </done>
</task>

</tasks>

<verification>
- [ ] $MILESTONE_SCORE has zero occurrences in linear.md (search entire file)
- [ ] `score:` field does not appear in linear-context.md frontmatter template
- [ ] Steps are numbered 1 through 8 with no gaps or duplicates
- [ ] Step 4 has three-tier routing: flag override, interview answer, ticket inference
- [ ] Routing maps: "Quick task" -> quick, "Medium" -> quick + $FULL_MODE, "Milestone" -> milestone
- [ ] Ticket inference path uses AskUserQuestion for confirmation
- [ ] Command spec (.claude/commands/gsd/linear.md) mentions "interview" not "scoring"
- [ ] Success criteria reference interview, not scoring heuristic
- [ ] All cross-references to step numbers are updated
</verification>

<success_criteria>
The scoring heuristic is completely removed. Routing is driven by interview complexity signal with three-tier fallback. Steps are properly renumbered 1-8. The command spec reflects interview-driven routing. No remnants of the old scoring system exist.
</success_criteria>

<output>
After completion, create `.planning/phases/84-interview-engine-and-route-decision/84-02-SUMMARY.md`
</output>
