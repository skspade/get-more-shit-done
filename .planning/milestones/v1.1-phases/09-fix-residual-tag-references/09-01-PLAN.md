---
phase: 09-fix-residual-tag-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/complete-milestone.md
  - docs/USER-GUIDE.md
autonomous: true
requirements: [WF-03, DOC-03]
gap_closure: true

must_haves:
  truths:
    - "The complete-milestone offer_next step output does not contain 'Tag: v[X.Y]'"
    - "USER-GUIDE.md usage examples do not contain '# Archive, tag, done'"
  artifacts:
    - path: "get-shit-done/workflows/complete-milestone.md"
      provides: "Milestone completion workflow with clean offer_next output"
      contains: "Summary: .planning/MILESTONES.md"
    - path: "docs/USER-GUIDE.md"
      provides: "User guide with accurate usage example comments"
  key_links: []
---

<objective>
Remove two residual git tag references missed by Phase 8 and identified by the v1.1 milestone audit.

Purpose: The v1.1 milestone audit found two gaps: (1) the complete-milestone workflow offer_next step still displays "Tag: v[X.Y]" to the user after every run, and (2) two usage examples in USER-GUIDE.md still contain the inline comment "# Archive, tag, done". Both mislead users into believing the command creates git tags.
Output: Updated complete-milestone.md (offer_next output cleaned) and updated USER-GUIDE.md (usage example comments corrected).
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove "Tag: v[X.Y]" from offer_next step output in complete-milestone.md</name>
  <files>get-shit-done/workflows/complete-milestone.md</files>
  <action>
  Edit `get-shit-done/workflows/complete-milestone.md`:

  **Line 673:** Delete the entire line `Tag: v[X.Y]` from the offer_next step output template. This line appears between `Summary: .planning/MILESTONES.md` (line 672) and a blank line (line 674).

  After deletion, the output block should read:

  ```
  Archived:
  - milestones/v[X.Y]-ROADMAP.md
  - milestones/v[X.Y]-REQUIREMENTS.md

  Summary: .planning/MILESTONES.md

  ---
  ```

  Note: The blank line after `Summary: .planning/MILESTONES.md` should remain (do not collapse double blank lines). The `v[X.Y]` references in the `Archived:` list are file paths, not git tag claims — leave those as-is.

  Do NOT modify any other lines in the file.
  </action>
  <verify>
    <automated>! grep -q "^Tag: v\[X\.Y\]" get-shit-done/workflows/complete-milestone.md && echo "PASS: No Tag: v[X.Y] line found" || echo "FAIL: Tag: v[X.Y] still present"</automated>
  </verify>
  <done>The offer_next step output no longer displays "Tag: v[X.Y]" to the user after milestone completion</done>
</task>

<task type="auto">
  <name>Task 2: Replace "# Archive, tag, done" comments in USER-GUIDE.md usage examples</name>
  <files>docs/USER-GUIDE.md</files>
  <action>
  Edit `docs/USER-GUIDE.md`:

  **Line 323:** Change:
  ```
  /gsd:complete-milestone     # Archive, tag, done
  ```
  to:
  ```
  /gsd:complete-milestone     # Archive and done
  ```

  **Line 362:** Change:
  ```
  /gsd:complete-milestone     # Archive, tag, done
  ```
  to:
  ```
  /gsd:complete-milestone     # Archive and done
  ```

  Both occurrences are inline code comments in bash code blocks within usage examples. Replace "Archive, tag, done" with "Archive and done" to accurately reflect that the command archives the milestone without creating a git tag.

  Do NOT modify any other lines in the file.
  </action>
  <verify>
    <automated>! grep -q "# Archive, tag, done" docs/USER-GUIDE.md && echo "PASS: No '# Archive, tag, done' found" || echo "FAIL: '# Archive, tag, done' still present"</automated>
  </verify>
  <done>Both usage example comments in USER-GUIDE.md accurately describe archive-only behavior without mentioning tagging</done>
</task>

</tasks>

<verification>
Run these checks after both tasks complete:
1. `grep -c "Tag: v\[X\.Y\]" get-shit-done/workflows/complete-milestone.md` returns 0
2. `grep -c "# Archive, tag, done" docs/USER-GUIDE.md` returns 0
3. `grep -c "# Archive and done" docs/USER-GUIDE.md` returns 2
4. `grep -c "offer_next" get-shit-done/workflows/complete-milestone.md` returns at least 1 (step still exists)
5. `grep -c "Summary: .planning/MILESTONES.md" get-shit-done/workflows/complete-milestone.md` returns at least 1 (surrounding content preserved)
</verification>

<success_criteria>
- WF-03: The complete-milestone.md offer_next step output does not contain "Tag: v[X.Y]"
- DOC-03: USER-GUIDE.md usage examples do not contain "# Archive, tag, done"
- All other content in both files is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-fix-residual-tag-references/09-01-SUMMARY.md`
</output>
