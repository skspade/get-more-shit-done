---
phase: 103-test-suite-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/autopilot.test.cjs, tests/cli.test.cjs]
autonomous: true
requirements: []

must_haves:
  truths:
    - "node --test exits with 0 failures in autopilot.test.cjs streaming and stdin redirect blocks"
    - "Test count reported by node --test is at least 14 fewer than before consolidation"
    - "routeCommand 'returns null for unknown command' test still exists in cli.test.cjs"
  artifacts:
    - tests/autopilot.test.cjs
    - tests/cli.test.cjs
  key_links: []
---

<objective>
Prune stale tests and promote subsumed unit tests to bring the test suite under the 800-test budget.

Purpose: The SDK migration (Phases 98-100) deleted `runClaudeStreaming`, `displayStreamEvent`, and all `claude -p` shell invocations from autopilot.mjs. Tests asserting those constructs now fail. Additionally, 5 `routeCommand` unit tests in cli.test.cjs are fully subsumed by integration tests. Removing these brings the suite under budget.

Output: Updated tests/autopilot.test.cjs and tests/cli.test.cjs with fewer tests and no failures in the consolidated blocks.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/103-test-suite-consolidation/103-CONTEXT.md
@tests/autopilot.test.cjs
@tests/cli.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prune stale tests and rename describe block in autopilot.test.cjs</name>
  <files>tests/autopilot.test.cjs</files>
  <action>
In tests/autopilot.test.cjs, make three changes:

**A. Delete the entire `autopilot.mjs stdin redirect (regression)` describe block** (the block starting with `describe('autopilot.mjs stdin redirect (regression)'` through its closing `});`). This block contains 4 tests that validate `claude -p` shell invocations which no longer exist after SDK migration. All 4 tests are stale: "every claude -p shell invocation includes < /dev/null", "there are exactly 2 claude -p shell invocations", "count of < /dev/null matches count of claude -p invocations", "comment and console.log lines with claude -p are not flagged". Also delete the section comment above it (`// --- Static Analysis: stdin redirect regression ---`).

**B. In the `autopilot.mjs streaming functions (static analysis)` describe block**, delete these 5 tests that reference deleted functions:
  - "runClaudeStreaming function exists"
  - "displayStreamEvent function exists"
  - "streaming path uses --output-format stream-json"
  - "displayStreamEvent handles assistant events"
  - "displayStreamEvent handles tool_use events"

Retain these 5 tests (they validate constructs that still exist):
  - "--quiet flag is in knownFlags"
  - "QUIET constant is defined"
  - "stall timer uses getConfig for timeout"
  - "stall timer uses unref to prevent blocking exit"
  - "runClaudeStreaming returns exitCode and stdout"

**C. Rename the describe block** from `autopilot.mjs streaming functions (static analysis)` to `autopilot.mjs SDK functions (static analysis)`. Also update the section comment above it from `// --- Static Analysis: streaming functions` to `// --- Static Analysis: SDK functions`.

**D. Rename the test** "runClaudeStreaming returns exitCode and stdout" to "runAgentStep returns exitCode and stdout".
  </action>
  <verify>
    <automated>node --test tests/autopilot.test.cjs 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
  </verify>
  <done>All tests in autopilot.test.cjs pass (0 failures). The 5 stale streaming tests, 4 stale stdin redirect tests are deleted. Describe block renamed to "SDK functions". Test renamed to "runAgentStep returns exitCode and stdout".</done>
</task>

<task type="auto">
  <name>Task 2: Remove subsumed routeCommand unit tests from cli.test.cjs</name>
  <files>tests/cli.test.cjs</files>
  <action>
In tests/cli.test.cjs, in the `routeCommand` describe block, delete these 5 tests that are subsumed by the `gsd-cli binary` integration tests:
  - "routes progress to handler with real data"
  - "routes todos to handler with structured data"
  - "routes health to handler with structured data"
  - "routes settings to handler"
  - "routes help to handler with all command names"

Retain "returns null for unknown command" (the integration test checks exit code 1, not the null return value from routeCommand).

Keep the `routeCommand` describe block header and section comment -- the block still has one test.
  </action>
  <verify>
    <automated>node --test tests/cli.test.cjs 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
  </verify>
  <done>The routeCommand describe block contains only "returns null for unknown command". All cli.test.cjs tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite: `node --test`
- All tests pass (0 failures from consolidated blocks)
- Total test count is reduced by 14 (5 streaming + 4 stdin redirect + 5 routeCommand)
- Remaining tests in affected blocks still pass
</verification>

<success_criteria>
- `node --test` shows 0 failures in autopilot.test.cjs and cli.test.cjs
- Test count reduced by 14 from pre-consolidation baseline
- "returns null for unknown command" test retained in cli.test.cjs
- Describe block renamed to "autopilot.mjs SDK functions (static analysis)"
- Test renamed to "runAgentStep returns exitCode and stdout"
</success_criteria>

<output>
After completion, create `.planning/phases/103-test-suite-consolidation/103-01-SUMMARY.md`
</output>
