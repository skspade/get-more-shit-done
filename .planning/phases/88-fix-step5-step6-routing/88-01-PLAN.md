---
phase: 88-fix-step5-step6-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [get-shit-done/workflows/linear.md]
autonomous: true
requirements: [CMNT-01, CMNT-04]

must_haves:
  truths:
    - All three Step 5 exit paths reference Step 6 (not Step 7)
    - Pre-execution comment-back (Step 6) is reachable from every Step 5 exit
    - Step flow is Step 5 → Step 6 → Step 7 for both quick and milestone routes
  artifacts:
    - get-shit-done/workflows/linear.md (modified)
  key_links:
    - Step 5 exit paths → Step 6 pre-execution comment-back → Step 7 write linear-context.md
---

<objective>
Fix three Step 5 exit paths in linear.md that incorrectly skip Step 6 (pre-execution comment-back) and jump directly to Step 7 (write linear-context.md).

Purpose: Ensures pre-execution interview summary is posted to Linear before execution starts (CMNT-01) and tickets receive both pre-execution and post-execution comments (CMNT-04).
Output: Corrected linear.md with all Step 5 exits routing through Step 6.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@get-shit-done/workflows/linear.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Step 5 exit path references from Step 7 to Step 6</name>
  <files>get-shit-done/workflows/linear.md</files>
  <action>
  Change three step references in get-shit-done/workflows/linear.md:

  1. Line 353 (quick route "Yes, proceed"):
     Change: `**If "Yes, proceed":** Continue to Step 7.`
     To: `**If "Yes, proceed":** Continue to Step 6.`

  2. Line 374 (quick route "Cancel -- proceed as-is"):
     Change: `**If "Cancel — proceed as-is":** Continue to Step 7.`
     To: `**If "Cancel — proceed as-is":** Continue to Step 6.`

  3. Line 474 (milestone route approach selected):
     Change: `Proceed to Step 7.`
     To: `Proceed to Step 6.`

  These are text-only changes. No logic, no new code, no structural changes.
  </action>
  <verify>
    <automated>grep -n "Step 7" get-shit-done/workflows/linear.md | grep -v "^550:" | grep -v "Step 7:" | grep -v "step 7" | grep -c "Continue to Step 7\|Proceed to Step 7" | xargs test 0 -eq && echo "PASS: No remaining incorrect Step 7 references in Step 5" || echo "FAIL: Found incorrect Step 7 references"</automated>
  </verify>
  <done>
  - Line 353 says "Continue to Step 6" (was Step 7)
  - Line 374 says "Continue to Step 6" (was Step 7)
  - Line 474 says "Proceed to Step 6" (was Step 7)
  - No other Step references were changed
  - Step 7 header at line 550 remains unchanged
  </done>
</task>

</tasks>

<verification>
1. All Step 5 exit paths route to Step 6: `grep -n "Continue to Step\|Proceed to Step" get-shit-done/workflows/linear.md` — verify lines 353, 374, 474 reference Step 6
2. Step 6 header unchanged at line 490: `grep -n "Step 6" get-shit-done/workflows/linear.md`
3. Step 7 header unchanged at line 550: `grep -n "Step 7" get-shit-done/workflows/linear.md`
4. No unintended changes: `git diff --stat` shows only linear.md with 3 lines changed
</verification>

<success_criteria>
- All Step 5 exit paths route to Step 6 (pre-execution comment) before Step 7
- Pre-execution comment (Step 6) is reachable before execution starts
- Tickets can receive two comments: pre-execution (Step 6) + post-execution (Step 9)
- No other step references or workflow logic changed
</success_criteria>

<output>
After completion, create `.planning/phases/88-fix-step5-step6-routing/88-01-SUMMARY.md`
</output>
