---
phase: 85-hybrid-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [~/.claude/get-shit-done/workflows/linear.md]
autonomous: true
requirements: [OUTP-01, OUTP-02]

must_haves:
  truths:
    - Quick route displays a confirmation summary (issue, goal, scope, criteria, route) before execution
    - "Yes, proceed" continues to next step
    - "No, let me clarify" presents a dimension picker with Goal, Scope, Success Criteria, Complexity options
    - Selecting a dimension re-asks that single interview question and updates $INTERVIEW_CONTEXT
    - Updated confirmation summary is re-displayed after re-ask with same Yes/No prompt
    - Re-clarification loop is unlimited — continues until user selects "Yes, proceed"
    - Steps 5-8 are renumbered to 6-9 with all internal references updated
  artifacts:
    - ~/.claude/get-shit-done/workflows/linear.md
  key_links:
    - Step 5 reads $ROUTE, $FULL_MODE, $INTERVIEW_CONTEXT, and $ISSUES from Steps 3-4
    - Step 5 quick route may mutate $INTERVIEW_CONTEXT via re-ask, carrying forward to Step 6+
    - Step renumbering updates all sub-step references (6a-6i become 7a-7i)
---

<objective>
Add Step 5 (Hybrid Output) to linear.md with the quick route confirmation summary and re-ask loop, and renumber existing Steps 5-8 to Steps 6-9.

Purpose: Users see a confirmation summary before quick route execution and can correct any interview dimension without restarting the full interview.
Output: Updated linear.md with new Step 5 (quick route branch) and renumbered subsequent steps.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/85-hybrid-output/85-CONTEXT.md
@.planning/phases/85-hybrid-output/85-RESEARCH.md
@~/.claude/get-shit-done/workflows/linear.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renumber existing Steps 5-8 to Steps 6-9</name>
  <files>~/.claude/get-shit-done/workflows/linear.md</files>
  <action>
Renumber all existing steps after Step 4 in linear.md:

1. Change "**Step 5: Write linear-context.md**" to "**Step 6: Write linear-context.md**"
2. Change "**Step 6: Execute route**" to "**Step 7: Execute route**"
3. Change "**Step 7: Comment-back to Linear issues**" to "**Step 8: Comment-back to Linear issues**"
4. Change "**Step 8: Cleanup**" to "**Step 9: Cleanup**"

Update ALL sub-step references within the renamed steps:
- "6a" through "6i" become "7a" through "7i" (in Step 7: Execute route)
- Any cross-references to step numbers in comments, display text, or documentation within the file

Update the success criteria section: change "Workflow steps renumbered to 8 steps (WKFL-01)" to "Workflow steps renumbered to 9 steps (WKFL-01)"

Update the purpose section at the top of the file if it mentions step counts.

Search for any remaining references to old step numbers (e.g., "Step 5" referring to writing linear-context.md, "Step 6" referring to executing) and update them.
  </action>
  <verify>
Read linear.md and confirm:
- Steps are numbered 1 through 9
- No references to old step numbers remain (search for "Step 5:" followed by "Write", "Step 6:" followed by "Execute", "Step 7:" followed by "Comment", "Step 8:" followed by "Cleanup")
- Sub-steps 7a-7i exist (previously 6a-6i)
  </verify>
  <done>All steps correctly numbered 1-9, all internal references updated, no stale step number references remain</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 5 with quick route confirmation summary and re-ask loop</name>
  <files>~/.claude/get-shit-done/workflows/linear.md</files>
  <action>
Insert a new "**Step 5: Hybrid output**" section between Step 4 (Route decision) and Step 6 (Write linear-context.md). This step branches on `$ROUTE`.

For this task, implement ONLY the quick route branch. Leave a placeholder comment for the milestone route (Plan 85-02 will add it).

The quick route branch should contain:

**5a. Display confirmation summary:**

Display a banner with the confirmation summary:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► CONFIRMATION SUMMARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue:    {$ISSUES[0].identifier} — {$ISSUES[0].title}
Goal:     {extracted from $INTERVIEW_CONTEXT **Goal:** line}
Scope:    {extracted from $INTERVIEW_CONTEXT **Scope:** line}
Criteria: {extracted from $INTERVIEW_CONTEXT **Success Criteria:** line}
Route:    {$ROUTE}{$FULL_MODE ? ' (full mode)' : ''}
```

**5b. Ask for confirmation:**

```
AskUserQuestion(
  header: "Confirm",
  question: "Does this look right?",
  options: [
    "Yes, proceed",
    "No, let me clarify"
  ],
  followUp: null
)
```

**If "Yes, proceed":** Continue to Step 6.

**If "No, let me clarify":** Proceed to 5c.

**5c. Dimension picker:**

```
AskUserQuestion(
  header: "Clarify",
  question: "Which part would you like to revisit?",
  options: [
    "Goal",
    "Scope",
    "Success Criteria",
    "Complexity",
    "Cancel — proceed as-is"
  ],
  followUp: null
)
```

**If "Cancel — proceed as-is":** Continue to Step 6.

**Otherwise:** Re-ask the selected dimension's interview question using the same AskUserQuestion format from Step 3 (Q1 for Goal, Q2 for Scope, Q3 for Success Criteria, Q4 for Complexity). After the user answers, update the corresponding line in `$INTERVIEW_CONTEXT` with the new answer. Then return to 5a to re-display the updated confirmation summary with the same Yes/No prompt. This loop continues until the user selects "Yes, proceed" or "Cancel — proceed as-is".

**Milestone route placeholder:**

```
**If `$ROUTE == "milestone"`:**

(Milestone approach proposals — added by Plan 85-02)

Proceed to Step 6.
```

Add a horizontal rule separator (`---`) after Step 5 to match the existing step separator pattern.

Update the success criteria at the bottom of linear.md to add:
- Quick route displays confirmation summary (issue, goal, scope, criteria, route) with "Yes, proceed" / "No, let me clarify" (OUTP-01)
- "No, let me clarify" re-enters relevant interview question via dimension picker (OUTP-02)
  </action>
  <verify>
Read linear.md and confirm:
- Step 5 exists between Step 4 (Route decision) and Step 6 (Write linear-context.md)
- Step 5 has sub-steps 5a, 5b, 5c for the quick route
- Confirmation summary banner is present with Issue, Goal, Scope, Criteria, Route fields
- AskUserQuestion with "Yes, proceed" / "No, let me clarify" options is present
- Dimension picker AskUserQuestion with Goal, Scope, Success Criteria, Complexity, Cancel options is present
- Re-ask loop logic described (update $INTERVIEW_CONTEXT, return to 5a)
- Milestone route placeholder exists
- Success criteria updated with OUTP-01 and OUTP-02
  </verify>
  <done>Step 5 quick route confirmation summary with re-ask loop is fully specified in linear.md, with milestone placeholder for Plan 85-02</done>
</task>

</tasks>

<verification>
- linear.md has 9 steps numbered sequentially
- Step 5 is titled "Hybrid output" and contains quick route confirmation summary logic
- Quick route displays issue, goal, scope, criteria, route in a banner
- "No, let me clarify" triggers dimension picker, re-asks single question, updates $INTERVIEW_CONTEXT, re-displays summary
- Loop continues until "Yes, proceed" or "Cancel"
- All step references updated (no stale references to old numbering)
- Success criteria include OUTP-01 and OUTP-02
</verification>

<success_criteria>
- Step 5 exists with quick route confirmation summary
- Re-ask loop allows unlimited re-clarification cycles
- Steps are numbered 1-9 with all internal references updated
- No stale step number references remain
</success_criteria>

<output>
After completion, create `.planning/phases/85-hybrid-output/85-01-SUMMARY.md`
</output>
