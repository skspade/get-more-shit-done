---
phase: 85-hybrid-output
plan: 02
type: execute
wave: 2
depends_on: [85-01]
files_modified: [~/.claude/get-shit-done/workflows/linear.md]
autonomous: true
requirements: [OUTP-03, OUTP-04]

must_haves:
  truths:
    - Milestone route displays 2-3 approach proposals with pros/cons and a recommendation
    - User selects approach via AskUserQuestion with approach names plus "Let me suggest modifications"
    - "Let me suggest modifications" asks what to change, revises proposals, and re-presents
    - Selected approach stored in $SELECTED_APPROACH variable
    - $SELECTED_APPROACH is appended to MILESTONE-CONTEXT.md as "## Selected Approach" section during Step 7 milestone path
    - "## Selected Approach" section contains approach name, description, pros, and cons
  artifacts:
    - ~/.claude/get-shit-done/workflows/linear.md
  key_links:
    - Step 5 milestone branch reads $INTERVIEW_CONTEXT and $ISSUES for approach synthesis
    - $SELECTED_APPROACH variable flows from Step 5 to Step 7 (7a milestone path)
    - MILESTONE-CONTEXT.md template in Step 7a extended with ## Selected Approach section
---

<objective>
Add milestone route approach proposals to Step 5 and update the MILESTONE-CONTEXT.md template in Step 7 to include the selected approach.

Purpose: Users see 2-3 implementation approaches for milestone tasks and their selection feeds into the milestone context document.
Output: Updated linear.md with milestone route in Step 5 and extended MILESTONE-CONTEXT.md template in Step 7.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/85-hybrid-output/85-CONTEXT.md
@.planning/phases/85-hybrid-output/85-RESEARCH.md
@.planning/phases/85-hybrid-output/85-01-SUMMARY.md
@~/.claude/get-shit-done/workflows/linear.md
@~/.claude/get-shit-done/workflows/brainstorm.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add milestone route approach proposals to Step 5</name>
  <files>~/.claude/get-shit-done/workflows/linear.md</files>
  <action>
Replace the milestone route placeholder in Step 5 (added by Plan 85-01) with the full milestone approach proposal logic. Follow the brainstorm.md Step 4 pattern exactly.

**If `$ROUTE == "milestone"`:**

**5d. Synthesize approach proposals:**

Based on `$INTERVIEW_CONTEXT` (goal, scope, criteria) and `$ISSUES` (title, description, labels), synthesize 2-3 distinct implementation approaches. Each approach should represent a meaningfully different strategy for achieving the goal.

Display:

```
## Proposed Approaches

### Approach 1: {Name}

{Description — 2-3 sentences explaining the approach and how it addresses the goal}

**Pros:**
- {Advantage}
- {Advantage}

**Cons:**
- {Disadvantage}
- {Disadvantage}

### Approach 2: {Name}

{Description}

**Pros:**
- {Advantage}
- {Advantage}

**Cons:**
- {Disadvantage}
- {Disadvantage}

### Approach 3: {Name} (if warranted by complexity)

{Description}

**Pros:**
- {Advantage}

**Cons:**
- {Disadvantage}

### Recommendation

I recommend **Approach {N}: {Name}** because {reasoning tied to user's stated goal and constraints from interview}.
```

**5e. Ask user to select approach:**

```
AskUserQuestion(
  header: "Approach Selection",
  question: "Which approach would you like to go with?",
  options: [
    "Approach 1: {Name}",
    "Approach 2: {Name}",
    {"Approach 3: {Name}" if 3 approaches presented},
    "Let me suggest modifications"
  ],
  followUp: null
)
```

**If user selects an approach:** Store the selected approach in `$SELECTED_APPROACH`:

```
$SELECTED_APPROACH = "
## Selected Approach

### {Approach Name}

{Description}

**Pros:**
- {pros list}

**Cons:**
- {cons list}
"
```

Display: `✓ Approach selected: {Name}`

Proceed to Step 6.

**If user selects "Let me suggest modifications":**

```
AskUserQuestion(
  header: "Modifications",
  question: "What would you like to change about the proposed approaches?",
  followUp: null
)
```

Incorporate the user's feedback, revise the approach proposals, and return to 5d to re-present the updated proposals. This loop continues until the user selects a specific approach.

Add to the success criteria at the bottom of linear.md:
- Milestone route displays 2-3 approach proposals with pros/cons and recommendation, user selects via AskUserQuestion (OUTP-03)
- Selected approach written to MILESTONE-CONTEXT.md under ## Selected Approach (OUTP-04)
  </action>
  <verify>
Read linear.md Step 5 and confirm:
- Milestone route branch has sub-steps 5d and 5e
- Approach proposal display format matches brainstorm.md Step 4 pattern (approaches with pros/cons, recommendation)
- AskUserQuestion with approach names plus "Let me suggest modifications" is present
- $SELECTED_APPROACH variable is set with approach name, description, pros, cons
- Modification loop returns to 5d for re-presentation
- Success criteria updated with OUTP-03 and OUTP-04
  </verify>
  <done>Step 5 milestone route fully specified with approach proposals, selection, modification loop, and $SELECTED_APPROACH variable</done>
</task>

<task type="auto">
  <name>Task 2: Extend MILESTONE-CONTEXT.md template with Selected Approach section</name>
  <files>~/.claude/get-shit-done/workflows/linear.md</files>
  <action>
In Step 7 (Execute route), find the milestone route sub-step 7a where MILESTONE-CONTEXT.md is built. The current template ends with "## Additional Context". Extend it to append `$SELECTED_APPROACH` after the Additional Context section.

Update the MILESTONE-CONTEXT.md template in sub-step 7a to:

```markdown
# Milestone Context

**Source:** Linear issues (${comma-separated identifiers})
**Fetched:** ${ISO date}

## Features

${For each issue in $ISSUES:}
### ${issue.title}

${issue.description}

${end for each}

## Additional Context

${For each issue with comments:}
### ${issue.identifier} Comments
${For each of first 5 comments:}
- ${comment.body truncated to 300 chars}
${end for each}
${end for each}

${$SELECTED_APPROACH}
```

The `$SELECTED_APPROACH` variable already contains the `## Selected Approach` heading and full content (set in Step 5e). If `$SELECTED_APPROACH` is empty (should not happen for milestone route but as a guard), omit the section.

Initialize `$SELECTED_APPROACH = ""` at the top of Step 5 so it exists for both route branches (quick route does not set it, milestone route does).
  </action>
  <verify>
Read linear.md Step 7 milestone path (sub-step 7a) and confirm:
- MILESTONE-CONTEXT.md template includes ${$SELECTED_APPROACH} after ## Additional Context
- $SELECTED_APPROACH is initialized at the top of Step 5
  </verify>
  <done>MILESTONE-CONTEXT.md template extended with $SELECTED_APPROACH, variable initialized in Step 5</done>
</task>

</tasks>

<verification>
- Step 5 milestone branch displays 2-3 approach proposals with pros/cons and recommendation
- AskUserQuestion offers approach selection plus "Let me suggest modifications"
- Modification loop asks what to change, revises, re-presents
- $SELECTED_APPROACH stored with approach name, description, pros, cons
- MILESTONE-CONTEXT.md template in Step 7a appends $SELECTED_APPROACH
- Success criteria include OUTP-03 and OUTP-04
</verification>

<success_criteria>
- Milestone route displays approach proposals following brainstorm.md Step 4 pattern
- User can select an approach or request modifications
- Selected approach flows to MILESTONE-CONTEXT.md via $SELECTED_APPROACH variable
- MILESTONE-CONTEXT.md includes ## Selected Approach section
</success_criteria>

<output>
After completion, create `.planning/phases/85-hybrid-output/85-02-SUMMARY.md`
</output>
