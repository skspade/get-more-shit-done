---
phase: 66-phase-navigation-and-autopilot-readiness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [get-shit-done/bin/lib/validation.cjs, tests/validation.test.cjs]
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]

must_haves:
  truths:
    - "NAV-01 confirms computePhaseStatus returns valid data for the current project"
    - "NAV-02 detects when findFirstIncompletePhase returns null while milestone is active"
    - "NAV-03 detects incomplete phases with ambiguous lifecycle step (step is null or unexpected value)"
    - "NAV-04 detects orphan phase directories (on disk but not in ROADMAP) and missing directories (in ROADMAP but not on disk)"
    - "NAV checks have category 'navigation'"
    - "NAV-02 severity is warning, NAV-03 severity is error, NAV-04 severity is warning"
  artifacts:
    - get-shit-done/bin/lib/validation.cjs
    - tests/validation.test.cjs
  key_links:
    - "NAV checks import computePhaseStatus, findFirstIncompletePhase, extractPhaseNumbers from phase.cjs"
    - "NAV checks import findPhaseInternal from core.cjs"
    - "NAV-04 strips <details> blocks before comparing ROADMAP phases to disk"
---

<objective>
Implement phase navigation checks (NAV-01 through NAV-04) in validation.cjs using TDD. These checks validate that phase lifecycle truth comes from artifact inspection via computePhaseStatus() rather than regex parsing.

Purpose: Ensure phase status is determined by artifact presence, navigation works, and disk/ROADMAP sync is maintained
Output: Updated validation.cjs with 4 navigation checks + comprehensive tests
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
@/Users/seanspade/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/66-phase-navigation-and-autopilot-readiness/66-CONTEXT.md
@.planning/phases/66-phase-navigation-and-autopilot-readiness/66-RESEARCH.md
@get-shit-done/bin/lib/validation.cjs
@get-shit-done/bin/lib/phase.cjs
@get-shit-done/bin/lib/core.cjs
@tests/validation.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for navigation checks</name>
  <files>tests/validation.test.cjs</files>
  <behavior>
    - NAV-01: When computePhaseStatus returns valid data for a phase with artifacts, check passes with category 'navigation' and severity 'info'
    - NAV-01: When phases/ directory missing, check passes (skipped gracefully)
    - NAV-02: When milestone is active (unchecked phases exist) and findFirstIncompletePhase returns a phase number, check passes with category 'navigation' and severity 'warning'
    - NAV-02: When milestone is active but findFirstIncompletePhase returns null, check fails with severity 'warning'
    - NAV-02: When ROADMAP.md missing, check passes (skipped)
    - NAV-03: When all incomplete phases have a deterministic step (discuss/plan/execute/verify), check passes with severity 'error'
    - NAV-03: When ROADMAP.md or phases/ missing, check passes (skipped)
    - NAV-04: When disk phase directories match ROADMAP phases, check passes with category 'navigation' and severity 'warning'
    - NAV-04: When a directory exists on disk but not in ROADMAP (orphan), check fails with severity 'warning' and message mentions orphan
    - NAV-04: When a ROADMAP phase has no disk directory (missing), check fails with severity 'warning' and message mentions missing
    - NAV-04: When phases/ directory or ROADMAP.md missing, check passes (skipped)
    - NAV-04: Only compares phases in the active milestone (strips <details> blocks)
    - All NAV checks have category 'navigation'
    - runChecks with categories: ['navigation'] returns only NAV checks
  </behavior>
  <action>
Add new describe blocks to tests/validation.test.cjs for navigation checks.

Create test fixtures:
- A minimal ROADMAP.md with active milestone section (NOT inside <details>) containing phase checkboxes
- A phases/ directory with phase subdirectories containing artifacts (CONTEXT.md, PLAN.md, etc.)

For NAV-01 tests:
- Create tmpDir with .planning/phases/01-test/ containing a CONTEXT.md file
- Also create .planning/ROADMAP.md with Phase 1 entry so computePhaseStatus can find it
- Run runChecks with categories: ['navigation'], find NAV-01 result, verify passed: true
- Test with no phases/ directory: verify passed: true (skipped)

For NAV-02 tests:
- Create tmpDir with ROADMAP.md containing unchecked phases and corresponding phase directories
- Verify check passes when incomplete phase exists
- Create tmpDir where all phases are checked [x] in ROADMAP (milestone complete) -- check should still pass (no failure when milestone is done)
- Test missing ROADMAP.md: passes (skipped)

For NAV-03 tests:
- Create tmpDir with ROADMAP.md + phases with clear artifact states (e.g., empty dir = discuss step, dir with CONTEXT.md but no PLAN.md = plan step)
- Verify check passes when all steps are deterministic
- Test missing ROADMAP.md/phases: passes (skipped)

For NAV-04 tests:
- Create tmpDir with ROADMAP.md listing Phase 1 and Phase 2, but only Phase 1 directory on disk -> fails (missing Phase 2)
- Create tmpDir with disk directory Phase 3 not in ROADMAP -> fails (orphan)
- Create tmpDir with matching phases -> passes
- Create tmpDir with shipped milestone phases inside <details> blocks -- these should NOT be compared
- Test missing phases/ or ROADMAP.md: passes (skipped)

All tests should FAIL initially since NAV checks don't exist yet.
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/get-more-shit-done && node --test tests/validation.test.cjs 2>&1 | tail -10</automated>
  </verify>
  <done>All new navigation check tests exist and fail (RED phase).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement navigation checks in validation.cjs</name>
  <files>get-shit-done/bin/lib/validation.cjs</files>
  <action>
Add navigation checks to validation.cjs:

1. Add imports at top:
```javascript
const { computePhaseStatus, findFirstIncompletePhase, extractPhaseNumbers } = require('./phase.cjs');
const { findPhaseInternal } = require('./core.cjs');
```

Note: `getMilestoneInfo` and `findPhaseInternal` are already available from core.cjs (getMilestoneInfo already imported). Add `findPhaseInternal` to the existing destructure.

2. Add NAV-01 check to checks array:
   - id: 'NAV-01', category: 'navigation', severity: 'info'
   - Check if phases/ directory exists; if not, return passed: true (skip)
   - Read phases/ directory, find first phase directory matching PHASE_DIR_REGEX
   - Call findPhaseInternal to get phaseInfo, then computePhaseStatus
   - If result has a valid `step` field, pass; otherwise fail
   - Wrap in try/catch, return passed: true on error

3. Add NAV-02 check:
   - id: 'NAV-02', category: 'navigation', severity: 'warning'
   - Skip if ROADMAP.md missing (return passed: true)
   - Call countRoadmapPhases to check if unchecked phases exist (milestone active)
   - If no unchecked phases, pass (milestone complete, nothing to check)
   - Call findFirstIncompletePhase(cwd)
   - If result is not null, pass; if null, fail with message "milestone active but no incomplete phase found"

4. Add NAV-03 check:
   - id: 'NAV-03', category: 'navigation', severity: 'error'
   - Skip if ROADMAP.md or phases/ missing
   - Read ROADMAP.md, strip <details> blocks, extract phase numbers with extractPhaseNumbers
   - For each phase number, call findPhaseInternal then computePhaseStatus
   - Skip phases that are complete (step === 'complete')
   - Check that each incomplete phase has step in ['discuss', 'plan', 'execute', 'verify']
   - If any has null or unexpected step, fail with message listing the problematic phase
   - If all are valid, pass

5. Add NAV-04 check:
   - id: 'NAV-04', category: 'navigation', severity: 'warning'
   - Skip if phases/ or ROADMAP.md missing
   - Read ROADMAP.md, strip <details> blocks, extract phase numbers with extractPhaseNumbers
   - Read phases/ directory, extract phase numbers from directory names (parse leading digits from PHASE_DIR_REGEX matches)
   - Compare: orphans = on disk but not in ROADMAP; missing = in ROADMAP but not on disk
   - If orphans or missing exist, fail with descriptive message
   - Otherwise pass

Key constraints:
- NO console output, NO ANSI codes
- Synchronous execution only
- Catch all errors internally, return { passed, message }
- Reuse existing countRoadmapPhases helper for <details> stripping pattern
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/get-more-shit-done && node --test tests/validation.test.cjs</automated>
  </verify>
  <done>All navigation check tests pass GREEN. runChecks with categories: ['navigation'] returns NAV-01 through NAV-04.</done>
</task>

</tasks>

<verification>
- `node --test tests/validation.test.cjs` -- all tests pass
- `node -e "const v = require('./get-shit-done/bin/lib/validation.cjs'); const r = v.runChecks('.', { categories: ['navigation'] }); console.log(r.map(c => c.id).join(', '))"` -- shows NAV-01, NAV-02, NAV-03, NAV-04
- `node -e "const v = require('./get-shit-done/bin/lib/validation.cjs'); const r = v.runChecks('.'); const cats = [...new Set(r.map(c => c.category))]; console.log(cats.sort().join(', '))"` -- shows "navigation, state, structure"
</verification>
