---
phase: 60-auto-skip-decision-points
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/new-milestone.md
autonomous: true
requirements: [SKIP-01, SKIP-02, SKIP-03, SKIP-04, SKIP-05, SKIP-06]

must_haves:
  truths:
    - "Step 2 auto-mode branch exists (SKIP-01 -- verify Phase 59 already handles this)"
    - "Step 3 auto-accepts version (minor bump) in auto mode without AskUserQuestion"
    - "Step 8 auto-selects Research first and persists workflow.research true"
    - "Step 9 auto-includes all features, skips per-category scoping, skips gaps question, and auto-approves requirements"
    - "Step 10 auto-approves roadmap when ROADMAP CREATED, but still errors on ROADMAP BLOCKED"
    - "All 6 SKIP requirements are satisfied by auto-mode conditional branches"
  artifacts:
    - path: "get-shit-done/workflows/new-milestone.md"
      provides: "Auto-mode conditional branches at steps 3, 8, 9, and 10"
      contains: "auto mode is active"
  key_links:
    - from: "get-shit-done/workflows/new-milestone.md"
      to: "get-shit-done/workflows/new-milestone.md"
      via: "auto_mode section (Phase 59) provides the boolean consumed by all SKIP conditionals"
      pattern: "auto mode"
---

<objective>
Add auto-mode conditional branches to steps 3, 8, 9, and 10 of `new-milestone.md` so all 6 interactive decision points are bypassed with correct defaults in auto mode.

Purpose: In auto mode, the new-milestone workflow produces a complete milestone (requirements + roadmap) without any human prompts. Phase 59 already added `--auto` flag parsing, context resolution, and the step 2 auto-branch. This plan adds the remaining 5 auto-skip conditionals (SKIP-02 through SKIP-06) and verifies SKIP-01 is already covered.

Output: Updated new-milestone.md with all 6 decision points auto-skipped.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-auto-skip-decision-points/60-CONTEXT.md
@.planning/phases/60-auto-skip-decision-points/60-RESEARCH.md

<interfaces>
<!-- Current new-milestone.md step structure (from Phase 59) -->

Step 2 already has auto mode branch (Phase 59):
```markdown
## 2. Gather Milestone Goals

**If auto mode with resolved context:**
- Use resolved context from `<context_resolution>` as milestone goals
- Skip interactive questioning
- Continue to step 3
```

Step 3 current (no auto branch):
```markdown
## 3. Determine Milestone Version

- Parse last version from MILESTONES.md
- Suggest next version (v1.0 → v1.1, or v2.0 for major)
- Confirm with user
```

Step 8 current (no auto branch):
```markdown
## 8. Research Decision

AskUserQuestion: "Research the domain ecosystem for new features before defining requirements?"
- "Research first (Recommended)" — ...
- "Skip research" — Go straight to requirements

**Persist choice to config** ...
```

Step 9 relevant decision points:
1. Per-category scoping via AskUserQuestion (multiSelect)
2. "Identify gaps" via AskUserQuestion
3. Requirements confirmation "Does this capture what you're building?"

Step 10 relevant decision point:
1. Roadmap approval via AskUserQuestion after ROADMAP CREATED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto-mode branches to steps 3, 8, 9, and 10</name>
  <files>get-shit-done/workflows/new-milestone.md</files>
  <action>
Read `get-shit-done/workflows/new-milestone.md` and make 4 targeted edits:

**Edit 1 -- Step 3 (SKIP-02): Version auto-accept**

Find:
```markdown
## 3. Determine Milestone Version

- Parse last version from MILESTONES.md
```

Replace with:
```markdown
## 3. Determine Milestone Version

**If auto mode is active:**
- Parse last version from MILESTONES.md
- Compute suggested next version (minor bump)
- Display: `Auto: accepting version v[X.Y] (minor bump)`
- Continue to step 4

- Parse last version from MILESTONES.md
```

This adds the auto-mode branch before the existing interactive flow. Auto mode computes and uses the suggested version without the "Confirm with user" AskUserQuestion.

**Edit 2 -- Step 8 (SKIP-03): Research auto-select**

Find:
```markdown
## 8. Research Decision

AskUserQuestion: "Research the domain ecosystem for new features before defining requirements?"
```

Replace with:
```markdown
## 8. Research Decision

**If auto mode is active:**
- Display: `Auto: selecting "Research first"`
- Persist choice to config:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-set workflow.research true
```
- Continue to research spawning (below)

AskUserQuestion: "Research the domain ecosystem for new features before defining requirements?"
```

This adds the auto-mode branch that always selects "Research first" and persists the config.

**Edit 3 -- Step 9 (SKIP-04, SKIP-05): Feature scoping and gap auto-skip**

Find the step 9 section. After the banner and "Read PROJECT.md" line, before "**If research exists:**", add the auto-mode block:

Find:
```markdown
Read PROJECT.md: core value, current milestone goals, validated requirements (what exists).

**If research exists:** Read FEATURES.md, extract feature categories.
```

Replace with:
```markdown
Read PROJECT.md: core value, current milestone goals, validated requirements (what exists).

**If auto mode is active:**
- If research exists: read FEATURES.md, include ALL features (all table stakes + all differentiators)
- If no research: include all features from resolved context
- Skip per-category AskUserQuestion scoping
- Skip "Identify gaps" AskUserQuestion (proceed as "No, research covered it")
- Skip requirements confirmation ("Does this capture what you're building?") -- auto-approve
- Display: `Auto: including all features from research`
- Generate REQUIREMENTS.md with all features included
- Commit requirements and continue to step 10

**If research exists:** Read FEATURES.md, extract feature categories.
```

**Edit 4 -- Step 10 (SKIP-06): Roadmap auto-approve**

Find the roadmap approval section. After the "If `## ROADMAP CREATED`" handler, before the "Ask for approval" AskUserQuestion, add the auto-mode branch:

Find:
```markdown
**Ask for approval** via AskUserQuestion:
```

Replace with:
```markdown
**If auto mode is active AND `## ROADMAP CREATED`:**
- Display: `Auto: approving roadmap`
- Skip approval AskUserQuestion
- Continue to commit roadmap

**If auto mode is active AND `## ROADMAP BLOCKED`:**
- Display error: `Auto mode cannot resolve roadmap blocker. Review the output above.`
- Exit workflow with error

**Ask for approval** via AskUserQuestion:
```

**Verification of SKIP-01:** After all edits, verify that step 2 already contains the auto-mode branch from Phase 59 ("If auto mode with resolved context"). If present, SKIP-01 is satisfied with no changes needed.
  </action>
  <verify>
    <automated>grep -c "auto mode" get-shit-done/workflows/new-milestone.md</automated>
  </verify>
  <done>
All 6 decision points have auto-mode branches:
- SKIP-01: Step 2 uses resolved context (Phase 59, verified present)
- SKIP-02: Step 3 auto-accepts minor bump version
- SKIP-03: Step 8 auto-selects "Research first" and persists config
- SKIP-04: Step 9 auto-includes all features from research
- SKIP-05: Step 9 auto-skips gap identification
- SKIP-06: Step 10 auto-approves roadmap (but errors on ROADMAP BLOCKED)
  </done>
</task>

</tasks>

<verification>
1. `grep -c "auto mode" get-shit-done/workflows/new-milestone.md` returns 8+ (auto_mode section + context_resolution + 6 step branches)
2. `grep "Auto: accepting version" get-shit-done/workflows/new-milestone.md` -- SKIP-02 present
3. `grep "Auto: selecting" get-shit-done/workflows/new-milestone.md` -- SKIP-03 present
4. `grep "Auto: including all features" get-shit-done/workflows/new-milestone.md` -- SKIP-04/05 present
5. `grep "Auto: approving roadmap" get-shit-done/workflows/new-milestone.md` -- SKIP-06 present
6. `grep "auto mode with resolved context" get-shit-done/workflows/new-milestone.md` -- SKIP-01 present (from Phase 59)
7. `grep "cannot resolve roadmap blocker" get-shit-done/workflows/new-milestone.md` -- ROADMAP BLOCKED error path present
8. Existing interactive paths remain unchanged (non-auto mode still prompts at all 6 points)
</verification>

<success_criteria>
- All 6 SKIP requirements (SKIP-01 through SKIP-06) have auto-mode conditional branches
- Auto mode bypasses all AskUserQuestion calls in steps 2, 3, 8, 9, and 10
- Each auto-skip displays a brief progress message (Auto: ...)
- SKIP-03 persists `workflow.research true` to config
- SKIP-06 errors on ROADMAP BLOCKED instead of auto-approving
- Interactive mode paths are completely unchanged
- No new config keys introduced
</success_criteria>

<output>
After completion, create `.planning/phases/60-auto-skip-decision-points/60-01-SUMMARY.md`
</output>
