---
phase: 68-testing-and-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [tests/cli.test.cjs]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-04]

must_haves:
  truths:
    - Total test count is at or below 750 after this plan completes
    - handleHealth describe block retains only 2 smoke tests for CLI adapter verification
    - 15 redundant handleHealth tests removed from cli.test.cjs
    - Remaining smoke tests verify output format mapping (rich mode ANSI, JSON mode structured fields)
  artifacts:
    - tests/cli.test.cjs (modified — reduced handleHealth tests)
  key_links:
    - validation.test.cjs already covers all check categories (TEST-01 satisfied)
    - validation.test.cjs already covers auto-repair (TEST-02 satisfied)
    - Removing 15 tests and plan 01 adding 3 gives net 766 - 15 + 3 = 754 (under 750 budget... wait need to verify)
---

<objective>
Remove redundant handleHealth tests from cli.test.cjs that are now covered by validation.test.cjs and verify-health.test.cjs. Keep 2 smoke tests for the CLI adapter layer.

Purpose: Satisfy TEST-04 requirement — total test count at or below 750 (pre-milestone budget). Also documents that TEST-01 and TEST-02 are already satisfied by existing validation.test.cjs tests.
Output: cli.test.cjs with 15 fewer tests, total project count at or below 750
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-testing-and-consolidation/68-CONTEXT.md
@.planning/phases/68-testing-and-consolidation/68-RESEARCH.md
@tests/cli.test.cjs
@tests/validation.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant handleHealth tests from cli.test.cjs</name>
  <files>tests/cli.test.cjs</files>
  <action>
In `tests/cli.test.cjs`, find the `handleHealth` describe block (starts around line 535).

**Remove these tests** (they are covered by validation.test.cjs):
1. `healthy state when all required files present (HLTH-01)` — covered by validation.test.cjs STRUCT checks
2. `broken status when PROJECT.md missing (HLTH-01)` — covered by validation.test.cjs STRUCT-01b
3. `broken status when ROADMAP.md missing (HLTH-01)` — covered by validation.test.cjs STRUCT-01c
4. `broken status when STATE.md missing (HLTH-01)` — covered by validation.test.cjs STRUCT-01d
5. `degraded status when config.json missing (HLTH-01)` — covered by validation.test.cjs STRUCT-01e
6. `phases/ directory check shows failed when missing (HLTH-01)` — covered by validation.test.cjs STRUCT-01f
7. `broken status when .planning/ entirely missing (HLTH-01)` — covered by validation.test.cjs STRUCT-01a
8. `no config errors with valid config.json (HLTH-02)` — covered by validation.test.cjs STRUCT-02
9. `error E005 when config.json has invalid JSON (HLTH-02)` — covered by validation.test.cjs STRUCT-02
10. `warning W004 when model_profile invalid (HLTH-02)` — covered by validation.test.cjs STRUCT-02
11. `no consistency warnings when state matches roadmap (HLTH-03)` — covered by validation.test.cjs STATE checks
12. `warnings when STATE.md has stale phase counts (HLTH-03)` — covered by validation.test.cjs STATE-02/03
13. `each issue has code, message, and fix fields (HLTH-04)` — covered by validation.test.cjs result shape
14. `health returns structured data with status and checks (HLTH-04)` — covered by validation.test.cjs result shape
15. `plain mode message contains no ANSI escape sequences` — weakest of the output mode tests (tests rich mode first, then strips ANSI — already partially covered by the rich mode test)

**Keep these 2 tests** as CLI adapter smoke tests:
1. `rich mode returns message with ANSI codes` — verifies the CLI adapter produces ANSI-formatted output
2. `JSON mode returns all structured fields` — verifies the CLI adapter maps ValidationResult to the legacy output shape with command, status, checks, errors, warnings, info fields

**Keep the describe block, beforeEach, and afterEach** — the 2 remaining tests need them.

After removing the 15 tests, verify the describe block structure is clean: `describe('handleHealth', () => { beforeEach..., afterEach..., test('rich mode...'), test('JSON mode...') })`.

Remove any comments that referenced the removed tests (e.g., `// HLTH-01: File existence checks`, `// HLTH-02: Config validation`, etc.) that no longer have associated tests beneath them. Keep `// Output modes` comment if the 2 remaining tests are under it.
  </action>
  <verify>
    <automated>node --test tests/cli.test.cjs 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
  </verify>
  <done>
    - cli.test.cjs handleHealth describe block has exactly 2 tests (rich mode, JSON mode)
    - All remaining cli.test.cjs tests pass
    - 15 tests removed from the file
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify total test count is at or below 750</name>
  <files>tests/cli.test.cjs</files>
  <action>
Run the full test suite and check the total count:
```bash
node --test 2>&1 | grep "^# tests"
```

Expected: 766 - 15 = 751. If plan 01 has already run (adding 3 tests), the count would be 754.

**If the count is above 750:**
- Review the handleHealth describe block for any additional tests that can be removed
- Check if the `Output modes` comment section has any tests beyond the 2 we're keeping
- Consider removing one more test if needed (the JSON mode test is slightly more redundant than the rich mode test, but both are useful as smoke tests)

**If the count exceeds 750 by more than 5 tests:** There may be tests from other files that were added during Phases 64-67 beyond what was tracked. Scan for recently-added tests:
```bash
git log --oneline --since="2026-03-15" -- tests/
```
And identify if any duplicative tests were added that can be consolidated.

The goal is total count <= 750. Document the final count.
  </action>
  <verify>
    <automated>node --test 2>&1 | grep "^# tests"</automated>
  </verify>
  <done>
    - `node --test` reports total tests <= 750
    - No test regressions (only the pre-existing roadmap.test.cjs failure remains)
  </done>
</task>

</tasks>

<verification>
- `node --test` total test count is at or below 750
- `node --test tests/cli.test.cjs` passes — remaining 2 handleHealth smoke tests work correctly
- `grep -c "test(" tests/cli.test.cjs` shows reduced count (87 - 15 = 72)
- All other test files unaffected
</verification>
