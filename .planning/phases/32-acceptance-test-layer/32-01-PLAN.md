---
phase: 32-acceptance-test-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/templates/context.md
  - get-shit-done/workflows/discuss-phase.md
autonomous: true
requirements: [AT-01, AT-02]

must_haves:
  truths:
    - "context.md template contains an <acceptance_tests> XML section with AT-{NN} format example"
    - "discuss-phase.md contains a gather_acceptance_tests step between discuss_areas and write_context"
    - "The gather_acceptance_tests step checks test.acceptance_tests config before running"
    - "The gather_acceptance_tests step uses AskUserQuestion to present requirements and gather ATs"
    - "The write_context step includes <acceptance_tests> block when ATs were gathered"
    - "The gather_acceptance_tests step is skipped during --auto mode"
  artifacts:
    - path: "get-shit-done/templates/context.md"
      provides: "CONTEXT.md template with acceptance_tests section"
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "discuss-phase workflow with AT gathering step"
  key_links:
    - from: "discuss-phase.md gather_acceptance_tests step"
      to: "gsd-tools.cjs config-get test.acceptance_tests"
      via: "config check determines whether to show AT prompts"
    - from: "discuss-phase.md write_context step"
      to: "context.md template"
      via: "write_context uses template structure including acceptance_tests section"
---

<objective>
Add acceptance test gathering to the discuss-phase workflow and update the CONTEXT.md template.

Purpose: Enable humans to define executable acceptance criteria during discuss-phase in Given/When/Then/Verify format. These acceptance tests become the human's contract that the AI works against throughout the phase lifecycle.

Output: Updated context.md template with `<acceptance_tests>` section; updated discuss-phase.md with `gather_acceptance_tests` step and updated `write_context` step.
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/32-acceptance-test-layer/32-CONTEXT.md
@.planning/phases/32-acceptance-test-layer/32-RESEARCH.md
@get-shit-done/templates/context.md
@get-shit-done/workflows/discuss-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add acceptance_tests section to CONTEXT.md template</name>
  <files>get-shit-done/templates/context.md</files>
  <action>
Add an `<acceptance_tests>` XML section to the CONTEXT.md template. Insert it AFTER the `<code_context>` section and BEFORE the `<deferred>` section.

The new section in the template should be:

```markdown
<acceptance_tests>
## Acceptance Tests

[Human-defined acceptance tests in Given/When/Then/Verify format. Each test has an AT-{NN} identifier that persists through the phase lifecycle. Only present when gathered during discuss-phase (interactive mode with test.acceptance_tests enabled).]

### AT-01: [Description of what is being tested]
- Given: [precondition or setup state]
- When: [action or trigger]
- Then: [expected observable outcome]
- Verify: `[shell command that exits 0 on pass, non-zero on fail]`

### AT-02: [Description]
- Given: [precondition]
- When: [action]
- Then: [expected outcome]
- Verify: `[shell command]`

[If no acceptance tests were gathered: section omitted entirely]

</acceptance_tests>
```

Also update the template's guidelines section to mention acceptance_tests. Add a line to the existing downstream consumers list noting that `gsd-verifier` executes the Verify commands during verify-phase.

In the good_examples section, do NOT add examples with acceptance tests -- the examples are about decisions, not testing. The template documentation is sufficient.
  </action>
  <verify>
    <automated>grep -c "acceptance_tests" get-shit-done/templates/context.md</automated>
  </verify>
  <done>context.md template contains an `<acceptance_tests>` XML section with AT-{NN} format example between code_context and deferred sections, with documentation about the section's purpose</done>
</task>

<task type="auto">
  <name>Task 2: Add gather_acceptance_tests step to discuss-phase.md</name>
  <files>get-shit-done/workflows/discuss-phase.md</files>
  <action>
Add a new `<step name="gather_acceptance_tests">` to discuss-phase.md. Insert it AFTER the `</step>` closing tag of `discuss_areas` (after the "Track deferred ideas internally." line, approximately line 518) and BEFORE the `<step name="write_context">`.

The new step:

```xml
<step name="gather_acceptance_tests">
Check if acceptance tests should be gathered:

1. **If `--auto` flag present:** Skip this step entirely (acceptance tests are interactive-only for v1.6).

2. **Check config:**
```bash
AT_ENABLED=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-get test.acceptance_tests 2>/dev/null || echo "true")
```

**If `AT_ENABLED` is false:** Skip this step silently. No acceptance test prompts appear.

**If `AT_ENABLED` is true (default):**

3. **Present requirements for AT gathering:**

Extract the phase's requirement IDs from ROADMAP.md. For each requirement, present to user:

```
Now let's define acceptance tests -- observable behaviors that prove this phase works.

Phase requirements:
- {REQ-ID}: {description}
- {REQ-ID}: {description}
```

4. **For each requirement, gather an AT:**

Use AskUserQuestion:
- header: "Accept test"
- question: "What observable behavior proves {REQ-ID} works? Describe the Given/When/Then, and I'll suggest a Verify command."
- options: "Describe behavior" / "Skip this requirement" / "Done with ATs"

For each user response:
a. Parse the user's description into Given/When/Then format
b. Suggest a Verify command (a shell command that exits 0 on success)
c. Present the formatted AT for approval:
   ```
   ### AT-{NN}: {title derived from description}
   - Given: {precondition}
   - When: {action}
   - Then: {expected outcome}
   - Verify: `{suggested shell command}`
   ```
d. Use AskUserQuestion to confirm:
   - header: "Confirm AT"
   - question: "Accept this test, or modify?"
   - options: "Accept" / "Modify Verify command" / "Rewrite"

5. **After all requirements processed or user says "Done with ATs":**

Present the full acceptance test list:
```
## Acceptance Tests Summary

{N} acceptance test(s) defined:

### AT-01: {title}
- Given: ...
- When: ...
- Then: ...
- Verify: `...`

[repeat for each AT]
```

Use AskUserQuestion:
- header: "AT review"
- question: "Finalize these acceptance tests?"
- options: "Approve all" / "Add more" / "Remove one"

6. **Store the gathered ATs internally** for use in `write_context` step.

If zero ATs were defined (user skipped all or said "Done" immediately), the `<acceptance_tests>` section is omitted from CONTEXT.md entirely.
</step>
```

Also update the `<step name="write_context">` to include the `<acceptance_tests>` block in the CONTEXT.md output template. Add it between `</code_context>` and `<deferred>`:

```markdown
<acceptance_tests>
## Acceptance Tests

{For each gathered AT:}
### AT-{NN}: {title}
- Given: {precondition}
- When: {action}
- Then: {expected outcome}
- Verify: `{shell command}`

</acceptance_tests>
```

Add a comment in write_context: "Include `<acceptance_tests>` section only if ATs were gathered in gather_acceptance_tests step. Omit entirely if no ATs defined or if acceptance tests were skipped."
  </action>
  <verify>
    <automated>grep -c "gather_acceptance_tests" get-shit-done/workflows/discuss-phase.md</automated>
  </verify>
  <done>discuss-phase.md contains a gather_acceptance_tests step between discuss_areas and write_context that: checks --auto flag (skips if present), checks test.acceptance_tests config (skips if false), prompts user for ATs per requirement in Given/When/Then/Verify format, suggests Verify commands, presents for approval. write_context step includes acceptance_tests block when ATs gathered.</done>
</task>

</tasks>

<verification>
1. `grep "acceptance_tests" get-shit-done/templates/context.md` returns matches showing the new section
2. `grep "gather_acceptance_tests" get-shit-done/workflows/discuss-phase.md` returns matches showing the new step
3. The gather_acceptance_tests step appears between discuss_areas and write_context in the workflow
4. The step checks `--auto` flag and `test.acceptance_tests` config before proceeding
5. The write_context step includes the `<acceptance_tests>` block in its template
</verification>

<success_criteria>
- context.md template has `<acceptance_tests>` section with AT format example
- discuss-phase.md has gather_acceptance_tests step in correct position
- Step checks --auto flag (skips for autopilot)
- Step checks test.acceptance_tests config (skips when disabled)
- Step uses AskUserQuestion for interactive AT gathering
- write_context includes acceptance_tests block when ATs gathered
- AT format uses ### AT-{NN}: title with Given/When/Then/Verify lines
</success_criteria>

<output>
After completion, create `.planning/phases/32-acceptance-test-layer/32-01-SUMMARY.md`
</output>
