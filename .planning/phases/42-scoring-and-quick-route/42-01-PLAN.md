---
phase: 42
plan: 1
title: Scoring and Route Decision
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/pr-review.md
autonomous: true
requirements:
  - RTE-01
  - RTE-02
  - RTE-03
must_haves:
  truths:
    - "Scoring heuristic computes +2 per critical, +1 per important, +0 per suggestion, +1 per 5 distinct files"
    - "Score >= 5 routes to milestone, < 5 routes to quick"
    - "--quick flag forces quick route bypassing scoring"
    - "--milestone flag forces milestone route bypassing scoring"
    - "review-context.md is updated with computed route and score after scoring"
    - "Scoring displays routing decision with score or flag override notation"
  artifacts:
    - "get-shit-done/workflows/pr-review.md contains Step 7 scoring and routing logic"
  key_links:
    - "Step 7 reads $GROUPS, $FORCE_QUICK, $FORCE_MILESTONE from earlier steps"
    - "Step 7 updates .planning/review-context.md with route and score values"
---

# Plan 42-01: Scoring and Route Decision

## Objective

Add Step 7 (Score and Route) to the pr-review workflow. This step computes a complexity score from the parsed findings groups, applies flag overrides, determines the routing decision (quick vs milestone), updates review-context.md with the computed values, and displays the decision.

## Tasks

<task id="1">
<title>Add Step 7: Score and Route to pr-review.md</title>
<files>
- get-shit-done/workflows/pr-review.md
</files>
<action>
Replace the placeholder "Steps 7-11: Scoring, Routing, and Cleanup" section in pr-review.md with a fully implemented Step 7 and a remaining placeholder for Steps 8-11.

Step 7 implementation:

**7a. Check flag overrides first:**

```
**If `$FORCE_QUICK`:**
- Set `$ROUTE = "quick"`
- Set `$REVIEW_SCORE = "override"`
- Display: "Route: QUICK (flag override)"
- Skip scoring, proceed to Step 8.

**If `$FORCE_MILESTONE`:**
- Set `$ROUTE = "milestone"`
- Set `$REVIEW_SCORE = "override"`
- Display: "Route: MILESTONE (flag override)"
- Skip scoring, proceed to Step 8.
```

**7b. Compute score from $GROUPS:**

Start `$REVIEW_SCORE` at 0. Iterate all findings across all groups:
- For each finding with severity "critical": +2
- For each finding with severity "important": +1
- For each finding with severity "suggestion": +0

Then count distinct files across all groups. Add +1 per 5 distinct files (integer division: `Math.floor(distinct_files / 5)`).

Minimum score is 0 (cannot go negative, though with this formula it never would).

**7c. Route decision:**
- If `$REVIEW_SCORE >= 5`: set `$ROUTE = "milestone"`
- If `$REVIEW_SCORE < 5`: set `$ROUTE = "quick"`

Display: `Routing: {$ROUTE} (score: {$REVIEW_SCORE})`

**7d. Update review-context.md:**

Read `.planning/review-context.md`. Update the YAML frontmatter:
- Set `route:` to `$ROUTE` (replacing the empty placeholder)
- Set `score:` to `$REVIEW_SCORE` (replacing the empty placeholder)

Write the updated file back.

**After Step 7, add placeholder:**

```markdown
**Steps 8-11: Quick Route, Milestone Route, and Cleanup**

(Implemented in Phases 42-43)
```
</action>
<verify>
Read get-shit-done/workflows/pr-review.md and confirm:
1. Step 7 exists with sections for flag override check, score computation, route decision, and review-context.md update
2. Scoring formula matches: +2 critical, +1 important, +0 suggestion, +1 per 5 files
3. Threshold is score >= 5 for milestone, < 5 for quick
4. $FORCE_QUICK and $FORCE_MILESTONE bypass scoring entirely
5. review-context.md frontmatter is updated with route and score
6. Steps 8-11 placeholder remains for subsequent plans/phases
</verify>
<done>Step 7 scoring and routing logic is fully specified in pr-review.md workflow</done>
</task>

## Verification Criteria

- [ ] pr-review.md Step 7 implements the scoring heuristic exactly as specified
- [ ] Flag overrides ($FORCE_QUICK, $FORCE_MILESTONE) bypass scoring
- [ ] Route threshold: >= 5 milestone, < 5 quick
- [ ] review-context.md is updated with route and score after scoring
- [ ] Display shows routing decision with either score or "flag override" notation
- [ ] Steps 8-11 placeholder preserved for Plan 02 and Phase 43
