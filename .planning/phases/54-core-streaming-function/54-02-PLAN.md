---
phase: 54-core-streaming-function
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [tests/autopilot.test.cjs]
autonomous: true
requirements: [STREAM-02, STREAM-03, STALL-01, STALL-03, CLI-01, CLI-05]

must_haves:
  truths:
    - "Static analysis tests verify runClaudeStreaming and displayStreamEvent exist in autopilot.mjs"
    - "Static analysis tests verify the --quiet flag is in knownFlags"
    - "Static analysis tests verify stall timer configuration uses getConfig"
    - "Existing stdin redirect regression test is updated for the new invocation count"
    - "All tests pass with node --test"
  artifacts:
    - path: "tests/autopilot.test.cjs"
      provides: "Static analysis tests for streaming functions"
      contains: "runClaudeStreaming"
  key_links:
    - from: "tests/autopilot.test.cjs"
      to: "get-shit-done/scripts/autopilot.mjs"
      via: "static source analysis"
      pattern: "readFileSync.*AUTOPILOT_PATH"
---

<objective>
Add static analysis tests for the new streaming functions and update the existing stdin redirect regression test to account for the new `claude -p` invocation count.

Purpose: Ensure the streaming function contract is verified by tests: function existence, quiet flag recognition, stall timer config, stdin redirect preservation, and event display structure.
Output: Updated test file with new test describe block and adjusted invocation count.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-core-streaming-function/54-01-SUMMARY.md
@tests/autopilot.test.cjs
@get-shit-done/scripts/autopilot.mjs

<interfaces>
<!-- Key patterns from existing test file -->

From tests/autopilot.test.cjs:
```javascript
// Test framework (node:test)
const { test, describe, beforeEach, afterEach } = require('node:test');
const assert = require('node:assert');
const fs = require('fs');

// Source loading pattern for static analysis
const AUTOPILOT_PATH = path.join(__dirname, '..', 'get-shit-done', 'scripts', 'autopilot.mjs');
const source = fs.readFileSync(AUTOPILOT_PATH, 'utf-8');

// Existing test: expects exactly 5 claude -p shell invocations (line 127)
// After Plan 01 adds runClaudeStreaming with 2 new invocations, this count becomes 7
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update invocation count and add streaming function static analysis tests</name>
  <files>tests/autopilot.test.cjs</files>
  <action>
Make the following changes to tests/autopilot.test.cjs:

1. **Update the invocation count** in the existing test at line 127:
   Change `5` to `7` in the assertion `assert.strictEqual(shellInvocationLines.length, 5, ...)` and update the error message accordingly. Plan 01 adds 2 new `claude -p` shell invocations (one in the quiet code path, one in the streaming code path of `runClaudeStreaming`).

2. **Add a new describe block** after the existing "argument validation" describe block (at the end of the file). This block performs static analysis of the source to verify the streaming function contract:

```javascript
// --- Static Analysis: streaming functions (Phase 54) -------------------------

describe('autopilot.mjs streaming functions (static analysis)', () => {
  const source = fs.readFileSync(AUTOPILOT_PATH, 'utf-8');

  test('runClaudeStreaming function exists', () => {
    assert.ok(
      source.includes('function runClaudeStreaming'),
      'autopilot.mjs should contain runClaudeStreaming function'
    );
  });

  test('displayStreamEvent function exists', () => {
    assert.ok(
      source.includes('function displayStreamEvent'),
      'autopilot.mjs should contain displayStreamEvent function'
    );
  });

  test('--quiet flag is in knownFlags', () => {
    // Extract the knownFlags line and verify quiet is listed
    const knownFlagsLine = source.split('\n').find(l => l.includes('knownFlags') && l.includes('new Set'));
    assert.ok(knownFlagsLine, 'knownFlags Set declaration should exist');
    assert.ok(
      knownFlagsLine.includes('quiet'),
      `knownFlags should include 'quiet', got: ${knownFlagsLine.trim()}`
    );
  });

  test('QUIET constant is defined', () => {
    assert.ok(
      source.includes('const QUIET'),
      'autopilot.mjs should define QUIET constant'
    );
  });

  test('stall timer uses getConfig for timeout', () => {
    assert.ok(
      source.includes('stall_timeout_ms'),
      'autopilot.mjs should reference stall_timeout_ms config key'
    );
  });

  test('streaming path uses --output-format stream-json', () => {
    assert.ok(
      source.includes('stream-json'),
      'autopilot.mjs should use --output-format stream-json'
    );
  });

  test('displayStreamEvent handles assistant events', () => {
    assert.ok(
      source.includes("event.type === 'assistant'") || source.includes('event.type === "assistant"'),
      'displayStreamEvent should handle assistant event type'
    );
  });

  test('displayStreamEvent handles tool_use events', () => {
    assert.ok(
      source.includes("event.type === 'tool_use'") || source.includes('event.type === "tool_use"'),
      'displayStreamEvent should handle tool_use event type'
    );
  });

  test('stall timer uses unref to prevent blocking exit', () => {
    assert.ok(
      source.includes('.unref()'),
      'Stall timer should call .unref() to prevent blocking process exit'
    );
  });

  test('runClaudeStreaming returns exitCode and stdout', () => {
    // Check both code paths return the expected shape
    const returnPattern = /return\s*\{\s*exitCode.*stdout|return\s*\{\s*stdout.*exitCode/;
    assert.ok(
      returnPattern.test(source),
      'runClaudeStreaming should return { exitCode, stdout }'
    );
  });
});
```
  </action>
  <verify>
    <automated>node --test tests/autopilot.test.cjs 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Existing invocation count test updated from 5 to 7
    - New "streaming functions (static analysis)" describe block added
    - All static analysis tests pass verifying: function existence, quiet flag, QUIET constant, stall timer config, stream-json format, event type handling, unref usage, return shape
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
- `node --test tests/autopilot.test.cjs` passes all tests (0 failures)
- New streaming function tests appear in output
- Existing stdin redirect regression tests pass with updated count
</verification>

<success_criteria>
- All 10+ new static analysis tests pass
- Existing tests pass with updated invocation count (7 instead of 5)
- No test failures
</success_criteria>

<output>
After completion, create `.planning/phases/54-core-streaming-function/54-02-SUMMARY.md`
</output>
