---
phase: 08-remove-git-tagging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/complete-milestone.md
  - commands/gsd/complete-milestone.md
autonomous: true
requirements: [WF-01, WF-02, WF-03]

must_haves:
  truths:
    - "The complete-milestone workflow contains no git_tag step"
    - "The complete-milestone workflow does not create annotated tags"
    - "The complete-milestone workflow does not prompt to push tags"
    - "The complete-milestone command spec does not reference git tagging"
    - "References to git_tag in handle_branches step point to git_commit_milestone"
  artifacts:
    - path: "get-shit-done/workflows/complete-milestone.md"
      provides: "Milestone completion workflow without git tagging"
      contains: "step name=\"git_commit_milestone\""
    - path: "commands/gsd/complete-milestone.md"
      provides: "Command spec without git tag references"
  key_links:
    - from: "handle_branches step"
      to: "git_commit_milestone step"
      via: "Skip to git_commit_milestone"
      pattern: "Skip to git_commit_milestone"
---

<objective>
Remove all git tag creation and push logic from the complete-milestone workflow and its command spec.

Purpose: The complete-milestone workflow should archive milestones without creating or pushing git tags, as this fork does not use automated git tagging.
Output: Updated complete-milestone.md workflow (git_tag step removed, references updated) and updated command spec (tag references removed).
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-remove-git-tagging/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove git_tag step and update references in complete-milestone.md</name>
  <files>get-shit-done/workflows/complete-milestone.md</files>
  <action>
  Make these specific edits to `get-shit-done/workflows/complete-milestone.md`:

  1. **Update purpose line (line 3):** Remove "and tags the release in git" from the purpose description. The purpose should describe archiving and milestone completion without mentioning git tagging.

  2. **Update handle_branches references (lines 536, 552):** Change both occurrences of "Skip to git_tag" to "Skip to git_commit_milestone" since git_commit_milestone is the step that follows git_tag in the current sequence.

  3. **Remove entire git_tag step (lines 646-672):** Delete the complete `<step name="git_tag">` block including:
     - The opening `<step name="git_tag">` tag
     - The "Create git tag:" header and `git tag -a` command block
     - The "Confirm: Tagged" line
     - The "Ask: Push tag to remote?" prompt
     - The conditional push logic and `git push origin` command
     - The closing `</step>` tag

  4. **Remove git tag from success criteria (line 754):** Delete the line `- [ ] Git tag created (v[X.Y])` from the success criteria checklist.

  Do NOT modify any other lines or steps. Preserve all whitespace and formatting in untouched sections.
  </action>
  <verify>
    <automated>! grep -q "git_tag\|git tag\|Push tag to remote" get-shit-done/workflows/complete-milestone.md && echo "PASS: No git tag references found" || echo "FAIL: git tag references still present"</automated>
  </verify>
  <done>The complete-milestone workflow has no git_tag step, no tag creation commands, no tag push prompts, and handle_branches references point to git_commit_milestone instead of git_tag</done>
</task>

<task type="auto">
  <name>Task 2: Remove git tag references from complete-milestone command spec</name>
  <files>commands/gsd/complete-milestone.md</files>
  <action>
  Make these specific edits to `commands/gsd/complete-milestone.md`:

  1. **Update Output line (line 16):** Change `Output: Milestone archived (roadmap + requirements), PROJECT.md evolved, git tagged.` to `Output: Milestone archived (roadmap + requirements), PROJECT.md evolved.` -- remove the trailing "git tagged." clause.

  2. **Update step 7 (lines 103-108):** The current step 7 is "Commit and tag:" with items including tagging and push. Update it to be "Commit:" only:
     - Change the heading from `7. **Commit and tag:**` to `7. **Commit:**`
     - Remove line 107: `- Tag: \`git tag -a v{{version}} -m "[milestone summary]"\``
     - Remove line 108: `- Ask about pushing tag`
     - Keep the stage/commit lines as-is

  3. **Remove from success criteria (line 122):** Delete the line `- Git tag v{{version}} created`

  Do NOT modify any other sections. Preserve all formatting in untouched areas.
  </action>
  <verify>
    <automated>! grep -q "git tag\|git tagged\|pushing tag" commands/gsd/complete-milestone.md && echo "PASS: No git tag references found" || echo "FAIL: git tag references still present"</automated>
  </verify>
  <done>The command spec no longer references git tagging in its objective output, process steps, or success criteria</done>
</task>

</tasks>

<verification>
Run these checks after both tasks complete:
1. `grep -c "git_tag\|git tag\|Push tag\|git tagged" get-shit-done/workflows/complete-milestone.md` returns 0
2. `grep -c "git tag\|git tagged\|pushing tag" commands/gsd/complete-milestone.md` returns 0
3. `grep -c "git_commit_milestone" get-shit-done/workflows/complete-milestone.md` returns at least 3 (the step itself plus 2 updated references)
4. The workflow still contains all other steps (verify_readiness, gather_stats, extract_accomplishments, archive_milestone, archive_requirements, update_project, handle_branches, git_commit_milestone, offer_next)
</verification>

<success_criteria>
- WF-01: The git_tag step is completely removed from complete-milestone.md workflow
- WF-02: The git tag push prompt and logic is removed from complete-milestone.md workflow
- WF-03: The complete-milestone command spec makes no mention of git tagging
- handle_branches "Skip to" references now point to git_commit_milestone
- Purpose line no longer mentions tagging
- Success criteria checklists no longer include git tag items
</success_criteria>

<output>
After completion, create `.planning/phases/08-remove-git-tagging/08-01-SUMMARY.md`
</output>
