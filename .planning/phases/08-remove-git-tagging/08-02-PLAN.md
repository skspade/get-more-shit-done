---
phase: 08-remove-git-tagging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/help.md
  - README.md
  - docs/USER-GUIDE.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "help.md does not mention git tags for complete-milestone"
    - "README.md does not claim complete-milestone tags releases"
    - "USER-GUIDE.md does not claim complete-milestone tags releases"
  artifacts:
    - path: "get-shit-done/workflows/help.md"
      provides: "Help text without git tag references for complete-milestone"
    - path: "README.md"
      provides: "README without git tag references for complete-milestone"
    - path: "docs/USER-GUIDE.md"
      provides: "User guide without git tag references for complete-milestone"
  key_links: []
---

<objective>
Remove all documentation references to automated git tagging during milestone completion.

Purpose: User-facing documentation should not claim that complete-milestone creates or pushes git tags, since that functionality is being removed.
Output: Updated help.md, README.md, and USER-GUIDE.md with git tag references removed from complete-milestone descriptions.
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-remove-git-tagging/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove git tag reference from help.md</name>
  <files>get-shit-done/workflows/help.md</files>
  <action>
  Edit `get-shit-done/workflows/help.md`:

  **Line 182:** Delete the line `- Creates git tag for the release` from the `/gsd:complete-milestone` entry. This is a bullet point within the command's feature list. Remove the entire line, do not leave a blank line in its place.

  Do NOT modify any other lines in the file.
  </action>
  <verify>
    <automated>! grep -q "Creates git tag for the release" get-shit-done/workflows/help.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>help.md no longer mentions git tag creation for complete-milestone</done>
</task>

<task type="auto">
  <name>Task 2: Remove git tag references from README.md and USER-GUIDE.md</name>
  <files>README.md, docs/USER-GUIDE.md</files>
  <action>
  Edit `README.md`:

  1. **Line 349:** Change `When all phases are done, \`/gsd:complete-milestone\` archives the milestone and tags the release.` to `When all phases are done, \`/gsd:complete-milestone\` archives the milestone.` -- remove "and tags the release" from the sentence.

  2. **Line 475:** In the command table, change the description cell from `Archive milestone, tag release` to `Archive milestone` for the `/gsd:complete-milestone` row.

  **Important:** Do NOT modify line 180 which contains `"Bash(git tag:*)"` -- this is a Claude Code permissions example, not a GSD feature claim about automated tagging. Per CONTEXT.md deferred decisions, this line is explicitly out of scope.

  Edit `docs/USER-GUIDE.md`:

  3. **Line 169:** In the command table, change the description cell from `Archive milestone, tag release` to `Archive milestone` for the `/gsd:complete-milestone` row.

  Do NOT modify any other lines in either file.
  </action>
  <verify>
    <automated>! grep -q "tags the release" README.md && ! grep -q "tag release" README.md && ! grep -q "tag release" docs/USER-GUIDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md and USER-GUIDE.md no longer reference git tagging in complete-milestone descriptions. The permissions example on README.md line 180 is preserved.</done>
</task>

</tasks>

<verification>
Run these checks after both tasks complete:
1. `grep -c "Creates git tag for the release" get-shit-done/workflows/help.md` returns 0
2. `grep -c "tags the release" README.md` returns 0
3. `grep -c "tag release" README.md` returns 0
4. `grep -c "tag release" docs/USER-GUIDE.md` returns 0
5. `grep -c "Bash(git tag" README.md` returns 1 (permissions example preserved)
</verification>

<success_criteria>
- DOC-01: help.md no longer mentions "Creates git tag for the release"
- DOC-02: README.md no longer references automated git tagging during milestone completion
- DOC-03: USER-GUIDE.md no longer references automated git tagging
- The Claude Code permissions example in README.md line 180 is preserved (not a GSD feature claim)
</success_criteria>

<output>
After completion, create `.planning/phases/08-remove-git-tagging/08-02-SUMMARY.md`
</output>
