---
phase: 33-test-steward
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - get-shit-done/workflows/plan-phase.md
autonomous: true
requirements: [STEW-04, STEW-05]

must_haves:
  truths:
    - "plan-phase.md gathers test budget data (project count, phase count, budget thresholds) before spawning the planner"
    - "The planner prompt includes a <test_budget> block with current counts, budgets, and warning/overage status"
    - "Budget data gathering uses existing gsd-tools.cjs test-count and test-config commands"
    - "Budget status is informational (warnings, not blockers) -- planner is informed but not prevented from planning"
    - "If test-count or test-config fail (no test infrastructure), the budget section is omitted silently"
  artifacts:
    - path: "get-shit-done/workflows/plan-phase.md"
      provides: "Updated plan-phase workflow with budget injection into planner prompt"
  key_links:
    - from: "plan-phase.md budget gathering step"
      to: "gsd-tools.cjs test-count"
      via: "Calls test-count and test-config CLI commands to gather budget data"
    - from: "plan-phase.md planner prompt"
      to: "gsd-planner agent"
      via: "Planner receives <test_budget> block with counts and thresholds"
---

<objective>
Inject test budget status into the plan-phase workflow so the planner is aware of test budget limits.

Purpose: During plan-phase, gather current test count and budget data, then include it in the planner prompt. This enables the planner to generate test plans within the remaining budget allocation. Budget is informational (warnings), not blocking.

Output: Updated get-shit-done/workflows/plan-phase.md with budget data gathering and planner prompt injection.
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-test-steward/33-CONTEXT.md
@.planning/phases/33-test-steward/33-RESEARCH.md
@get-shit-done/workflows/plan-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add budget data gathering to plan-phase workflow</name>
  <files>get-shit-done/workflows/plan-phase.md</files>
  <action>
Modify `get-shit-done/workflows/plan-phase.md` to add test budget gathering and injection into the planner prompt.

**1. Add a new step 7.5 between Step 7 (Use Context Paths from INIT) and Step 8 (Spawn gsd-planner Agent).**

Insert AFTER the step 7 section (`## 7. Use Context Paths from INIT`) and BEFORE `## 8. Spawn gsd-planner Agent`:

```markdown
## 7.5. Gather Test Budget Status (if available)

Gather test budget data for the planner. This is informational — failures to gather data are silently ignored.

```bash
# Get project-wide test count
PROJECT_TEST_COUNT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" test-count --raw 2>/dev/null || echo "")

# Get phase-specific test count
PHASE_TEST_COUNT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" test-count --phase ${PHASE} --raw 2>/dev/null || echo "")

# Get test config (includes budget thresholds)
TEST_CONFIG_JSON=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" test-config 2>/dev/null || echo "")
```

**If `PROJECT_TEST_COUNT` is empty or `TEST_CONFIG_JSON` is empty:** Skip budget injection (no test infrastructure detected). Set `BUDGET_BLOCK=""`.

**If data available:** Parse budget thresholds from TEST_CONFIG_JSON:
- `project_budget` = test config `.budget.project` (default 800)
- `phase_budget` = test config `.budget.per_phase` (default 50)

Calculate status:
- Project usage: `PROJECT_TEST_COUNT / project_budget * 100`
- Phase usage: `PHASE_TEST_COUNT / phase_budget * 100` (if phase count available)
- Warning at >= 80%, Over at >= 100%

Build budget block:
```
BUDGET_BLOCK="
<test_budget>
**Test Budget Status:**
- Project: ${PROJECT_TEST_COUNT}/${project_budget} tests (${project_pct}%) — ${project_status}
- Phase ${PHASE}: ${PHASE_TEST_COUNT}/${phase_budget} tests (${phase_pct}%) — ${phase_status}

**Guidance:** Plan tests within remaining budget. Budget overruns trigger warnings during audit, not blockers.
</test_budget>
"
```
```

**2. Update Step 8 (Spawn gsd-planner Agent) to include the budget block.**

In the planner prompt template inside step 8, add the `BUDGET_BLOCK` variable after the `**Phase requirement IDs**` line and before the `**Project instructions:**` line.

Add this to the `<planning_context>` block:

```markdown
{BUDGET_BLOCK}
```

This means the planner prompt will include:
```
**Phase requirement IDs (every ID MUST appear in a plan's `requirements` field):** {phase_req_ids}

{BUDGET_BLOCK}

**Project instructions:** Read ./CLAUDE.md if exists — follow project-specific guidelines
```

If `BUDGET_BLOCK` is empty (no test infrastructure), nothing is added — the planner prompt is unchanged from current behavior.

**3. Also update the revision prompt in Step 12** to include the same `BUDGET_BLOCK` in the `<revision_context>` if it is non-empty. Add it after the `**Checker issues:**` line.

Do NOT modify any other steps. The changes are:
- New step 7.5 (budget gathering)
- Updated step 8 planner prompt (add BUDGET_BLOCK)
- Updated step 12 revision prompt (add BUDGET_BLOCK)
  </action>
  <verify>
    <automated>grep -c "test_budget\|BUDGET_BLOCK\|test-count\|test-config" get-shit-done/workflows/plan-phase.md</automated>
  </verify>
  <done>plan-phase.md contains step 7.5 that gathers test budget data using gsd-tools.cjs test-count and test-config. The planner prompt in step 8 includes a `<test_budget>` block with project and phase test counts, budget thresholds, and warning/overage status. If no test infrastructure exists, the budget block is omitted silently. Step 12 revision prompt also includes the budget block.</done>
</task>

</tasks>

<verification>
1. `grep "7.5" get-shit-done/workflows/plan-phase.md` — budget gathering step exists
2. `grep "test_budget" get-shit-done/workflows/plan-phase.md` — budget block defined
3. `grep "BUDGET_BLOCK" get-shit-done/workflows/plan-phase.md` — budget injected into planner prompt
4. `grep "test-count" get-shit-done/workflows/plan-phase.md` — test-count CLI command used
5. `grep "test-config" get-shit-done/workflows/plan-phase.md` — test-config CLI command used
6. The budget block is inside the planner prompt (step 8), not outside it
</verification>

<success_criteria>
- plan-phase.md has step 7.5 gathering test budget data
- Budget data uses existing gsd-tools.cjs test-count and test-config commands
- Planner prompt in step 8 includes <test_budget> block when data is available
- Budget injection is informational (warnings, not blockers)
- If no test infrastructure, budget section is omitted silently (no errors)
- Revision prompt in step 12 also includes budget block
</success_criteria>

<output>
After completion, create `.planning/phases/33-test-steward/33-03-SUMMARY.md`
</output>
