---
phase: 03-verification-gates
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .claude/get-shit-done/workflows/autopilot.md
autonomous: true
requirements: [VRFY-03]

must_haves:
  truths:
    - "autopilot.md workflow recognizes exit code 2 as user abort (distinct from halt=1 and interrupt=130)"
    - "Exit code 2 displays clean abort message without showing error/halt language"
    - "Updated autopilot.sh and autopilot.md are installed to ~/.claude/get-shit-done/"
  artifacts:
    - path: ".claude/get-shit-done/workflows/autopilot.md"
      provides: "Exit code 2 handling for user abort"
      contains: "exit code 2"
  key_links:
    - from: ".claude/get-shit-done/workflows/autopilot.md"
      to: ".claude/get-shit-done/scripts/autopilot.sh"
      via: "bash invocation and exit code handling"
      pattern: "EXIT_CODE"
---

<objective>
Update the autopilot.md workflow to handle exit code 2 (user abort at verification gate) and install all updated files.

Purpose: Completes the VRFY-03 requirement by ensuring the Claude-facing workflow correctly interprets the new abort exit code. Without this, a user abort would be displayed as an error halt rather than a clean abort.

Output: Updated autopilot.md with exit code 2 handling. All Phase 3 files installed to ~/.claude/.
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification-gates/03-01-PLAN.md

<interfaces>
<!-- Key contracts the executor needs -->

Current autopilot.md workflow at ~/.claude/get-shit-done/workflows/autopilot.md:
```markdown
## 5. Handle Result

The script exits with:
- **0**: All phases complete
- **1**: Halted (circuit breaker, step failure, or error)
- **130**: User interrupt (SIGINT)

If exit code 0:
[shows complete banner]

If exit code non-zero, the script already printed its halt report.
Display resume instructions:
[shows resume command]
```

Exit code meanings after Phase 3:
- **0**: All phases complete
- **1**: Halted (circuit breaker, step failure, or error)
- **2**: User abort at verification gate (clean, state preserved)
- **130**: User interrupt (SIGINT)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update autopilot.md to handle exit code 2 and install files</name>
  <files>.claude/get-shit-done/workflows/autopilot.md</files>
  <action>
Read the existing autopilot.md at `~/.claude/get-shit-done/workflows/autopilot.md`.

**1. Update the exit code documentation in section 5:**

Replace the current exit code list:
```markdown
The script exits with:
- **0**: All phases complete
- **1**: Halted (circuit breaker, step failure, or error)
- **130**: User interrupt (SIGINT)
```

With:
```markdown
The script exits with:
- **0**: All phases complete
- **1**: Halted (circuit breaker, step failure, or error)
- **2**: User abort at verification gate (state preserved)
- **130**: User interrupt (SIGINT)
```

**2. Update the result handling logic:**

Replace the current handling:
```markdown
If exit code non-zero, the script already printed its halt report.
Display resume instructions:
```

With differentiated handling:
```markdown
If exit code 1 (halted):
The script already printed its halt report.
Display:
```
Autopilot halted. Review the output above for details.
To resume: /gsd:autopilot --from-phase {N}
```

If exit code 2 (user abort):
The script already printed the abort message with resume command.
Display:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► AUTOPILOT PAUSED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

User chose to abort at verification. State preserved.
To resume: /gsd:autopilot --from-phase {N}
```

If exit code 130 (interrupt):
The script already printed the interrupt message.
```

**3. Install updated files:**

After making the changes, copy the updated files to the installed location:
```bash
# autopilot.sh was already modified in-place in Task 1 of Plan 01
# Just verify it's still valid
bash -n ~/.claude/get-shit-done/scripts/autopilot.sh

# autopilot.md needs to be written to the installed location
# (this task edits it in-place at ~/.claude/get-shit-done/workflows/autopilot.md)
```

Since both files are edited in-place at `~/.claude/get-shit-done/`, no separate install step is needed. Just verify both files exist and are valid.
  </action>
  <verify>
    <automated>grep -q "exit code 2" ~/.claude/get-shit-done/workflows/autopilot.md && grep -q "PAUSED" ~/.claude/get-shit-done/workflows/autopilot.md && bash -n ~/.claude/get-shit-done/scripts/autopilot.sh && echo "All files updated and valid"</automated>
  </verify>
  <done>autopilot.md documents exit code 2, shows PAUSED banner for user abort. autopilot.sh passes syntax check. Both files are in installed location.</done>
</task>

</tasks>

<verification>
- `grep "exit code 2" autopilot.md` confirms new exit code documented
- `grep "PAUSED" autopilot.md` confirms abort gets distinct display
- `bash -n autopilot.sh` still passes after all changes
- Exit code 0 → COMPLETE banner (unchanged)
- Exit code 1 → halt report (unchanged)
- Exit code 2 → PAUSED banner (new)
- Exit code 130 → interrupt message (unchanged)
</verification>

<success_criteria>
- Workflow correctly differentiates exit code 2 from exit code 1
- User abort gets clean "PAUSED" display, not error language
- All files installed and valid
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-gates/03-02-SUMMARY.md`
</output>
