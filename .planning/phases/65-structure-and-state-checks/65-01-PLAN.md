---
phase: 65-structure-and-state-checks
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [get-shit-done/bin/lib/validation.cjs, tests/validation.test.cjs]
autonomous: true
requirements: [STRUCT-01, STRUCT-02, STRUCT-03, STRUCT-04]

must_haves:
  truths:
    - "STRUCT-01a through STRUCT-01f detect missing .planning/, PROJECT.md, ROADMAP.md, STATE.md, config.json, phases/"
    - "STRUCT-02 validates config.json parse errors, invalid model_profile enum, and unknown keys"
    - "STRUCT-03 detects phase directories not matching NN-name format"
    - "STRUCT-04 detects PLAN.md files without corresponding SUMMARY.md"
    - "Error codes match existing: E001-E004, E005, W003, W004, W005, I001"
    - "Severity assignments match CONTEXT.md specifications"
  artifacts:
    - get-shit-done/bin/lib/validation.cjs
    - tests/validation.test.cjs
  key_links:
    - "Each check is a separate { id, category, severity, check } object in the checks array"
    - "KNOWN_SETTINGS_KEYS constant defined in validation.cjs"
---

<objective>
Implement all structure checks (STRUCT-01 through STRUCT-04) in validation.cjs using TDD. Replace the existing Phase 64 STRUCT-01 entry with granular sub-entries (STRUCT-01a through STRUCT-01f) and add config validation, phase naming, and orphaned plan detection checks.

Purpose: Migrate structure validation logic from cli.cjs/verify.cjs into the validation.cjs check registry
Output: Updated validation.cjs with 9 structure checks + comprehensive tests
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
@/Users/seanspade/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/65-structure-and-state-checks/65-CONTEXT.md
@.planning/phases/65-structure-and-state-checks/65-RESEARCH.md
@get-shit-done/bin/lib/validation.cjs
@get-shit-done/bin/lib/cli.cjs
@get-shit-done/bin/lib/verify.cjs
@tests/validation.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for structure checks</name>
  <files>tests/validation.test.cjs</files>
  <behavior>
    - STRUCT-01a: When .planning/ missing, check fails with severity 'error' and id 'STRUCT-01a'
    - STRUCT-01a: When .planning/ exists, check passes
    - STRUCT-01b: When PROJECT.md missing from .planning/, check fails with severity 'error' and id 'STRUCT-01b'
    - STRUCT-01b: When PROJECT.md exists, check passes
    - STRUCT-01c: When ROADMAP.md missing, check fails with severity 'error' and id 'STRUCT-01c'
    - STRUCT-01c: When ROADMAP.md exists, check passes
    - STRUCT-01d: When STATE.md missing, check fails with severity 'error' and id 'STRUCT-01d'
    - STRUCT-01d: When STATE.md exists, check passes
    - STRUCT-01e: When config.json missing, check fails with severity 'warning' and id 'STRUCT-01e'
    - STRUCT-01e: When config.json exists, check passes
    - STRUCT-01f: When phases/ directory missing, check fails with severity 'warning' and id 'STRUCT-01f'
    - STRUCT-01f: When phases/ exists, check passes
    - STRUCT-02: When config.json has invalid JSON, check fails with severity 'error' and message mentioning parse error
    - STRUCT-02: When config.json has invalid model_profile, check fails with severity 'warning' and message mentioning invalid profile
    - STRUCT-02: When config.json has unknown keys, check fails with severity 'info' and message mentioning unknown key
    - STRUCT-02: When config.json is valid with known keys, check passes
    - STRUCT-02: When config.json does not exist, check passes (skipped gracefully)
    - STRUCT-03: Phase directory "foo" (no NN- prefix) causes check to fail with severity 'warning'
    - STRUCT-03: Phase directory "01-setup" passes
    - STRUCT-03: When phases/ directory does not exist, check passes (skipped)
    - STRUCT-04: PLAN.md without SUMMARY.md causes check to fail with severity 'info'
    - STRUCT-04: PLAN.md with matching SUMMARY.md passes
    - STRUCT-04: When phases/ directory does not exist, check passes (skipped)
    - Old STRUCT-01 check (single entry) no longer exists — replaced by STRUCT-01a through STRUCT-01f
    - All STRUCT checks have category 'structure'
  </behavior>
  <action>
Add new describe blocks to tests/validation.test.cjs for each structure check.

For STRUCT-01a through STRUCT-01f tests:
- Use fs.mkdtempSync for isolated test dirs
- Create/omit specific files to test each check independently
- For each, verify: id, category, severity, passed, and message fields
- To test individual checks, use runChecks() and find results by ID

For STRUCT-02 tests:
- Create .planning/config.json with various content: invalid JSON, invalid model_profile, unknown keys, valid content
- Test that when config.json missing, STRUCT-02 passes (graceful skip)

For STRUCT-03 tests:
- Create .planning/phases/ with directories matching and not matching the regex /^\d{2}(?:\.\d+)*-[\w-]+$/
- Test pass/fail for each pattern

For STRUCT-04 tests:
- Create .planning/phases/01-test/ with PLAN.md only, then with both PLAN.md and SUMMARY.md
- Use pattern: XX-YY-PLAN.md matches XX-YY-SUMMARY.md

Also add a test verifying the old STRUCT-01 ID no longer appears in check results (replaced by STRUCT-01a).

Update existing STRUCT-01 tests to reference STRUCT-01a instead.

All tests should FAIL initially since validation.cjs still has old single STRUCT-01.
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/get-more-shit-done && node --test tests/validation.test.cjs 2>&1 | tail -10</automated>
  </verify>
  <done>All new structure check tests exist and fail (RED phase). Existing passing tests updated for STRUCT-01a ID.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement structure checks in validation.cjs</name>
  <files>get-shit-done/bin/lib/validation.cjs</files>
  <action>
Update validation.cjs to replace the single STRUCT-01 check with all structure checks:

1. Add KNOWN_SETTINGS_KEYS constant:
```javascript
const KNOWN_SETTINGS_KEYS = ['model_profile', 'commit_docs', 'search_gitignored', 'branching_strategy',
  'workflow', 'parallelization', 'autopilot', 'mode', 'depth', 'model_overrides',
  'research', 'plan_checker', 'verifier', 'nyquist_validation', 'test'];
```

2. Replace the existing STRUCT-01 entry in the checks array with these entries:

STRUCT-01a (.planning/ directory): severity 'error', check fs.existsSync('.planning')
STRUCT-01b (PROJECT.md): severity 'error', check fs.existsSync('.planning/PROJECT.md')
STRUCT-01c (ROADMAP.md): severity 'error', check fs.existsSync('.planning/ROADMAP.md')
STRUCT-01d (STATE.md): severity 'error', check fs.existsSync('.planning/STATE.md')
STRUCT-01e (config.json): severity 'warning', check fs.existsSync('.planning/config.json')
STRUCT-01f (phases/): severity 'warning', check fs.existsSync('.planning/phases')

3. Add STRUCT-02 (config validation): single check per CONTEXT.md decision.
   - Default severity: 'warning' in the registry entry
   - If config.json doesn't exist, return passed: true (skip)
   - Try JSON.parse; on failure return { passed: false, message: 'config.json: JSON parse error - ...', severity: 'error' }
   - If parsed, accumulate issues: invalid model_profile (warning), unknown keys (info)
   - Return passed: false if any issues, message joins all with '; '
   - Check function can return optional `severity` field to override the entry default
   - Update runChecks to use `result.severity || entry.severity` (one-line change)

4. Add STRUCT-03 (phase directory naming): severity 'warning'
   - If phases/ doesn't exist, return passed: true
   - Read phases/ directory, check each subdirectory name against /^\d{2}(?:\.\d+)*-[\w-]+$/
   - Fail with message listing non-conforming directories

5. Add STRUCT-04 (orphaned plans): severity 'info'
   - If phases/ doesn't exist, return passed: true
   - For each phase dir, find *-PLAN.md files without matching *-SUMMARY.md
   - Fail with message listing orphaned plans

Export KNOWN_SETTINGS_KEYS for future use.

Key constraints:
- NO console output, NO ANSI codes
- Synchronous execution only
- Catch all errors internally, return { passed, message }
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/get-more-shit-done && node --test tests/validation.test.cjs</automated>
  </verify>
  <done>All structure check tests pass GREEN. runChecks with categories: ['structure'] returns all STRUCT checks. No old STRUCT-01 ID exists.</done>
</task>

</tasks>

<verification>
- `node --test tests/validation.test.cjs` — all tests pass
- `node -e "const v = require('./get-shit-done/bin/lib/validation.cjs'); const r = v.runChecks('.', { categories: ['structure'] }); console.log(r.map(c => c.id).join(', '))"` — shows STRUCT-01a through STRUCT-04
- `node -e "const v = require('./get-shit-done/bin/lib/validation.cjs'); console.log(v.KNOWN_SETTINGS_KEYS.length)"` — prints 15
- `node -e "const v = require('./get-shit-done/bin/lib/validation.cjs'); const r = v.runChecks('.'); console.log(r.find(c => c.id === 'STRUCT-01'))"` — prints undefined (old ID gone)
</verification>
