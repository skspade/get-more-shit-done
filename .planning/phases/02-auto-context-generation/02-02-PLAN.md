---
phase: 02-auto-context-generation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - get-shit-done/workflows/discuss-phase.md
autonomous: true
requirements: [ACTX-01, ACTX-03]

must_haves:
  truths:
    - "discuss-phase.md detects --auto flag in arguments and routes to auto-context agent spawn"
    - "When --auto flag present, discuss-phase skips interactive steps (check_existing, scout_codebase, analyze_phase, present_gray_areas, discuss_areas) and instead spawns gsd-auto-context"
    - "When --auto flag absent, discuss-phase behaves identically to before (no regression in interactive path)"
    - "After gsd-auto-context returns CONTEXT GENERATED, discuss-phase proceeds to git_commit, update_state, and auto_advance steps identically to the interactive path"
    - "The auto-context spawn uses Task() with the resolved model from the model profile"
  artifacts:
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "discuss-phase workflow with --auto routing for autopilot mode"
      contains: "auto_context_check"
  key_links:
    - from: "get-shit-done/workflows/discuss-phase.md"
      to: "agents/gsd-auto-context.md"
      via: "Task spawn with agent file reference"
      pattern: "gsd-auto-context"
---

<objective>
Modify discuss-phase.md to add --auto routing that spawns the gsd-auto-context agent for autonomous context generation.

Purpose: When autopilot.sh calls `/gsd:discuss-phase N --auto`, the workflow needs to detect the --auto flag and spawn the auto-context agent instead of running the interactive question loop. The post-write operations (git commit, STATE.md update, auto-advance) remain identical to the interactive path.

Output: Updated discuss-phase.md with a new `auto_context_check` step that routes between interactive and autonomous paths.
</objective>

<execution_context>
@/home/seans/.claude/get-shit-done/workflows/execute-plan.md
@/home/seans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auto-context-generation/02-RESEARCH.md
@.planning/phases/02-auto-context-generation/02-CONTEXT.md

<interfaces>
<!-- Current discuss-phase.md step ordering -->

Current step flow (interactive):
```
1. initialize          - Parse phase number, run gsd-tools init phase-op
2. check_existing      - Check if CONTEXT.md already exists, offer update/view/skip
3. scout_codebase      - Lightweight code scan for gray area identification
4. analyze_phase       - Identify gray areas from ROADMAP phase description
5. present_gray_areas  - Ask user which areas to discuss
6. discuss_areas       - Interactive Q&A loop per selected area
7. write_context       - Generate CONTEXT.md from discussion
8. confirm_creation    - Show summary and next steps
9. git_commit          - Commit CONTEXT.md via gsd-tools
10. update_state       - Update STATE.md via gsd-tools
11. auto_advance       - If --auto, spawn plan-phase
```

New step flow (when --auto detected):
```
1. initialize          - Same as interactive
2. auto_context_check  - NEW: detect --auto, spawn gsd-auto-context
   (skips steps 3-8)
9. git_commit          - Same as interactive
10. update_state       - Same as interactive
11. auto_advance       - Same as interactive
```

From model-profile-resolution.md:
```
Model profile resolved from .planning/config.json model_profile field.
For gsd-auto-context, this is a new agent. In quality profile: opus (inherit).
In balanced: sonnet. In budget: haiku.
```

From gsd-tools.cjs roadmap get-phase:
```json
{
  "found": true,
  "phase_number": "2",
  "phase_name": "Auto-Context Generation",
  "goal": "...",
  "success_criteria": ["..."],
  "section": "### Phase 2: ..."
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto_context_check step to discuss-phase.md</name>
  <files>get-shit-done/workflows/discuss-phase.md</files>
  <action>
Read the current `get-shit-done/workflows/discuss-phase.md` file. Add a new step `auto_context_check` that is inserted AFTER the `initialize` step and BEFORE the `check_existing` step. This step only executes when `--auto` flag is present in $ARGUMENTS.

**Insert the following step after `</step>` of `initialize` and before `<step name="check_existing">`:**

```markdown
<step name="auto_context_check" priority="after-initialize">
**If `--auto` flag present in $ARGUMENTS:**

1. Get phase metadata for the agent:
\`\`\`bash
PHASE_INFO=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap get-phase "${PHASE}")
PHASE_GOAL=$(echo "$PHASE_INFO" | jq -r '.goal // .section')
PHASE_CRITERIA=$(echo "$PHASE_INFO" | jq -r '.success_criteria // []')
PHASE_REQ_IDS=$(echo "$PHASE_INFO" | jq -r '.section' | grep -oP 'Requirements: \K.*' || echo "")
\`\`\`

2. Check if CONTEXT.md already exists using `has_context` from init:
   - **If `has_context` is true:** Display `Using existing context: ${context_path}` and skip to git_commit step (context already generated, proceed to auto-advance).
   - **If `has_context` is false:** Continue to spawn agent.

3. Resolve model profile for gsd-auto-context:
\`\`\`bash
MODEL_PROFILE=$(cat .planning/config.json 2>/dev/null | grep -o '"model_profile"[[:space:]]*:[[:space:]]*"[^"]*"' | grep -o '"[^"]*"$' | tr -d '"' || echo "balanced")
\`\`\`
Look up gsd-auto-context in the model profiles table. Since gsd-auto-context is a new agent not yet in the table, use the same model as gsd-phase-researcher (it does similar read-analyze-write work): quality=opus(inherit), balanced=sonnet, budget=haiku.

4. Display banner:
\`\`\`
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► AUTO-CONTEXT: PHASE {X}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

◆ Generating context autonomously...
\`\`\`

5. Find prior completed phases' CONTEXT.md files for style reference:
\`\`\`bash
PRIOR_CONTEXTS=$(ls .planning/phases/*/??-CONTEXT.md 2>/dev/null | head -5)
\`\`\`

6. Spawn gsd-auto-context agent:
\`\`\`
Task(
  prompt="First, read /home/seans/.claude/agents/gsd-auto-context.md for your role and instructions.\n\n<objective>\nGenerate CONTEXT.md for Phase ${phase_number}: ${phase_name} autonomously.\nNo human input. Read project artifacts, make decisions, annotate reasoning.\n</objective>\n\n<files_to_read>\n- .planning/PROJECT.md\n- .planning/ROADMAP.md\n- .planning/REQUIREMENTS.md\n${PRIOR_CONTEXTS as bullet list}\n</files_to_read>\n\n<phase_info>\nPhase: ${phase_number}\nName: ${phase_name}\nGoal: ${PHASE_GOAL}\nSuccess criteria: ${PHASE_CRITERIA}\nRequirement IDs: ${PHASE_REQ_IDS}\nPhase dir: ${phase_dir}\nPadded phase: ${padded_phase}\nOutput file: ${phase_dir}/${padded_phase}-CONTEXT.md\n</phase_info>",
  subagent_type="general-purpose",
  model="{resolved_model}",
  description="Auto-generate context for Phase ${PHASE}"
)
\`\`\`

7. Handle agent return:
   - **If `## CONTEXT GENERATED`:** Display `✓ Context generated for Phase ${PHASE}`. Proceed to git_commit step.
   - **If `## CONTEXT BLOCKED`:** Display the blocker message. Exit workflow with error:
     \`\`\`
     Auto-context generation blocked for Phase ${PHASE}.
     Reason: {blocker from agent}

     Run /gsd:discuss-phase ${PHASE} interactively to resolve.
     \`\`\`

**If `--auto` flag NOT present:** Continue to check_existing step (existing interactive flow unchanged).
</step>
```

**CRITICAL: Do NOT modify any existing steps.** The interactive path (check_existing through write_context) must remain exactly as-is. The auto_context_check step is an early-return branch that skips the interactive steps entirely when --auto is detected.

**Also update the model profiles table** in `get-shit-done/references/model-profiles.md` to include gsd-auto-context. Add a new row:
```
| gsd-auto-context | opus | sonnet | haiku |
```
This matches gsd-phase-researcher's profile since both do similar read-analyze-write work.
  </action>
  <verify>
    <automated>grep -c "auto_context_check" /home/seans/Documents/Source/get-more-shit-done/get-shit-done/workflows/discuss-phase.md && grep -c "gsd-auto-context" /home/seans/Documents/Source/get-more-shit-done/get-shit-done/workflows/discuss-phase.md</automated>
  </verify>
  <done>discuss-phase.md contains auto_context_check step with --auto detection, gsd-auto-context agent spawn, and proper routing. Interactive path unchanged. Model profiles table includes gsd-auto-context.</done>
</task>

<task type="auto">
  <name>Task 2: Install updated workflow and update model profiles</name>
  <files>get-shit-done/workflows/discuss-phase.md, get-shit-done/references/model-profiles.md</files>
  <action>
1. Copy the updated discuss-phase.md to the installed location:
```bash
cp get-shit-done/workflows/discuss-phase.md ~/.claude/get-shit-done/workflows/discuss-phase.md
```

2. Update `get-shit-done/references/model-profiles.md` to include gsd-auto-context in the Profile Definitions table. Add a new row after gsd-phase-researcher:
```markdown
| gsd-auto-context | opus | sonnet | haiku |
```

3. Copy the updated model-profiles.md to installed location:
```bash
cp get-shit-done/references/model-profiles.md ~/.claude/get-shit-done/references/model-profiles.md
```

4. Verify the routing works by checking the installed file:
```bash
grep "auto_context_check" ~/.claude/get-shit-done/workflows/discuss-phase.md
grep "gsd-auto-context" ~/.claude/get-shit-done/references/model-profiles.md
```
  </action>
  <verify>
    <automated>grep -q "auto_context_check" /home/seans/.claude/get-shit-done/workflows/discuss-phase.md && grep -q "gsd-auto-context" /home/seans/.claude/get-shit-done/references/model-profiles.md && echo "Installation verified"</automated>
  </verify>
  <done>Updated discuss-phase.md and model-profiles.md installed to ~/.claude/get-shit-done/. auto_context_check step present in installed workflow. gsd-auto-context present in installed model profiles.</done>
</task>

</tasks>

<verification>
- discuss-phase.md has auto_context_check step after initialize and before check_existing
- When --auto flag present, workflow spawns gsd-auto-context agent
- When --auto flag absent, workflow falls through to check_existing (interactive path unchanged)
- After agent returns CONTEXT GENERATED, workflow proceeds to git_commit and update_state
- Model profiles table includes gsd-auto-context with correct profile values
- Both source and installed copies are updated
- No existing steps in discuss-phase.md are modified
</verification>

<success_criteria>
- --auto flag detection works in discuss-phase.md
- Auto-context agent is spawned with correct phase metadata
- Interactive path has zero changes (no regression risk)
- Post-write operations (commit, state, auto-advance) shared between paths
</success_criteria>

<output>
After completion, create `.planning/phases/02-auto-context-generation/02-02-SUMMARY.md`
</output>
