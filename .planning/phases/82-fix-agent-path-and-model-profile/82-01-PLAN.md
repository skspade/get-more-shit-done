---
phase: 82-fix-agent-path-and-model-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/gsd-test-reviewer.md
  - ~/.claude/get-shit-done/bin/lib/core.cjs
  - ~/.claude/get-shit-done/bin/lib/init.cjs
autonomous: true
requirements:
  - CMD-01
  - AGT-01
  - AGT-02
  - AGT-03
  - AGT-04
  - AGT-05
  - AGT-06
  - AGT-07
  - AGT-08

must_haves:
  truths:
    - gsd-test-reviewer agent file exists at ~/.claude/agents/gsd-test-reviewer.md
    - MODEL_PROFILES in core.cjs includes gsd-test-reviewer entry with quality sonnet, balanced sonnet, budget haiku
    - cmdInitTestReview returns reviewer_model, planner_model, executor_model but NOT checker_model or verifier_model
  artifacts:
    - ~/.claude/agents/gsd-test-reviewer.md
  key_links:
    - test-review.md Task() spawn references ~/.claude/agents/gsd-test-reviewer.md
    - resolveModelInternal('gsd-test-reviewer') resolves from MODEL_PROFILES
---

<objective>
Deploy gsd-test-reviewer agent file, register its model profile, and remove unused init fields so the test-review command works end-to-end.

Purpose: Closes three integration gaps found during v2.9 milestone audit that prevent the test-review command from functioning correctly.
Output: Agent file deployed, model profile registered, init function cleaned up.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/gsd-test-reviewer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy agent file and register model profile</name>
  <files>~/.claude/agents/gsd-test-reviewer.md, ~/.claude/get-shit-done/bin/lib/core.cjs</files>
  <action>
1. Copy the agent file from the project repo to the runtime path:
   ```bash
   cp agents/gsd-test-reviewer.md ~/.claude/agents/gsd-test-reviewer.md
   ```

2. In `~/.claude/get-shit-done/bin/lib/core.cjs`, add a `gsd-test-reviewer` entry to the `MODEL_PROFILES` object immediately after the `gsd-test-steward` entry (line 30). The new entry should match `gsd-test-steward` values:
   ```javascript
   'gsd-test-reviewer':        { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
   ```
  </action>
  <verify>
    <automated>test -f ~/.claude/agents/gsd-test-reviewer.md && grep -q "gsd-test-reviewer" ~/.claude/get-shit-done/bin/lib/core.cjs && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Agent file exists at ~/.claude/agents/gsd-test-reviewer.md AND MODEL_PROFILES contains gsd-test-reviewer entry</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused model fields from cmdInitTestReview</name>
  <files>~/.claude/get-shit-done/bin/lib/init.cjs</files>
  <action>
In `~/.claude/get-shit-done/bin/lib/init.cjs`, in the `cmdInitTestReview` function (line 834), remove these two lines from the result object:
- `checker_model: resolveModelInternal(cwd, 'gsd-plan-checker'),` (line 856)
- `verifier_model: resolveModelInternal(cwd, 'gsd-verifier'),` (line 857)

Keep `reviewer_model`, `planner_model`, and `executor_model` — these are used by the test-review command.

After removal, the Models section of the result object should have exactly 3 fields:
```javascript
// Models
reviewer_model: resolveModelInternal(cwd, 'gsd-test-reviewer'),
planner_model: resolveModelInternal(cwd, 'gsd-planner'),
executor_model: resolveModelInternal(cwd, 'gsd-executor'),
```
  </action>
  <verify>
    <automated>node -e "const m = require('$HOME/.claude/get-shit-done/bin/lib/init.cjs'); const r = m.cmdInitTestReview('.', true); const j = JSON.parse(r); if (j.checker_model || j.verifier_model) { console.log('FAIL: unused fields still present'); process.exit(1); } if (!j.reviewer_model || !j.planner_model || !j.executor_model) { console.log('FAIL: required fields missing'); process.exit(1); } console.log('PASS');"</automated>
  </verify>
  <done>cmdInitTestReview returns reviewer_model, planner_model, executor_model but does NOT return checker_model or verifier_model</done>
</task>

</tasks>

<verification>
All three success criteria from the roadmap are met:
1. `gsd-test-reviewer` agent file exists at `~/.claude/agents/gsd-test-reviewer.md`
2. `MODEL_PROFILES` in `core.cjs` includes `gsd-test-reviewer` entry matching `gsd-test-steward` profile
3. `cmdInitTestReview` no longer returns unused `checker_model` / `verifier_model` fields
</verification>
