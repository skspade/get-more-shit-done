---
phase: 43-milestone-route-and-cleanup
plan: 1
title: Milestone Route, Cleanup, and Completion Banner
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/pr-review.md
autonomous: true
requirements:
  - MST-01
  - MST-02
  - CLN-01
  - CLN-02
  - CLN-03
must_haves:
  truths:
    - "Step 9 milestone route builds MILESTONE-CONTEXT.md from $GROUPS with one Feature section per file-region group"
    - "Step 9 MILESTONE-CONTEXT.md includes Source, Findings, Score metadata and milestone goal line"
    - "Step 9b delegates to new-milestone workflow via gsd-tools.cjs init new-milestone and inline execution of steps 1-11"
    - "Step 9b uses researcher_model, synthesizer_model, roadmapper_model from MINIT JSON"
    - "Step 10 deletes .planning/review-context.md via rm -f after both quick and milestone routes"
    - "Step 10 explicitly does NOT delete the permanent review report"
    - "Step 11 displays unified completion banner with GSD > PR REVIEW COMPLETE header"
    - "Step 11 banner shows source (findings/groups), route taken, and report path"
    - "Step 11 quick route banner includes directory and commit hash"
    - "Step 11 milestone route banner includes milestone version and name"
    - "Step 8i quick route completion banner is replaced by Step 11 unified banner"
    - "Cleanup runs as shared exit path after both quick and milestone routes"
  artifacts:
    - "get-shit-done/workflows/pr-review.md contains Steps 9, 10, and 11"
  key_links:
    - "Step 9 reads $ROUTE, $REVIEW_SCORE, $GROUPS from Steps 7-8"
    - "Step 9 mirrors Linear workflow lines 420-493 milestone route pattern"
    - "Step 9b follows new-milestone.md steps 1-11 inline"
    - "Step 10 and 11 are shared exit path reached by both quick (after 8i) and milestone (after 9b)"
    - "Step 11 replaces the quick route's Step 8i completion banner with unified display"
---

# Plan 43-01: Milestone Route, Cleanup, and Completion Banner

## Objective

Replace the "Steps 9-11: Milestone Route and Cleanup" placeholder in pr-review.md with fully implemented Steps 9 (milestone route with MILESTONE-CONTEXT.md generation and new-milestone delegation), 10 (cleanup of temporary files), and 11 (unified completion banner). Also update Step 8i to remove its quick-specific completion banner, deferring to the unified Step 11 banner.

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@get-shit-done/workflows/pr-review.md
@get-shit-done/workflows/linear.md
@get-shit-done/workflows/new-milestone.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Steps 9-11 and update Step 8i in pr-review.md</name>
  <files>get-shit-done/workflows/pr-review.md</files>
  <action>
In get-shit-done/workflows/pr-review.md, make these changes:

**1. Update Step 8i** — Remove the completion banner display from Step 8i. Keep the final commit and commit hash extraction, but remove the banner display block (the `GSD > PR REVIEW QUICK TASK COMPLETE` section). After the commit hash extraction, Step 8i should just store `$QUICK_DIR` and `$commit_hash` for Step 11.

**2. Replace the "Steps 9-11" placeholder** with three fully implemented steps:

---

**Step 9: Milestone route**

If `$ROUTE == "milestone"`:

**9a. Build MILESTONE-CONTEXT.md:**

```markdown
# Milestone Context

**Source:** PR Review (.planning/reviews/YYYY-MM-DD-pr-review.md)
**Findings:** {total_findings_count} findings in {groups_count} groups
**Score:** {$REVIEW_SCORE}

## Goal

Resolve all critical and important issues identified by PR review across {distinct_files_count} files.

## Features

${For each group in $GROUPS:}
### Fix: ${group.primary_file} (${group.max_severity})

| # | Severity | Agent | Description | Line | Fix |
|---|----------|-------|-------------|------|-----|
${For each finding in group.findings:}
| ${n} | ${finding.severity} | ${finding.agent} | ${finding.description} | ${finding.line} | ${finding.fix_suggestion or "—"} |
${end}
${end}
```

Write to `.planning/MILESTONE-CONTEXT.md`.

For groups with null primary_file (general observations), use header `### Fix: General observations (${max_severity})`.

**9b. Initialize and delegate to new-milestone workflow:**

```bash
MINIT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init new-milestone)
```

Parse MINIT JSON for: `researcher_model`, `synthesizer_model`, `roadmapper_model`, `commit_docs`, `research_enabled`, `current_milestone`, `project_exists`, `roadmap_exists`.

Execute new-milestone workflow steps 1-11 inline (follow new-milestone.md):

1. Load context (PROJECT.md, MILESTONES.md, STATE.md)
2. Gather milestone goals — MILESTONE-CONTEXT.md exists, use it
3. Determine milestone version
4. Update PROJECT.md
5. Update STATE.md
6. Cleanup and commit (delete MILESTONE-CONTEXT.md after consuming)
7. Resolve models from MINIT
8. Research decision (ask user)
9. Define requirements
10. Create roadmap (spawn gsd-roadmapper)
11. Done — display completion

Use models from MINIT JSON for spawned agents:
- Researchers: `researcher_model`
- Synthesizer: `synthesizer_model`
- Roadmapper: `roadmapper_model`

Extract `$MILESTONE_VERSION` and `$MILESTONE_NAME` from the new-milestone output (version from step 3, name from PROJECT.md update in step 4).

Display after delegation:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► PR REVIEW MILESTONE INITIALIZED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

**Step 10: Cleanup**

This step runs after BOTH routes (shared exit path).

```bash
rm -f .planning/review-context.md
```

The permanent review report at `.planning/reviews/YYYY-MM-DD-pr-review.md` is explicitly preserved — it serves as the audit trail.

---

**Step 11: Completion banner**

Display unified completion banner:

**If `$ROUTE == "quick"`:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► PR REVIEW COMPLETE ✓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Source: {total_findings_count} findings in {groups_count} groups
Route: Quick task
Report: .planning/reviews/YYYY-MM-DD-pr-review.md
Directory: ${QUICK_DIR}
Commit: ${commit_hash}
```

**If `$ROUTE == "milestone"`:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► PR REVIEW COMPLETE ✓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Source: {total_findings_count} findings in {groups_count} groups
Route: Milestone
Report: .planning/reviews/YYYY-MM-DD-pr-review.md
Milestone: ${MILESTONE_VERSION} ${MILESTONE_NAME}
```

---

**3. Ensure control flow:** After Step 8 quick route completes (8i), flow falls through to Step 10 (cleanup) then Step 11 (banner). After Step 9 milestone route completes (9b), flow falls through to Step 10 (cleanup) then Step 11 (banner). This is the shared exit path described in CONTEXT.md.

**Pattern reference:** The milestone route pattern (9a-9b) mirrors the Linear workflow's milestone route at lines 420-493 of linear.md. Use the same structure: build MILESTONE-CONTEXT.md, init new-milestone, execute steps 1-11 inline.
  </action>
  <verify>
Read get-shit-done/workflows/pr-review.md and confirm:
1. Step 8i no longer has its own completion banner — it ends after commit hash extraction
2. Step 9 exists with subsections 9a (build MILESTONE-CONTEXT.md) and 9b (new-milestone delegation)
3. MILESTONE-CONTEXT.md template has Source, Findings, Score metadata and Goal line
4. Each file-region group becomes a Feature section with Fix header and findings table
5. Step 9b calls gsd-tools.cjs init new-milestone and executes steps 1-11 inline
6. Step 10 runs rm -f .planning/review-context.md for both routes
7. Step 10 explicitly notes permanent report is preserved
8. Step 11 displays unified banner with GSD > PR REVIEW COMPLETE header
9. Step 11 quick route shows directory and commit hash
10. Step 11 milestone route shows milestone version and name
11. Both routes reach Steps 10-11 as shared exit path
  </verify>
  <done>pr-review.md Steps 9-11 are fully implemented replacing the placeholder, and Step 8i defers its completion banner to Step 11</done>
</task>

</tasks>

<verification>
- [ ] pr-review.md Step 9 implements milestone route with MILESTONE-CONTEXT.md generation
- [ ] MILESTONE-CONTEXT.md has file-region groups as Feature sections
- [ ] Step 9b delegates to new-milestone workflow inline
- [ ] Step 10 deletes review-context.md after both routes
- [ ] Permanent review report is not deleted
- [ ] Step 11 displays unified completion banner for both routes
- [ ] Quick route banner includes directory and commit hash
- [ ] Milestone route banner includes milestone version and name
- [ ] Step 8i no longer displays its own completion banner
</verification>

<success_criteria>
- pr-review.md workflow is complete with all 11 steps implemented
- Milestone route builds MILESTONE-CONTEXT.md and delegates to new-milestone
- Cleanup removes temporary files while preserving permanent report
- Unified completion banner shows route-specific information
</success_criteria>

<output>
After completion, create `.planning/phases/43-milestone-route-and-cleanup/43-01-SUMMARY.md`
</output>
