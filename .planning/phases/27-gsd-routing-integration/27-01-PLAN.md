# Plan 01: GSD Routing Integration

```yaml
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/brainstorm.md
  - commands/gsd/brainstorm.md
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
```

## Objective

Extend the brainstorm workflow with steps 9-10 that detect project state after design commit and route into the correct GSD creation flow (new-milestone or new-project) with design context seeded to skip the questioning phase.

## must_haves

- [ ] After design commit, workflow checks `.planning/PROJECT.md` existence to determine route
- [ ] If PROJECT.md exists, workflow writes MILESTONE-CONTEXT.md and executes new-milestone flow inline
- [ ] If PROJECT.md does not exist, workflow delegates to `/gsd:new-project --auto @{design_file}`
- [ ] User is asked whether to proceed with creation or stop after design commit
- [ ] MILESTONE-CONTEXT.md contains design doc content structured as milestone features
- [ ] The brainstorm command file includes Task in allowed-tools
- [ ] The brainstorm command objective describes the full flow including routing

## Tasks

<task id="1" title="Update brainstorm command file">

**What:** Update `commands/gsd/brainstorm.md` to add Task to allowed-tools and update the objective to describe the full brainstorm-to-creation flow.

**Files:** `commands/gsd/brainstorm.md`

**Steps:**

1. Read `commands/gsd/brainstorm.md`
2. Add `Task` to the `allowed-tools` list in frontmatter (after AskUserQuestion)
3. Update the `<objective>` section to mention routing into GSD creation flows after design approval:
   - Add step 9: "Detect project state and ask user whether to create milestone/project or stop"
   - Add step 10: "Route into new-milestone (inline) or new-project (--auto) with design context"
   - Update the **Output** line to include: "Optionally routes into milestone/project creation"
4. Update the `description` in frontmatter to mention routing capability

**Verification:**
- `allowed-tools` includes Task
- Objective lists all 10 steps
- Description mentions routing

</task>

<task id="2" title="Add routing steps to brainstorm workflow">

**What:** Add steps 9 and 10 to `get-shit-done/workflows/brainstorm.md` after the existing step 8 (commit design file). Step 9 asks the user whether to route into a creation flow. Step 10 handles both routes.

**Files:** `get-shit-done/workflows/brainstorm.md`

**Steps:**

1. Read `get-shit-done/workflows/brainstorm.md`
2. Read `get-shit-done/workflows/linear.md` step 5a for MILESTONE-CONTEXT.md format reference
3. Read `get-shit-done/workflows/new-milestone.md` steps 1-11 for inline execution reference
4. After step 8's content, add:

**Step 9: Offer GSD Routing**

```markdown
## 9. Offer GSD Routing

After the design is committed, check project state and offer routing options.

**Check project state:**
```bash
test -f .planning/PROJECT.md && echo "exists" || echo "missing"
```

**If PROJECT.md exists:**

Use AskUserQuestion:
- header: "Next Steps"
- question: "Design committed. Want to create a new milestone from this design?"
- options: ["Create milestone", "Done — just keep the design"]

**If PROJECT.md does not exist:**

Use AskUserQuestion:
- header: "Next Steps"
- question: "Design committed. Want to create a new project from this design?"
- options: ["Create project", "Done — just keep the design"]

**If user selects "Done":** Display completion message and exit workflow.

**If user selects "Create milestone" or "Create project":** Proceed to step 10.
```

**Step 10: Route into Creation Flow**

```markdown
## 10. Route into Creation Flow

### Milestone Route (PROJECT.md exists)

**10a. Build MILESTONE-CONTEXT.md:**

Write to `.planning/MILESTONE-CONTEXT.md`:

```markdown
# Milestone Context

**Source:** Brainstorm session ({$TOPIC})
**Design:** .planning/designs/{date}-{topic-slug}-design.md

## Milestone Goal

{Extract from design doc: the overall goal/purpose described in the selected approach}

## Features

{For each design section that was approved:}
### {Section Name}

{Section content — the approved design section text}
```

**10b. Initialize milestone models:**

```bash
MINIT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init new-milestone)
```

Parse JSON for model parameters.

**10c. Execute new-milestone workflow steps 1-11 inline:**

Follow the new-milestone workflow (`new-milestone.md`) from Step 1 through Step 11:

1. Load context (PROJECT.md, MILESTONES.md, STATE.md)
2. Gather milestone goals — MILESTONE-CONTEXT.md exists, use it
3. Determine milestone version
4. Update PROJECT.md
5. Update STATE.md
6. Cleanup and commit (delete MILESTONE-CONTEXT.md after consuming)
7. Resolve models from MINIT
8. Research decision (ask user)
9. Define requirements
10. Create roadmap (spawn gsd-roadmapper)
11. Done — display completion

### New-Project Route (no PROJECT.md)

Display:
```
Routing to new-project with design context...
```

The executor should output instructions for the user to run:

```
/gsd:new-project --auto @.planning/designs/{date}-{topic-slug}-design.md
```

Note: new-project cannot be executed inline because it requires its own command context and spawns research subagents. The user must invoke it as a separate command.
```

5. Update the `</process>` closing tag position to be after step 10

**Verification:**
- Step 9 uses AskUserQuestion with correct options based on PROJECT.md existence
- Step 10 milestone route writes MILESTONE-CONTEXT.md following the linear.md pattern
- Step 10 milestone route executes new-milestone steps 1-11 inline
- Step 10 new-project route provides the correct `/gsd:new-project --auto @{file}` command
- MILESTONE-CONTEXT.md includes source attribution, topic, and design sections as features

</task>

## Verification Criteria

1. **ROUTE-01 coverage:** Run `/gsd:brainstorm` with a PROJECT.md present — after design approval and commit, the workflow offers "Create milestone" and proceeds to write MILESTONE-CONTEXT.md and run new-milestone inline. Run without PROJECT.md — offers "Create project" and provides the new-project command.
2. **ROUTE-02 coverage:** MILESTONE-CONTEXT.md contains design doc sections mapped as features. For new-project, the design file path is passed as the `@` argument.
3. **Command file:** `commands/gsd/brainstorm.md` has Task in allowed-tools and objective describes all 10 steps.
4. **No upstream modifications:** `new-milestone.md` and `new-project.md` are not modified.
