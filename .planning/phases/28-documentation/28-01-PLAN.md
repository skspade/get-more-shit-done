# Plan 01: Brainstorm Command Documentation

```yaml
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/help.md
  - docs/USER-GUIDE.md
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
```

## Objective

Add `/gsd:brainstorm` documentation to all three user-facing reference files: help.md (detailed command reference), USER-GUIDE.md (usage guide with examples), and README.md (concise command table entry).

## must_haves

- [ ] help.md contains a Brainstorming section with `/gsd:brainstorm` command entry including purpose, flow description, and usage syntax
- [ ] USER-GUIDE.md command reference table includes a Brainstorming row with `/gsd:brainstorm [topic]`
- [ ] USER-GUIDE.md contains a usage example showing the brainstorm-to-milestone flow
- [ ] README.md Utilities table includes a `/gsd:brainstorm [topic]` row
- [ ] Command description is consistent across all three files (collaborative brainstorming that produces a design doc and optionally routes into GSD creation)
- [ ] Usage syntax shown as `/gsd:brainstorm [topic]` with brackets indicating optional argument

## Tasks

<task id="1" title="Add brainstorm section to help.md">

**What:** Add a "Brainstorming" section to `get-shit-done/workflows/help.md` with the `/gsd:brainstorm` command entry, placed between "Milestone Management" and "Progress Tracking" sections.

**Files:** `get-shit-done/workflows/help.md`

**Steps:**

1. Read `get-shit-done/workflows/help.md`
2. Read `commands/gsd/brainstorm.md` to get the authoritative command description and flow
3. Find the "### Milestone Management" section (around line 163) and the "### Progress Tracking" section (around line 186)
4. Insert a new "### Brainstorming" section between them, following the established entry format (bold command name, description paragraph, bullet list of capabilities, usage line). Content:

```markdown
### Brainstorming

**`/gsd:brainstorm [topic]`**

Run a collaborative brainstorming session that explores your project context, asks clarifying questions, proposes approaches with trade-offs, and produces a design document. After the design is approved and committed, optionally routes into GSD milestone or project creation.

- Explores project files, docs, and recent commits before asking questions
- Asks clarifying questions one at a time, preferring multiple choice
- Proposes 2-3 distinct approaches with trade-offs and a recommendation
- Presents the design in sections, pausing for approval after each
- Writes the approved design to `.planning/designs/`
- After design commit, offers to create a milestone (if PROJECT.md exists) or project (if not)

Usage: `/gsd:brainstorm` (start a free-form session)
Usage: `/gsd:brainstorm "add dark mode support"` (seed with a topic)

Result: Design doc in `.planning/designs/` + optional milestone/project creation
```

5. Also add `/gsd:brainstorm [topic]` to the quick-reference examples section at the bottom of the file (around line 475), in a "Brainstorming" group:

```markdown
# Brainstorming
/gsd:brainstorm                              # Start brainstorming session
/gsd:brainstorm "add caching layer"          # Start with a specific topic
```

**Verification:**
- "### Brainstorming" section exists between "### Milestone Management" and "### Progress Tracking"
- Entry follows the established format (bold command, description, bullets, usage, result)
- Quick-reference section includes brainstorm examples

</task>

<task id="2" title="Add brainstorm to USER-GUIDE.md">

**What:** Add `/gsd:brainstorm` to the command reference table and add a usage example section in `docs/USER-GUIDE.md`.

**Files:** `docs/USER-GUIDE.md`

**Steps:**

1. Read `docs/USER-GUIDE.md`
2. Find the command reference table section (around line 157). Locate the table grouping pattern — look for existing group headers like "**Core Workflow**", "**Navigation**", "**Phase Management**", "**Brownfield & Utilities**"
3. Add a new row for brainstorm. Place it in the "Brownfield & Utilities" group (where `/gsd:linear` lives) or add a "**Brainstorming**" group row before it. Follow whatever grouping pattern is used:

```markdown
| `/gsd:brainstorm [topic]` | Collaborative brainstorming that produces a design doc and routes into milestone/project creation | Want to explore an idea before committing to implementation |
```

4. Find the "## Usage Examples" section (around line 309). Add a brainstorming example after the Linear examples (around line 421):

```markdown
### Brainstorming

```bash
# Start a brainstorming session
/gsd:brainstorm

# Seed with a specific topic
/gsd:brainstorm "add webhook support"
```

**Flow:** The brainstorm command explores your codebase, asks clarifying questions, proposes approaches, and builds a design document section by section. After approval, it offers to create a milestone or project from the design.
```

**Verification:**
- Command reference table includes `/gsd:brainstorm [topic]` row
- Usage examples section includes a Brainstorming subsection with at least one example
- Row follows the existing table format (Command | Purpose | When to Use)

</task>

<task id="3" title="Add brainstorm to README.md">

**What:** Add `/gsd:brainstorm [topic]` to the Utilities command table in `README.md`.

**Files:** `README.md`

**Steps:**

1. Read `README.md`
2. Find the "### Utilities" section (around line 510) and its command table
3. Add a new row after the `/gsd:linear` entry:

```markdown
| `/gsd:brainstorm [topic]` | Collaborative brainstorming that produces a design doc and routes into GSD creation |
```

4. Follow the existing table format: `| command with args | one-line description |`

**Verification:**
- Utilities table includes `/gsd:brainstorm [topic]` row
- Description is concise (one line) and consistent with other files
- Row follows the existing two-column table format

</task>

## Verification Criteria

1. **DOCS-01 coverage:** `get-shit-done/workflows/help.md` contains a "### Brainstorming" section with `/gsd:brainstorm` entry including purpose, flow steps, and usage syntax.
2. **DOCS-02 coverage:** `docs/USER-GUIDE.md` has `/gsd:brainstorm [topic]` in the command reference table AND a usage example showing how to use the command.
3. **DOCS-03 coverage:** `README.md` Utilities table has a `/gsd:brainstorm [topic]` row.
4. **Consistency:** All three files describe the command's purpose using consistent language (brainstorming session, design doc output, optional routing to milestone/project creation).
