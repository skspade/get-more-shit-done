---
phase: 91-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/templates/MILESTONE-UAT.md
  - commands/gsd/uat-auto.md
autonomous: true
requirements:
  - CFG-03
must_haves:
  truths:
    - "MILESTONE-UAT.md template defines YAML frontmatter with status, milestone, browser, started, completed, total, passed, failed fields"
    - "MILESTONE-UAT.md template defines a results table with columns #, Phase, Test, Status, Evidence"
    - "MILESTONE-UAT.md template defines a gaps section using identical schema to MILESTONE-AUDIT.md (truth, status, reason, severity)"
    - "The /gsd:uat-auto command spec exists with name, description, argument-hint, and allowed-tools in YAML frontmatter"
    - "The command spec delegates to workflows/uat-auto.md"
  artifacts:
    - path: "get-shit-done/templates/MILESTONE-UAT.md"
      provides: "UAT results artifact template"
      contains: "status:"
    - path: "commands/gsd/uat-auto.md"
      provides: "UAT auto command specification"
      contains: "gsd:uat-auto"
  key_links:
    - from: "commands/gsd/uat-auto.md"
      to: "workflows/uat-auto.md"
      via: "workflow delegation reference"
      pattern: "uat-auto.md"
---

<objective>
Define the MILESTONE-UAT.md artifact template and the /gsd:uat-auto command spec.

Purpose: Establish the output format contract for UAT results (consumed by plan-milestone-gaps.md in Phase 94) and the command entry point (workflow implementation in Phase 92). These are pure specification artifacts — no runtime code.

Output: `get-shit-done/templates/MILESTONE-UAT.md` template file; `commands/gsd/uat-auto.md` command spec.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-foundation/91-CONTEXT.md
@.planning/phases/91-foundation/91-RESEARCH.md
@commands/gsd/audit-milestone.md
@commands/gsd/ui-test.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MILESTONE-UAT.md template</name>
  <files>get-shit-done/templates/MILESTONE-UAT.md</files>
  <action>
    Create the MILESTONE-UAT.md template at `get-shit-done/templates/MILESTONE-UAT.md`.

    The template must include:

    1. **YAML frontmatter** with these fields:
       - `status`: passed | gaps_found
       - `milestone`: {milestone} (placeholder)
       - `browser`: chrome-mcp | playwright
       - `started`: ISO 8601 timestamp placeholder
       - `completed`: ISO 8601 timestamp placeholder
       - `total`: number placeholder
       - `passed`: number placeholder
       - `failed`: number placeholder

    2. **Results table** section:
       ```
       ## Results

       | # | Phase | Test | Status | Evidence |
       |---|-------|------|--------|----------|
       ```

    3. **Gaps section** in the markdown body (NOT in frontmatter — this avoids the known extractFrontmatter limitation with nested array-of-objects). Use identical core schema to MILESTONE-AUDIT.md gaps:
       ```
       ## Gaps

       - truth: "{expected behavior}"
         status: failed
         reason: "{why it failed}"
         severity: major | minor
         evidence: "{path to screenshot}"
         observed: "{what was actually observed}"
       ```

       The four core fields (truth, status, reason, severity) match MILESTONE-AUDIT.md exactly. The evidence and observed fields are additive UAT-specific metadata.

    Use `{placeholder}` markers for values that get filled at runtime. Include a comment block at the top explaining the template's purpose and the gap schema compatibility requirement.
  </action>
  <verify>File exists at get-shit-done/templates/MILESTONE-UAT.md and contains "status:", "## Results", "## Gaps", "truth:", "severity:"</verify>
  <done>MILESTONE-UAT.md template exists with YAML frontmatter (status, milestone, browser, started, completed, total, passed, failed), results table (#, Phase, Test, Status, Evidence columns), and gaps section using truth/status/reason/severity schema</done>
</task>

<task type="auto">
  <name>Task 2: Create /gsd:uat-auto command spec</name>
  <files>commands/gsd/uat-auto.md</files>
  <action>
    Create the command spec at `commands/gsd/uat-auto.md` following the existing command spec pattern from `commands/gsd/audit-milestone.md` and `commands/gsd/ui-test.md`.

    YAML frontmatter:
    ```yaml
    ---
    name: gsd:uat-auto
    description: Run automated UAT session against a live web application
    argument-hint: "[--timeout <minutes>]"
    allowed-tools:
      - Read
      - Glob
      - Grep
      - Bash
      - Write
    ---
    ```

    Note: No `Task` in allowed-tools — the UAT workflow runs inline without subagent spawning per the design constraint in CONTEXT.md.

    Body should include:
    - `<objective>` — Run automated UAT using Chrome MCP (primary) or Playwright (fallback) to verify user-facing web UI behavior at milestone level
    - `<execution_context>` — Reference to `@~/.claude/get-shit-done/workflows/uat-auto.md` (workflow file created in Phase 92)
    - `<context>` — Describes that config is loaded from `.planning/uat-config.yaml`, tests discovered from phase directories
    - `<process>` — High-level workflow steps: load config, discover tests, detect browser, start app, execute tests, write results, commit, exit. Note that workflow implementation is Phase 92; this spec defines the contract only.

    Keep the spec concise — it's a thin wrapper that delegates to the workflow file.
  </action>
  <verify>File exists at commands/gsd/uat-auto.md and contains "gsd:uat-auto", "allowed-tools:", "uat-auto.md"</verify>
  <done>Command spec exists with proper YAML frontmatter (name, description, argument-hint, allowed-tools) and delegates to workflows/uat-auto.md</done>
</task>

</tasks>

<verification>
- MILESTONE-UAT.md template has YAML frontmatter with all required fields
- MILESTONE-UAT.md gaps section uses truth/status/reason/severity schema (identical to MILESTONE-AUDIT.md)
- uat-auto.md command spec has valid YAML frontmatter
- uat-auto.md references workflows/uat-auto.md for delegation
</verification>

<success_criteria>
- MILESTONE-UAT.md format is defined with YAML frontmatter and results table (CFG-03)
- Gap schema matches MILESTONE-AUDIT.md core fields (CFG-03)
- /gsd:uat-auto command spec exists with arguments, flags, and workflow delegation (success criterion 4)
</success_criteria>

<output>
After completion, create `.planning/phases/91-foundation/91-02-SUMMARY.md`
</output>
