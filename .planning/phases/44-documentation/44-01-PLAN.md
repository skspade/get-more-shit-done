---
phase: 44-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/help.md
  - docs/USER-GUIDE.md
  - README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "help.md contains a PR Review section with /gsd:pr-review command reference including all flags (--ingest, --quick, --milestone, --full, aspects)"
    - "help.md contains usage examples for pr-review covering fresh, ingest, force quick, force milestone, and aspect passthrough modes"
    - "help.md Common Workflows section includes a PR Review entry"
    - "USER-GUIDE.md Brownfield & Utilities table includes /gsd:pr-review row"
    - "USER-GUIDE.md contains a PR Review subsection documenting the end-to-end workflow (capture, dedup, scoring, routing)"
    - "USER-GUIDE.md PR Review section includes a flags table and routing heuristic explanation"
    - "README.md Commands table includes /gsd:pr-review row"
  artifacts:
    - get-shit-done/workflows/help.md
    - docs/USER-GUIDE.md
    - README.md
  key_links:
    - "help.md PR Review section references the same flags as commands/gsd/pr-review.md argument-hint"
    - "USER-GUIDE.md PR Review section matches the workflow steps in get-shit-done/workflows/pr-review.md"
    - "README.md pr-review row uses concise description consistent with command spec"
---

<objective>
Add /gsd:pr-review documentation to all three project documentation files.

Purpose: Users can discover and learn the PR review workflow from help.md (command reference), USER-GUIDE.md (end-to-end workflow), and README.md (command table).
Output: Updated help.md, USER-GUIDE.md, README.md with pr-review documentation.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-documentation/44-CONTEXT.md

# Source files for accurate documentation
@commands/gsd/pr-review.md
@get-shit-done/workflows/pr-review.md

# Files to modify (read for existing patterns)
@get-shit-done/workflows/help.md
@docs/USER-GUIDE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PR Review section to help.md</name>
  <files>get-shit-done/workflows/help.md</files>
  <action>
Add a PR Review section to help.md in TWO locations:

**1. Command reference section — insert after the Brainstorming section (after line ~202, after "Result: Design doc..."):**

```markdown
### PR Review

**`/gsd:pr-review [--ingest] [--quick|--milestone] [--full] [aspects...]`**

Run a fresh PR review or ingest an existing one, extract structured findings, and route to quick task or milestone.

- Fresh mode (default): invokes the PR review toolkit, captures and parses findings
- Ingest mode (--ingest): paste a pre-existing review summary
- Deduplicates findings by file proximity (20-line threshold)
- Scores findings and routes: score >= 5 to milestone, < 5 to quick task
- Flag overrides: --quick or --milestone bypass scoring
- --full adds plan-checking and verification to quick route
- Aspects passthrough: pass review focus areas (e.g., security, performance)

Usage: `/gsd:pr-review` (run fresh review)
Usage: `/gsd:pr-review --ingest` (paste existing review)
Usage: `/gsd:pr-review --quick` (force quick task route)
Usage: `/gsd:pr-review --milestone` (force milestone route)
Usage: `/gsd:pr-review security performance` (focus on specific aspects)

Result: Quick task in `.planning/quick/` or new milestone, plus permanent report in `.planning/reviews/`
```

**2. Common Workflows section — insert a "PR Review" block after the "Brainstorming" block (after line ~545):**

```markdown
**PR Review:**

```
/gsd:pr-review                            # Run fresh review, auto-route findings
/gsd:pr-review --ingest                   # Paste existing review summary
/gsd:pr-review --quick                    # Force quick task route
/gsd:pr-review security performance       # Focus review on specific aspects
```
```

Follow the exact formatting patterns used by the Brainstorming section: same heading hierarchy, same code block style (no language tag), same description line format.
  </action>
  <verify>
    <automated>grep -c "pr-review" get-shit-done/workflows/help.md | test $(cat) -ge 5</automated>
  </verify>
  <done>help.md contains /gsd:pr-review command reference with all flags, 5 usage examples, and a Common Workflows entry</done>
</task>

<task type="auto">
  <name>Task 2: Add PR Review section to USER-GUIDE.md</name>
  <files>docs/USER-GUIDE.md</files>
  <action>
Add PR Review documentation to USER-GUIDE.md in TWO locations:

**1. Brownfield & Utilities command table (~line 312) — add a new row after the `/gsd:brainstorm` row:**

```markdown
| `/gsd:pr-review [flags] [aspects...]` | Route PR review findings to quick task or milestone | Have PR review feedback to act on |
```

**2. Usage Examples section — insert a new "### PR Review" subsection after the "### Brainstorming" subsection (after line ~565, after the brainstorming flow description):**

```markdown
### PR Review

Route PR review findings into GSD workflows. Runs a fresh review or ingests an existing one, deduplicates findings by file proximity, scores for complexity, and routes to a quick task or milestone.

#### Pipeline

```
  /gsd:pr-review
         |
         +-- Capture (fresh toolkit review or --ingest paste)
         |
         +-- Parse findings (severity, file, line, fix suggestion)
         |
         +-- Deduplicate by file proximity (20-line threshold)
         |     Group overlapping findings transitively
         |
         +-- Score: +2 critical, +1 important, +1 per 5 files
         |
         +-- Route
               |
               +-- Score < 5 --> Quick task (one task per file-region group)
               +-- Score >= 5 --> New milestone
```

#### Flags

| Flag | Effect |
|------|--------|
| *(none)* | Run fresh review, auto-route based on scoring |
| `--ingest` | Paste a pre-existing review summary instead of running fresh |
| `--quick` | Force quick task route (skip scoring) |
| `--milestone` | Force milestone route (skip scoring) |
| `--full` | Add plan-checking and verification to quick route |

#### Routing Heuristic

When no flag override is provided, GSD scores findings: +2 per critical, +1 per important, +1 per 5 distinct files touched. Score >= 5 routes to a new milestone; < 5 routes to a quick task with one task per file-region group.

#### Examples

```
# Run a fresh PR review with auto-routing
/gsd:pr-review

# Paste an existing review summary
/gsd:pr-review --ingest

# Force quick task route regardless of score
/gsd:pr-review --quick

# Force milestone route regardless of score
/gsd:pr-review --milestone

# Focus review on specific aspects
/gsd:pr-review security performance

# Quick task with plan-checking and verification
/gsd:pr-review --full
```

After completion, GSD displays a banner with route taken, report path, and artifacts created. The permanent review report at `.planning/reviews/YYYY-MM-DD-pr-review.md` serves as an audit trail.
```

Follow the exact formatting patterns used by the Linear Integration and Brainstorming sections: same heading hierarchy (### for section, #### for subsections), same flags table format (| Flag | Effect |), same code block style (no language tag).
  </action>
  <verify>
    <automated>grep -c "pr-review" docs/USER-GUIDE.md | test $(cat) -ge 5</automated>
  </verify>
  <done>USER-GUIDE.md contains /gsd:pr-review row in Brownfield & Utilities table, and a PR Review subsection with pipeline diagram, flags table, routing heuristic, and 6 usage examples</done>
</task>

<task type="auto">
  <name>Task 3: Add PR Review row to README.md Commands table</name>
  <files>README.md</files>
  <action>
Add a `/gsd:pr-review` row to the Commands table in README.md. Insert after the `/gsd:quick` row (line ~87):

```markdown
| `/gsd:pr-review` | Route PR review findings to quick task or milestone |
```

Follow the exact table format: `| Command | What it does |` columns, concise one-line description, same backtick formatting as other rows.
  </action>
  <verify>
    <automated>grep -c "pr-review" README.md | test $(cat) -ge 1</automated>
  </verify>
  <done>README.md Commands table includes /gsd:pr-review row with concise description after /gsd:quick</done>
</task>

</tasks>

<verification>
- [ ] help.md has a "### PR Review" section under command reference with full flag documentation
- [ ] help.md has 5 usage examples covering all modes
- [ ] help.md Common Workflows has a PR Review block
- [ ] USER-GUIDE.md Brownfield & Utilities table has /gsd:pr-review row
- [ ] USER-GUIDE.md has "### PR Review" section with pipeline diagram, flags table, routing heuristic, and examples
- [ ] README.md Commands table has /gsd:pr-review row
- [ ] All documentation follows existing formatting patterns exactly
</verification>

<success_criteria>
- help.md includes `/gsd:pr-review` with argument reference and usage examples
- USER-GUIDE.md documents the PR review workflow end-to-end (capture, dedup, scoring, routing)
- README.md command table includes `/gsd:pr-review` entry
</success_criteria>

<output>
After completion, create `.planning/phases/44-documentation/44-01-SUMMARY.md`
</output>
